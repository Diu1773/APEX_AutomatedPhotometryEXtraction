# APEX 연구 틀 — 2026-08-28

논문 한 편 위로 한 단계 올라가서, 이 작업이 **무엇을 위한 것이고 무엇에 답하는지**를
잡는다. 논문·검증·개발의 우선순위가 여기서 나온다.

**표기**: 「기록됨」은 레포·메모리·이전 세션에 근거가 있는 것,
**「확인 필요」는 내가 정할 수 없고 사용자만 답할 수 있는 것**이다. 지어내지 않았다.

---

## 1. 궁극적 목표

### 기록에서 지지되는 형태

> **시설 파이프라인이 없는 관측자가, 자기가 찍은 원시 영상에서 출판 가능한 과학
> 산출물까지 스스로 갈 수 있게 한다.**

근거는 원체험이다 — 학부 1 학년 때 HOPS 로 외계행성 통과는 결과를 냈는데, 성단
색-등급도를 하려니 IRAF 앞에서 막혔다. **한 사람이 한 도구로는 되고 다른 도구로는
안 됐다.** 그때 막힌 셋(설치·문서·파라미터 불투명)이 설계 요구가 됐다.

### 확인 필요 — 이 목표의 지평이 어디까지인가

기록만으로는 셋 중 어느 것인지 정할 수 없다. **셋은 논문 편수도, 검증 범위도,
개발 종료 시점도 다르게 만든다.**

- **APEX 하나를 끝까지 만든다.** 측광 사슬을 완성하고 남들이 쓰게 한다.
  끝은 v1.0 릴리즈와 논문 한 편
- **관측부터 분석까지의 사슬 전체를 세운다.** ASTERION(관측 실행)·AstralImage
  (영상 처리)·APEX(측광·분석)·시잉 예측(관측 계획)이 실제로 그 네 조각이다.
  그러면 APEX 는 한 부품이고 논문도 그 자리에서 쓴다
- **접근성 자체를 연구 주제로 삼는다.** 「왜 어떤 도구는 초심자를 통과시키고 어떤
  도구는 막는가」. 그러면 산출물이 소프트웨어가 아니라 교육·도구론 쪽 논문이 된다

**지금 문서·코드가 가장 강하게 지지하는 것은 첫째다.** 다만 프로젝트 지도를 보면
둘째가 이미 진행 중이다.

---

## 2. 배경

### 2.1 분야의 상태 — 무엇이 비어 있나

측광에 필요한 **개별 연산은 이미 충분히 갖춰져 있다.** IRAF/DAOPHOT, SExtractor,
Astropy·photutils·SEP·ccdproc 가 검출·측광·좌표 변환의 표준 구현을 제공한다.

**비어 있는 것은 연산이 아니라 범위다.**

| 도구 | raw 부터 | 성단 색-등급도 | 다중밤 광도곡선 | 창 |
|---|---|---|---|---|
| AstroImageJ · HOPS | O | **X** | O | O |
| Siril · C-Munipack | O | **X** | O | O |
| THELI | O | **X**(목록에서 멈춤) | X | O |
| AutoPhOT · prose · phoptic · PhoPS | 부분 | X | O | **X** |
| ASteCA · MATCH · BASE-9 | X | O(목록 입력) | X | **X** |
| IRAF/DAOPHOT | X | 측광까지 | 가능 | **X** |

**raw 에서 시작해 성단 색-등급도와 다중밤 광도곡선 둘 다로 갈 수 있는 창 기반
도구를 조사 범위에서 찾지 못했다.**

**주의**: 이 빈칸은 「아무도 안 했다」가 아니라 **「한 도구 안에서 안 했다」**이다.
사람들은 도구를 갈아타며 한다. **그러니 주장은 「불가능했다」가 아니라 「갈아타지
않아도 된다」여야 한다.**

### 2.2 왜 그 범위가 문제인가 — 도구를 갈아탈 때 무엇이 깨지나

이게 이 연구의 진짜 배경이고, 아직 아무도 이걸로 쓰지 않았다.

**여러 밤·여러 필터의 측광에서 분석의 기준은 한 프레임의 검출 목록이 아니라
프레임을 건너 유지되는 천체의 신원이다.** ALLFRAME(Stetson 1994)이 마스터 목록으로
한 일이 그것이다. 도구를 갈아타면 그 신원이 도구 경계에서 끊기고, **끊긴 자리는
최종 산출물에서 안 보인다** — 색지수가 조금 이상하거나 광도곡선에 잡음이 낀 것으로만
보인다.

### 2.3 저자의 상태 — 왜 이 사람이 이걸 하나

- 관측 실데이터를 스스로 만들고 있다(Moravian C3-61000, 성단·변광성)
- 시설 파이프라인이 없다 — 보정부터 직접 책임진다
- **AI 보조 개발로 1 인 완성이 가능해진 시점**에 시작했다(2025 여름)

마지막 항목이 이 연구의 조건이자 부담이다. **혼자 만든 도구에는 빌려 올 신뢰가
없다.**

---

## 3. 연구문제

**실제로 답해야 했던 것들**이다. 순서는 발견된 순서이고, 뒤로 갈수록 일반화된다.

### Q1. 새로 만든 도구가 낸 숫자를 남이 믿게 하려면 무엇을 보여야 하는가

IRAF 는 검증이 없는 학회 논문 두 편으로 800 회 넘게 인용됐고, AstroImageJ 도 27 쪽에
검증 절이 없다. **두 도구가 신뢰를 얻은 경로는 논문이 아니라 오랜 사용이었다.**
APEX 에는 그 시간이 없다.

**→ 그러면 무엇을 측정해야 하는가.** 이게 논문의 질문이 됐다.

### Q2. 단계마다 표준 구현과 맞으면 최종 결과도 맞는가

**아니다.** 두 번 확인했다.

- 구경보정에서 성장곡선을 격자점에서 읽어 값이 0.1 등급 틀렸는데 최종 색-등급도는
  사실상 안 바뀌었다(영점이 +96.7 mmag 로 상쇄, 보정등급 변화 중앙값 −0.06 mmag)
- PSF 측광에서 프레임 영점이 회차 사이 90 mmag 어긋났는데 실전에선 안 보였다

**공통점은 둘 다 프레임마다 일정한 값이라 뒤의 영점 단계가 통째로 먹었다는 것이다.**

**→ 단계별 일치는 최종 정확도의 충분조건도 필요조건도 아니다.**

### Q3. 그러면 어떤 오차가 최종까지 살아남는가

프레임 상수는 흡수되고, **별마다·밴드마다·위치마다 다른 양은 흡수되지 않고 최종
물리량까지 간다.** 확인된 것 넷.

- **검출기 gain 을 헤더에서 믿으면** 등급표는 멀쩡한데 **오차막대와 선택 기준이
  조용히 망가진다**(기기등급은 카운트의 비라 gain 이 약분된다)
- **별이 붐비면** 중앙값은 먹히고 흩어짐은 살아남는다(구경 대 PSF 불일치가
  ≥6 FWHM 에서 0.0241, <1.5 FWHM 에서 0.1421 mag)
- **어두운 쪽 검증에 Gaia BP 기반 참조를 쓰면** 밴드마다 범인이 바뀐다
- **밴드마다 영점 기준을 다르게 잡으면** 프레임 상수처럼 보이지만 **밴드 사이의
  차이라 색으로 넘어간다**(M67 금속량 −0.83 → 한 표준계로 맞추니 +0.06)

### Q4. 한 창 안에 사슬을 다 넣으면 고유한 위험이 생기는가

**생긴다.** Q3 의 넷째가 그 예다 — 단계가 한 창 안에 있으면 밴드마다 다른 기준을
고르기가 **오히려 쉬워진다.** 도구를 갈아탈 때는 경계에서 한 번 멈추지만, 한 창
안에서는 안 멈춘다.

**→ 통합의 이득만이 아니라 통합의 위험도 이 연구의 결과다.**

### Q5. 창과 명령줄이 같은 계산을 하게 하려면 구조를 어떻게 짜는가

**GUI 천문 소프트웨어의 가장 오래된 고장이 화면과 스크립트가 조용히 다른 값을
내는 것이다.** APEX 는 계층 계약(`apex/pipeline` 가 `apex.gui` 를 임포트하지 않는다)과
계산 객체 공유로 답했다.

### Q6. 산출물이 자기 출처를 말하게 하려면 무엇을 남겨야 하는가

**「기록 파일이 있다」와 「그 파일이 이력이다」는 다른 사실이다.** 부분 실행 한 번이
전체 실행의 기록을 지웠고(`pipeline_run.json` 이 덮어쓰기), 실행이 끝난 시점의
설정은 산출물을 만든 설정이 아니다(`parameters_used.json`).

**→ 덧붙이기만 하는 저널이 답이었다.** 그리고 **소급은 안 된다** — 지금 결과 폴더
전부가 「미상」이다.

### Q7. 몇 년 뒤에 같은 수가 다시 나오게 하려면 무엇이 필요한가

**동일한 코드의 두 실행 사이에서 PSF 측정 22,305 개 중 다섯이 4.8e-05 mag
움직였고, 원인이 scipy 1.18.0 대 1.17.1 이었다.** 다섯 다 이미 불량으로 걸러진
별이라 발표 그림에 안 들어갔지만, 다음번이 그러리란 보장이 없다.

**→ 이게 APEX 밖으로 일반화되는 유일한 결과다.**

---

## 4. 연구목적

Q1~Q7 을 목적으로 다시 쓰면 셋이다.

**첫째, 도구를 만든다.** 시설 파이프라인이 없는 관측자가 원시 영상에서 성단
색-등급도 또는 다중밤 광도곡선까지 **도구를 갈아타지 않고** 갈 수 있게 한다.
창에서 탐색하고 명령줄에서 반복하되 **둘이 같은 계산을 부르게** 한다.

**둘째, 그 도구가 낸 숫자를 믿을 근거를 만든다.** 오랜 사용 대신 측정으로.
그러려면 **무엇을 재야 하는지부터 정해야 하고**, 그 답이 「그 단계의 오차가 최종
물리량에 얼마나 남는지」였다.

**셋째, 그 과정에서 알게 된 것을 남긴다.** 헤더 gain 을 믿으면 안 되는 것, 어두운
쪽 검증에 BP 기반 참조를 쓰면 안 되는 것, 밴드별 영점 기준이 색으로 새는 것,
의존성 한 판올림이 측정을 움직이는 것. **이건 APEX 사용자가 아니라 측광하는
사람 전부에게 해당한다.**

---

## 5. 연구주제

**한 줄**: 여러 프레임을 지나며 천체의 신원을 유지하는 측광 파이프라인을 만들고,
그 파이프라인이 낸 값을 어디까지 믿을 수 있는지 단계별로 재는 일.

**무엇에 대한 연구가 아닌지도 적는다.**

- **새 측광 알고리즘에 대한 연구가 아니다.** 검출·측광·우주선 제거는 기존 구현을
  부른다. 자체 구현은 보정 프레임 짝짓기, 지역 Gaia 목록 측성해, 프레임 간
  천체 짝짓기와 강제 측광이다
- **정확도 경쟁이 아니다.** 기존 구현을 능가한다고 주장하지 않는다. 확인하는 것은
  **어느 범위 안에서 일치하고 그 범위를 벗어나는 조건이 무엇인가**이다
- **교육 효과에 대한 연구가 아니다.** 수업 설문이 있지만 논문에 안 쓴다
  (N=8, 대조군 없음, 진행자가 저자, 인식 조사이지 수행 측정이 아님)

---

## 6. 방법

### 6.1 개발 방법

**계층 계약을 코드가 강제한다.** `gui → analysis/core/utils`, `analysis → utils`
방향만 허용하고 역방향을 금지한다. 계산은 창 밖에 두고 창이 그것을 부른다.
**계약을 말로 적는 것과 검사가 강제하는 것은 다르다.**

**경로를 문자열로 조립하지 않는다.** `step_paths*.py` 가 산출물 경로의 정본이다.

**설정은 한 곳에서만 온다.** `apex_config.json` 이 정본이고 TOML 은 1 회 변환 후
무시한다. 로더가 두 단이라 **맵에만 넣은 키는 조용히 사라지므로** 감사 명령이 있다.

**의존성은 범위와 잠금을 둘 다 둔다.** 설치가 패치 릴리스로 깨지지 않게 범위를
두고, 논문 수치를 낸 조합을 잠금 파일에 못 박는다. **둘이 어긋나지 않게 검사가
지킨다.**

**산출물은 덧붙이기만 하는 저널로 자기를 설명한다.** 헤드리스는 스텝이 실제로 읽은
값을, 창은 마무리 시점의 설정을 남기고 **두 주장을 구분해 적는다.**

### 6.2 검증 방법

**힘을 어디에 쓸지부터 정한다.** 두 축의 조합이다 — 그 단계가 기존 구현을 부르는가
자체 구현인가, 그리고 그 단계의 오차가 최종 물리량에 얼마나 남는가.
**모든 단계에 면제 없는 바닥(계약 검사)을 두고 그 위에 쌓는다.**

**오차를 주입해 최종까지 흘린다.** 주입은 종류를 갈라서 한다 — 프레임 전체에 걸린
상수, 색에 의존하는 것, 시야 위치에 의존하는 것, 밝기에 의존하는 것, 대기질량과
상관된 것, 밀도에 의존하는 것, 그리고 위치(측성) 오차.
**주입마다 하류를 다시 돌렸는지 고정했는지를 적는다** — 프레임 상수는 그 선택에
따라 민감도가 0 이거나 1 이고 중간이 없다.

**단계마다 독립 구현과 맞춘다.** 그리고 **그 구현이 어느 점에서 독립인지를
논증한다** — 같은 편향을 공유하는 둘의 일치는 검증이 아니다. B 필터 조사가 결론에
도달한 것은 두 기준이 **밴드마다 반대 방향으로** 어긋났기 때문이다.

**합성 자료로 참값을 알고 재고, 실측 자료로 문헌값을 되찾는지 본다.**
앞은 내적 타당도, 뒤는 외적 타당도다.

**통과 못 한 것을 싣는다.** 실패한 검사를 담을 자리를 미리 만들어 둔다.

### 6.3 이 방법의 한계 — 지금 확정된 것

- **전 사슬을 끝까지 검증한 것은 검출기 한 대다.** 성단이 다섯이어도 다중 기기가
  아니다. **그래서 지금 보고되는 실패의 원인을 소프트웨어와 검출기로 배정할 수
  없다** — PSF 가 한 기기에서 지는 것, 영점이 90 mmag 흔들리는 것, M67 금속량이
  안 맞는 것 셋 다
- **등시선 적합은 축퇴한 역문제다.** 문헌값 넷 중 둘을 고정해야 나머지 둘이 온다
- **성단원 판정을 하지 않는다.** 천체측정 멤버십 확률을 계산하지 않고, 마스터 표에
  그 열이 있으면 문턱만 적용한다
- **결과 폴더가 자기 출처를 말하지 못한다.** 저널 도입 이전 것은 소급 불가다

---

## 7. 이 틀에서 논문이 어디인가

**논문 한 편(PASP)은 목적 셋 중 첫째와 둘째를 담고, 셋째는 그 안에 부분으로
들어간다.** 목적 셋을 다 담기에 한 편은 좁고, 나누기에는 셋이 서로를 필요로 한다.

지금 목차(`OUTLINE_20260828.md`)와의 대응:

| 연구목적 | 논문 어디 |
|---|---|
| 도구를 만든다 | 2 절 (8.5 쪽) |
| 낸 숫자를 믿을 근거를 만든다 | 3 절 (10 쪽) |
| 알게 된 것을 남긴다 | 3.2·3.9·3.10 과 4 절, 그리고 재현 가능성 절 |

**Q4(통합의 고유 위험)가 지금 목차에서 자기 자리가 없다.** 3.9(밴드별 기준이 색으로
샘)에 들어가 있지만, 이건 **이 논문에서 가장 새로운 각도**다 — 통합 도구를 옹호하는
논문이 통합의 위험을 스스로 찾아낸 자리다.

---

## 8. 사용자만 답할 수 있는 것

1. **궁극적 목표의 지평** — 1 절의 셋 중 어디인가. **논문 편수·검증 범위·개발 종료
   시점이 여기서 갈린다**
2. **APEX 가 사슬의 부품인가 완제품인가** — ASTERION·AstralImage 와의 관계.
   부품이면 인터페이스가 설계 요구가 되고, 완제품이면 안 그렇다
3. **누가 쓸 것을 상정하는가** — 「나와 같은 처지의 사람」인지, 「내 수업 학생」인지,
   「일반 공개」인지. **이건 문서·배포·지원 부담을 완전히 다르게 만든다**
4. **언제 끝인가** — v1.0 릴리즈의 조건이 무엇인지. 지금 TRACK 의 목표는
   「APEX 소프트웨어 v1.0 릴리즈」인데 그 조건이 적혀 있지 않다
5. **Q4 를 논문의 앞으로 낼 것인가** — 통합의 고유 위험은 이 연구에서 가장 새로운
   각도인데 지금은 3 절 안쪽 소절이다
