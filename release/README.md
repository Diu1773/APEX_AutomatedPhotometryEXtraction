# Release Output

`build.bat` writes final distributables to `release/Setup/`.

Share the files inside `release/Setup/` with users:

- `setup.exe`: installer for normal users
- `APEX-Portable-<version>-x64.zip`: portable bundle for testing/manual use
