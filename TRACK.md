# TRACK: apex-dev — APEX 소프트웨어 v1.0 릴리즈

> 이 파일이 이 트랙 상태의 **유일한 진실**이다. 세션을 열면 여기부터 읽는다.
> 세션을 끝내기 전에 `## 지금` / `## 다음 3개` / `## 함정`을 반드시 다시 쓴다.
> 논문 트랙은 별도: [TRACK_PAPER.md](TRACK_PAPER.md)

## 완료 정의

측광 파이프라인이 실관측 데이터에서 GUI·헤드리스 양쪽으로 완주하고,
회귀 테스트 전량 통과 상태로 v1.0 릴리즈 태그가 붙는다.

## 오라클 — 이 트랙의 검증 명령

```bash
.venv-deploy/Scripts/python.exe -m pytest tests/ -q
```

- **통과 기준:** 1,367 passed, 0 failed, 0 skipped  (2026-08-21, 스텝 8 공용 리더 포함)
- **산수가 맞는지 같이 본다:** 1,312 → +20(선별 14 · 파이프라인 6) = 1,332
  → +10(2 차 색항) = 1,342 → +13(창 선택 8 · 후보 표 5) = 1,355
  → +1(식쌍성 P/2) = 1,356 → +2(필터 규칙 공유) = 1,358
  → +1(후보 표 판정 열) = 1,359 → +2(스텝 9 자기출력 우선) = 1,361
  → +2(보고서 writer 통합) = 1,363 → +4(스텝 8 공용 리더) = 1,367.
  총계만 보면 조용히 사라진 테스트를 놓친다.
- **소요:** 약 8분 36초 ~ 13분 (E: 부하에 따라)
- **마지막 실행:** 2026-08-21 — **1,367 전량 통과, 스킵 0** (13분 19초,
  스텝 11 이 두 기록을 다 읽게 한 뒤).
- **같은 코드에서 한 번 `3 failed, 1309 passed` 가 나왔다 — 재현 안 됨.**
  무작위 순서 플러그인은 설치돼 있지 않으므로 순서 의존이 아니다. 그 실행만
  549초였고(맑은 실행은 432~773초) 당시 내가 같은 E: 를 창 캡처로 두드리고 있었다.
  실패 이름은 로그가 잘려 못 건졌다. 이후 두 번 맑게 통과. **미해결로 남긴다** —
  오라클은 다른 E: 작업과 겹치지 않게 돌리고 `-rf` 로 파일에 남길 것.
- **스킵 0 을 기준에 넣은 이유**: 중복 삭제가 `test_detection_limit` 이 쓰는 보정
  프레임을 지웠을 때 그 파일은 **실패가 아니라 스킵**했다. 총계만 보면 통과처럼
  보였고 스킵 수만이 단서였다 (F-062).
- **스텝 11 작업 중 네 번째 실패가 나왔고, 표적 테스트로는 안 보였다.**
  `test_step12_period_layout.py` 가 `compute_ls` 를 **GUI 모듈에** monkeypatch
  하는데 그 호출이 `period_plot` 으로 옮겨갔다. 패치가 아무것도 가로채지 못하게
  됐고 테스트는 **빈 리스트를 대상으로 단언**했다. 이관 때 monkeypatch 대상이
  같이 움직이는지 확인할 것.
- **직전 실행에서 2 실패, 그리고 그건 이 세션이 낸 게 아니었다.** 레지스트리
  모양을 보는 두 테스트가 `LC == [1..7]` 을 주장하고 있었는데, LC 스텝 8 은
  **08-19 에 이미 들어가 있었다.** 스텝 8 을 만든 작업에서 표적 테스트만 돌리고
  전체를 안 돌려서 하루 동안 빨간 채로 있었다. 같은 형태의 사고가 08-17 에도
  있었다 — `test_psf_isochrone_integration.py` 가 잡은 **GUI 워커 생성 불가**
  (다중상속 순서, `64ae8f1`). 둘 다 «표적만 돌리면 안 보인다» 는 같은 원인이다.
  **스텝을 추가하거나 클래스 기반을 바꾸면 전체 스위트를 돌릴 것.**
- CI: `.github/workflows/tests.yml` 외 3개

## 지금

### 2026-08-21 — 헤드리스 워크스페이스로 창을 열자 둘이 더 나왔다

「다음 3개」 3 번(「파일이 생겼다는 것과 창이 그걸 읽는다는 것은 다른 주장이다」).
열어 보니 맞았고, 여는 과정에서 별개 버그를 하나 더 잡았다.

**1) GUI 가 지정한 워크스페이스를 조용히 바꾸고 있었다.** `--params other.json` 으로
열어도 `data_dir` 만 같으면 지난 세션의 `result_dir` 이 덮었다. **같은 원본을 두
result_dir 로 재처리하는 건 흔한 구성**이고(이 저장소의 `reprocess/YZBoo_2n` 와
`_fullrun_yzboo` 가 정확히 그것) 그때 가드가 통과한다. 코드 주석이 「그대로 두면
지정한 result_dir 이 조용히 바뀌어 엉뚱한 자료를 읽는다」고 막으려던 그 일이 계속
일어나고 있었다. `result_dir` 도 비교하게 고쳤다.

**2) 스텝 11 이 선택 기록을 창 파일에서만 읽었다.**

    고치기 전  g 비교성 0 개 · 선택파일 없음  → 릴리스 보류 8 건
    고친 뒤    g 비교성 5 개 · 체크성 176      → 보류 2 건

남은 2 건은 헤드리스가 g 하나만 선별했으니 i·r 에 근거가 없다는 **사실 그대로**다.
창 워크스페이스는 여전히 보류 0 건(회귀 없음). 리더 하나
(`read_step8_selection`)가 창 파일과 파이프라인 파일 둘 다 받는다.

### 2026-08-21 — 스텝 9 가 자기 출력을 읽고, 선별 보고서가 한 벌이 됐다

「다음 3개」 1·2 번. 둘 다 **전제가 틀린 채로 큐에 올라 있었다.**

**1) 스텝 9 가 자기 출력을 무시하고 있었다.** 커브를 저장하면
`comp_selection.json` 에 `comp_active_ids` 까지 적히고 **스텝 10 은 그걸 우선한다.**
그런데 스텝 9 자신은 다시 열 때 그 파일을 안 읽고 스텝 8 픽만 봤다. 이제 읽는다 —
「만든 것」이 「제안된 것」을 이긴다. project_state 가 없는 워크스페이스(다른 PC 로
복사)에서 마지막으로 실제 만든 목록이 살아난다.

**2) 선별 보고서가 두 벌이었는데, 그중 하나를 내가 만들었다.** 큐 항목은 «창에서
선별한 사용자는 그 표를 볼 방법이 없다» 였는데 **창은 예전부터
`comparison_screening_<필터>.csv` 를 쓰고 있었다.** 내가 오늘 만든 서비스가 같은
이름의 `.tsv` 를 옆에 쓰고 있었을 뿐이다 (F-085). 이제 창도 파이프라인도
`write_screening_report()` 하나를 부르고, 창이 늘 넣던 출처 열(필터·측광원·입력
컬럼)이 그 안으로 들어갔다.

이건 **내가 같은 날 함정으로 적어 둔 형태**다 — 「창과 헤드리스가 같은 것을 다른
파일에 쓰고 있지 않은지 본다」. 적어 놓고 몇 시간 뒤에 새로 만들었다.

**3) 그래서 나머지도 옮겼고, 셋이 아니라 넷이었다** (`b621218`). 다음 큐 항목을
쓸 때 전제를 먼저 확인했더니 창 인라인 파일이 넷이고 그중
`comparison_stability_<필터>.json` 은 **스텝 11 주기창이 읽는 파일**이었다. 즉
헤드리스로 만든 워크스페이스에서 스텝 11 을 열면 「어떤 비교성 위에 서 있는지」를
설명할 근거가 아예 없었다. 이제 넷 다 `write_selection_reports()` 하나에서 나온다.

창 고유 정보(손으로 거부한 별·시각)는 인자로 넘기고, 배치 실행은 그 둘이 없으니
**비워 두지 지어내지 않는다** — 테스트가 그걸 본다. 창 쪽 메서드는 72 → 32 줄.
선별 결과 자체는 안 변했다(창↔헤드리스 대조 재실행: 깔때기·앙상블·체크성 일치).

### 2026-08-21 — 창이 합집합을 쓰고 있었고, 그게 더 나빴다

「다음 3개」 1 번. 물음은 «헤드리스도 필터별 앙상블을 쓸지» 였는데, **창을 잘못
알고 있었다.** 창의 스텝 8 은 `selection_<필터>.json` 을 필터마다 쓰지만 스텝 9 는
그걸 필터별로 쓰지 않는다 — `_load_selection_ids()` 가 세트가 다르면 **합집합**을
만들어 하나로 넘기고, 커브를 만드는 `_build_ensemble_series()` 에는 필터 인자가
아예 없다 (F-083).

그래서 실제 대비는 **합집합 11 개 대 선별 6 개**다. 변광성 자신으로는 못 재니
(YZ Boo 는 1.2 mag 로 흔들린다) 그 11 개를 각각 체크성처럼 써서 밤별 영점을 뺀
robust 산포를 쟀다:

| 필터 | 합집합 11 | 선별 6 | 차이 |
|---|---:|---:|---:|
| g | 0.02816 | **0.02482** | −3.35 mmag |
| r | 0.02002 | **0.01687** | −3.15 mmag |
| i | 0.01230 | **0.01162** | −0.67 mmag |

**선별한 6 개가 전 필터에서 낫다.** 합집합은 다른 필터의 선별이 탈락시킨 별을 도로
넣기 때문이다. 그러니 고칠 쪽은 파이프라인이 아니라 창이었다 — 창의 씨앗도 이제
프레임이 가장 많은 필터의 세트를 쓴다. 「한 필터를 조용히 편들지 않으려고」 합집합을
쓰던 것인데, 편드는 것보다 탈락한 별을 되살리는 쪽이 비쌌다.

필터 선택 규칙은 이제 `target_config.filters_by_frame_count()` 하나이고 창과
파이프라인이 **같은 객체**를 부른다(테스트가 그걸 본다).

씨앗을 바꾸자 **그 아래 숨어 있던 결함이 드러났다.** 창을 열어 필드를 읽어 보니
사용자가 저장한 비교성 목록이 스텝 8 값으로 덮이고 있었다. 목록이 둘인데 하나만
저장되고 있었기 때문이다 — `comp_candidate_ids`(QC 후보 풀, 일부러 누적)는 저장되고
`comp_ids_list`(칸의 활성 목록, 실제로 커브를 만드는 쪽)는 저장도 복원도 안 됐다.
복원 직후 칸이 비니 생성자의 `_auto_load_ids()` 가 선택 파일로 채웠다. **씨앗이
합집합이던 동안에는 사용자 목록과 비슷해 안 보였다** (F-084).

실화면 확인 (`scratchpad/shot_lc10_seed.png`):

    씨앗            226,199,166,209,182,119  (6 개) · Active comps: 6
    저장 → 재개봉    119,166,182,199,209,226,273  →  그대로 유지

재현: `validation/ensemble_per_filter_cost.py`.

### 2026-08-21 — 후보 표를 밤 수로 흔들어 봤다, 그리고 전제가 틀렸다

「다음 3개」 2 번. 적어 둔 전제는 «밤이 늘면 별칭이 줄어 표가 짧아져야 한다» 였다.
YZ Boo 한 대상으로 밤만 바꿔 통제 실험한 결과 **정반대다**:

| 경우 | 점 | 밤 | 기준선 | 판정 | 후보 수 | 2위와 ΔBIC | rank1 | 문헌대비 |
|---|---:|---:|---:|---|---:|---:|---:|---:|
| 밤 1 만 | 81 | 1 | 0.035 d | INSUFFICIENT | 2 | 185.0 | 0.480914 | **+362 %** |
| 밤 2 만 | 283 | 1 | 0.128 d | AMBIGUOUS | 1 | — | 0.101930 | −2.08 % |
| 두 밤 전부 | 364 | 2 | 1.097 d | AMBIGUOUS | 8 | 50.8 | 0.104151 | **+0.06 %** |

**기준선이 길수록 별칭이 더 많이 「분해」되므로 표는 길어진다.** 답이 좋아질수록
행이 늘어난다(1 행 → 8 행, −2.08 % → +0.06 %). 표 길이는 확신의 척도가 아니다.

**ΔBIC 간격도 척도가 아니다.** 가장 틀린 실행(+362 %)이 간격이 가장 컸다(185.0).

**척도는 판정뿐이고, 그건 제대로 동작한다.** +362 % 짜리를 INSUFFICIENT 로 막고,
쓸 만한 둘을 AMBIGUOUS 로 표시한다. 표 맨 위 행의 비고에 그 판정이 같이 적힌다
(「rank 1 of 8 — best fit, but the run says ambiguous」).

**식쌍성 반주기**: 국내 아카이브에 식쌍성 자료가 없어 합성 신호(주·부 식 깊이가
다른 분리형)로 확인했다 — 표에 P 와 P/2 가 **둘 다** 올라온다. 관측 주장 아니고
표 동작 주장이다(`tests/test_lc_period_step.py`).

재현: `validation/period_candidates_vs_nights.py`.

### 2026-08-21 — LC 가 창 선택에서 출발해 8→11 을 완주한다

「다음 3개」 1 번. YZ Boo 로 돌려 보니 **막히는 자리는 과학이 아니라 파일이었다** —
창은 고른 별을 `selection_<필터>.json` 에 쓰고 헤드리스는 설정만 봐서, 창에서 별을
고른 워크스페이스에 대고 「어느 별인지 알아야 한다」고 거절했다. 이제 창이 적어 둔
선택을 읽고, 어느 파일에서 읽었는지 메시지에 적는다(`selected_by: window`).

돌려 본 결과 (같은 워크스페이스·같은 창 선택·364 프레임):

| | 창 저장본(08-01) | 헤드리스(08-21) |
|---|---|---|
| 추세제거 전 `diff_mag_raw` | — | 최대 0.40 mag 차이 |
| 추세제거 후 진폭 | 0.4417 mag | **0.4417 mag** |
| 잔차 RMS | 0.11049 | 0.11007 |
| 주기 4 개 | — | **소수 6 자리까지 동일** |
| 별칭 판정 | RESOLVED | AMBIGUOUS |

**앙상블 차이는 흡수된다.** (여기 처음 쓸 때 「창은 필터별 앙상블」이라고 적었는데
**틀렸다** — 창이 필터별로 쓰는 건 스텝 8 기록뿐이고, 스텝 9 는 세트가 다르면
**합집합**을 쓴다(`_load_selection_ids`). 실제 대비는 합집합 11 개 대 선별 6 개다.)
그래서 보정 전엔 0.40 mag 벌어지지만 밤·필터 6 군에 각각 영점을 맞추면 진폭이
소수 4 자리까지 같아진다.

**판정이 갈린 건 헤드리스가 맞다.** 창 저장본은 `night_id` 가 전부 0 — 두 밤을
1.10 일짜리 「하룻밤」으로 봤다. 그래서 leave-one-night-out 시행이 **0 회**였고
RESOLVED 가 공짜로 나왔다. 헤드리스는 밤 2 개(04-29·04-30)를 보고 2 회 중 1 회
일치(50 %)로 AMBIGUOUS 를 낸다 — 가장 긴 밤이 1.23 주기뿐이라 못 가린다는 게 사실
이고, D-014 의 후보 표가 바로 이 경우를 위한 것이다.

**같이 고친 것 둘:**
- 헤드리스 스텝 11 이 필터별만 돌고 **합본을 아예 안 했다.** 밴드별 124/119/21 점
  대 합본 364 점이라 별칭 해소력이 다르다. 이제 합본을 먼저 돌리고 필터별을
  교차검증으로 남긴다.
- **후보 표가 자기 목적을 방해하고 있었다.** D-014 가 「고르지 말고 후보를 내라」로
  닫혔으니 그 표가 산출물인데, 주기 순으로 정렬해 rank 1 이 12 행 중 6 번째에
  묻혀 있었다. 게다가 `strength` 열에 주기도 세기(0~1, 클수록 강함)와 원시
  BIC(−2400 근처, 작을수록 좋음)가 같이 들어가 **제일 강한 후보가 제일 약해
  보였다.** 이제 rank 순으로 앞세우고, BIC 는 1 위 대비 ΔBIC(0 이 최상)로 적고,
  모든 행에 그 수가 무엇인지(`strength_kind`)를 붙인다.

  YZ Boo 확인: rank 1 = 0.104151 d, **문헌 대비 +0.06 %**, 2 위와 ΔBIC 50.8 차이.
  판정이 AMBIGUOUS 여도 표 맨 위가 정답이고, 비고에 「1 위지만 이번 실행은 못
  가렸다」가 적힌다.

그림 `validation/fig_lc_headless_vs_window.png`.

### 2026-08-21 — 2 차 색항이 시야를 따라다니고 있었다 (`2eaf783`)

D-012 를 닫았다. 「R 밴드만 켜진다」가 전제였는데 실측 15 개 적합 중 10 개가
켜져 있었고 밴드를 안 가렸다. 진짜 문제는 **값이 시야마다 달랐다**는 것이다 —
카메라 한 대로 찍은 세 시야에서 R(V−R) 곡률이 −0.243 / −0.003 / +0.309 였다.
색항은 기기 속성이라 성단이 바뀌어도 같아야 한다.

한 시야의 곡률을 남에게 빌려주면 24 쌍 중 10 쌍이 **곡률을 안 쓰느니만 못했다**
(최악 +16.9 mmag). 그리고 **한 시야 안의 통계로는 못 가린다** — 가장 유의한
곡률(|ct2|/σ = 46)이 하필 가장 안 옮겨가는 것이라, 더 엄격한 검정을 붙였으면
더 확신을 갖고 채택했을 것이다.

이제 적합은 하되 `ct2_fitted`·`ct2_sigma` 로 기록만 하고, 적용은
`cmd.zp.quadratic_color_term` 에 측정해서 넣을 때만 한다. 그림
`validation/fig_d012_color_term.png`. **열린 결정이 0 이 됐다.**

### 2026-08-20 — 비교성 선별이 헤드리스에서도 돈다 (`20f71f9`)

배치 실행의 `auto` 는 비교성을 **카탈로그 순서**로 골라 왔다. 안정성으로 순위를
매기는 선별이 창 안에만 있었기 때문이다. 같은 YZ Boo 364 프레임에서 두 앙상블의
비교성 평균이 0.68 mag 벌어졌고 **어느 산출물도 두 실행이 다른 별을 썼다고 말하지
않았다.** 선별은 이제 `analysis/light_curve/comparison_screening.py` 이고 창이 그
함수를 부른다. 같은 워크스페이스·같은 플래그로:

    창       1357 measured → 232 coverage → 232 eligible → 30 pool → 6 adopted
    헤드리스 1357 measured → 232 coverage → 232 eligible → 30 pool → 6 adopted
    앙상블 source_id 6 개 전부 일치 · 체크성 일치

**폴백을 쓰면 파일이 그렇게 말한다** — `lc_target_selection.json` 의 `selected_by`
가 `stability`/`catalog_order`/`manual` 을 구분한다. 이게 없었으면 위 0.68 mag 은
다음에도 조용히 났을 것이다. 측정된 별 전부와 각자를 떨어뜨린 단계는
`comparison_screening_<필터>.tsv` 에 남는다.

창(스텝 9)도 같이 고쳤다 — 단축키 11 개를 상단 배너에, 값을 그 값 바꾸는 버튼
옆으로, 표 비율 2:1→2:3(9 열 중 6 열만 보이던 것 해소), 선별 깔때기 한 줄.
비포/애프터 `scratchpad/step9_before_after.png`.

### 2026-08-19~20 — LC 가 헤드리스로 완주한다 (deferred 없음)

커밋 9 개(`bedfdc2` … `7c9a522`). 기록:
`docs/audit/LC_HEADLESS_STEPS_8_TO_11.md`.

    CMD  1-12  (deferred 는 9 하나 — Master ID editor, 대화형이라 그대로 둔다)
    LC   1-11  (deferred 없음)

LC 는 7 에서 멈춰 있었다. 막고 있던 건 과학이 아니라 **계산이 창 안에 쓰여 있던
것**이다 — `apex/analysis/light_curve/` 의 14 개 모듈은 처음부터 Qt 를 안 썼다.

| 스텝 | 옮긴 양 | 창 저장본 대비 |
|---|---|---|
| 8 대상 선택 | 신규 | 설정에서 읽고, 없으면 막는다 |
| 9 라이트커브 | 1,149 줄 | 과학 열 전부 **0.000e+00** |
| 10 추세제거 + 그림 2 장 | 3,294 줄 | 26 열·적합표 6 행 **0.000e+00** |
| 11 주기분석 + 그림 1 장 | 0 줄 (호출자만) | 주기 4 개·채택 주기 **0.000e+00** |

창 세 개가 합쳐 **9,000 → 4,000 줄**이 됐고, 동일성은 검사 대상이 아니라 구조다
(창이 계산 객체를 상속한다).

**답이 틀리던 결함 다섯을 잡았다** — 전부 실제로 돌려 보거나 그림을 열어 봐야
보였다:

1. **LC 스텝 8 이 존재 이래 한 번도 통과한 적 없었다** — 마스터 카탈로그 이름을
   지어냈고 **테스트 픽스처가 같은 허구를 썼다** (F-054).
2. **별칭 분석을 안 돌려 9.2 % 틀린 주기로 위상을 접었다** (F-057).
3. **서비스가 `AMBIGUOUS`(못 가렸다)라고 했는데 1 순위를 채택**해 0.01 % 를
   4.07 % 로 만들었다 (F-058, AE UMa).
4. **밤 기준선을 지우는 보정 뒤에 보정본을 썼다** — 1.22 % 오차.
5. **죽은 좌표 폴백** — 스텝 8 을 안 거친 워크스페이스는 BJD 가 전부 NaN 이었다.

**내 판단이 틀린 것도 하나.** 스텝 10 을 「반나절 이상 재설계」로 보고했는데,
위젯 **접촉 횟수**만 세고 읽기·쓰기를 안 갈랐다. 경계는 이미 있었고
(`_sync_state_from_controls`) 한 세션에 끝났다 (F-059).

**디스크**: 중복 정리로 순 **41 GB** 회수 (진단 이미지 11.1 + 프레임·마스터
30.0), C: 1.65 GB. 그 과정에서 **레포 추적 파일 3 개**(F-061)와 **테스트가 쓰는
보정 프레임 4 개**(F-062)를 지웠다가 되돌렸다 — 둘 다 「남는 사본」 표를 남겨
둔 덕에 구했다.

**사용자 결정 셋을 닫았다** (D-013·D-014·D-015). D-014 는 **내가 질문을 잘못
낸 것**이 드러났다 — 「관측 판단이니 선생님이」로 넘겼는데 시계열 주기추정
방법론은 이 사용자 전공이 아니다 (OPERATOR C-036, C-034 재발).

### 2026-08-18~19 — 레거시를 걷어내고, 헤드리스가 그림을 낸다

커밋 14 개(`788a8d1` … `193d5ce`). 기록: `docs/audit/HEADLESS_QC_FIGURES.md` ·
`docs/audit/APERTURE_RADIUS_DECISION.md` ·
`docs/audit/SESSION_2026-08-17_CONFIG_AND_HEADLESS.md`.

### 구경 반지름 — 측정이 답했고, 내 이론이 틀렸다

사용자: *"원래 권장되는게 구경배수를 쓰면돼 그리고 어차피 apcorr 있으니 괜찮은거아님?"*
지시대로 `1.0 / 3.0` 으로 올리고 **다섯 성단 137 프레임을 재처리**했다. 그리고
외부 참조등급과 별별로 대조하니 **1.0 이 15 밴드 중 13 에서 나빴다**(중앙 +0.008 등급).
참조등급은 두 실행에서 최대차 `0.00e+00` 으로 동일함을 먼저 확인했다.

M13·M3 에서 섞인 조합을 돌려 범인을 분리했다 — **재는 구경이 전부다.**

      0.8/2.4  M13 0.1118  M3 0.1016      1.0/2.4  M13 0.1220  M3 0.1191
      0.8/3.0  M13 0.1109  M3 0.1007  ←   1.0/3.0  M13 0.1214  M3 0.1198

`large_scale` 2.4→3.0 은 -0.001 로 거의 공짜다(참조 구경은 밝고 **고립된** 별에서만
성장곡선을 뽑으니 넓혀도 오염이 안 는다). 0.001 등급 때문에 다섯 성단을 다시 돌릴
값은 아니라 2.4 로 뒀다 — 다음 재처리 때 올린다.

**문헌 대조도 했다.** M67 실프레임에 고립 인공별 500 개를 주입해(실제 별에서 6 FWHM,
서로 8 FWHM) 오염만 제거하고 SNR 을 훑었다. 최적은 **0.9×FWHM**, 밝기별로는 밝음 0.8 ·
중간 0.7 · 어두움 0.6 — Howell 계열 유도의 예측(어두울수록 작아진다)이 이 기기에서
그대로 재현된다. 숫자가 교과서의 1.0~1.5 가 아닌 이유는 **표본화**다(FWHM 7.91 px 라
1×FWHM 구경이 약 200 px², 하늘 잡음이 면적에 비례해 일찍 물린다).

세 측정이 같은 곳을 가리킨다: 고립 인공별 SNR 0.6~0.8 · 성단 정확도 0.8 승(13/15) ·
성단 정밀도 0.8 승(5/5). **`0.8` 은 우연이 아니라 최적 근처였다.**
산출물은 검증본을 제자리로 되돌렸고 1.0/3.0 재처리본은 `*_AP10` 으로 증거로 남겼다.

### 레거시 제거

- **죽은 설정 72 개 삭제** — `idmatch_*` 19 · `overlay_*` 9 · `cross_frame_ransac_*` 4 …
  도착만 하고 아무도 안 읽던 것들. 선언 462 → 390, **아무도 안 읽는 설정 113 → 0**.
  `config_audit --unread` 로 기계 검사하고 상한 0 으로 못박았다.
- **TOML 을 런타임에서 제거.** 먼저 **이전 안 된 TOML 27 개를 발견**했다 — 그냥
  지웠으면 그 워크스페이스들이 설정을 통째로 잃었다. 전부 이전 후 제거.
  `schema.py` 1762 줄(미사용)·`tomllib` 임포트 7 파일(호출 0 곳)도 같이.
- **스텝마다 쓴 파라미터를 기록한다** — `parameters_used.json` / `.csv`.
  스텝별 열은 선언이 아니라 **실측**(기록용 대리자가 실제로 읽은 이름만 잡는다).

### 헤드리스가 그림을 낸다 — 7 → 19 장

원인은 층이었다: `savefig` 가 analysis 8 곳, **gui 32 곳**. 그림 저장 코드의 80 % 가
창에 묶여 있었다.

| 스텝 | 전 | 후 | | 스텝 | 전 | 후 |
|---|---|---|---|---|---|---|
| 4 검출 | 0 | 3 | | 8 PSF | 3 | 4 |
| 5 WCS | 0 | 1 | | 10 영점 | 4 | 4 |
| 6 참조 | 0 | 1 | | 11 CMD | 0 | 1 |
| 7 측광 | 0 | 4 | | 12 이소크론 | 0 | 2 |

**Step 5 는 창에도 그림이 없었다.** 무엇을 넣을지도 측정으로 골랐다 — 137 프레임의
변동계수를 재서, `match_rate`(0.01)처럼 평평한 것은 제목 숫자로 내리고 자리를 말하는
패널에 줬다.

그 그림이 바로 하나를 판별했다: `resid_vs_radius_slope` 가 음수(-0.42)여서 SIP 차수
부족인지 혼잡인지 갈랐더니 — **구상 -0.21~-0.48 vs 산개 +0.058/+0.072**. 혼잡이다.
SIP 해는 멀쩡하고 구상성단 중심부가 중심 측정을 망친다. 오늘 구경 결론과 같은 방향의
독립 측정이다.

### 스텝 현황

CMD 1-12(`deferred` 는 9 하나 — 대화형) · **LC 1-11, deferred 없음**(전엔 1-7).

**LC 가 헤드리스로 완주한다.** 스텝 10(추세제거)까지 옮겼고, 그 과정에서
**내가 미룬 이유가 틀렸다는 게 드러났다.** 「계산이 위젯에서 읽고 위젯에 쓴다」
중 쓰기만 맞았다 — 창의 `_sync_state_from_controls` 가 스핀박스 전부를 한 곳에서
평범한 속성으로 복사하고 있었고, 계산은 처음부터 속성을 읽었다. 그 통로를
우회하는 직접 읽기는 **네 개**뿐이었다. 나는 위젯 접촉 횟수만 세고 읽기·쓰기를
안 갈랐다.

창은 **4,874 → 1,580 줄**, 70 개 중 59 개가 동일 객체다. Qt 를 코드에서 완전히
걷어냈다 — `QMessageBox` 17 곳이 `self._tell_user(...)` 가 됐다(옮긴 메서드는
정의 모듈의 전역을 보므로, 모듈에 스텁을 두면 창의 대화상자까지 죽는다).

**그림 4 장도 같이 나온다.** `_update_plots`·`_plot_global_diagnostics` 는 Qt 를
한 줄도 안 쓴다 — 헤드리스에서 안 나오던 이유는 손에 잡히는 figure 가
`FigureCanvas` 것뿐이어서였다.

LC 가 7 에서 멈춰 있던 이유는 과학이 아니라 **설정이 「어느 별인지」를 말할 수 없어서**
였다. 이소크론과 같은 관문이고 같은 답 — 설정 행과 거부(스텝 8, 08-19).

**스텝 9 는 그 관문을 지나 실제로 커브를 만든다 (08-19).** 라이트커브 계산
1,149 줄이 `QMainWindow` 자식 클래스 안에 있던 걸
`apex/analysis/light_curve/raw_lightcurve.py` 로 옮기고 **창이 그것을 상속한다** —
스텝 8·10 이 받은 것과 같은 처리다. 창은 5,041 → 3,892 줄.

동일성이 검사 대상이 아니라 구조다: 20 개 메서드 중 17 개가
`Window._build_ensemble_series is RawLightCurveBuilder._build_ensemble_series`
로 참이고, 나머지 셋(`log`·진행표시 훅 둘)은 일부러 다르다.

**실측** (YZ Boo 2 밤 364 프레임, 창이 2026-08-01 에 저장한 커브 대비):
`mag`·`comp_avg`·`diff_mag_raw`·`airmass`·`BJD_TDB`·`color_index` 전부
**0.0e+00**, 파일 10 개 동일. `night_id` 만 2 다른데 **새 코드가 맞다** —
대조본이 밤 추론 수정(`3d52f7b`, 08-03) 이전이다.

**스텝 11 (주기분석)도 같은 날 붙었고, 이건 정반대로 쉬웠다.** 창에 계산이
없었다 — `PeriodAnalysisWorker` 는 배열을 받아 `run_period_analysis()` 를 부르는
순수 전달자고, 파일 고르기·읽기·저장이 전부 이미 서비스였다. 없던 건 스핀박스에
값을 넣어 줄 사람 없이 그것들을 부르는 호출자뿐이라 **스텝 파일 170 줄이 전부**고
창에서는 아무것도 안 뺐다. 설정 5 개(`period_min_days`·`period_max_days`·
`period_samples_per_peak`·`period_methods`·`period_pdm_bins`)를 열었는데 전부
창이 쓰던 기본값이 있어 막지 않고 돈다.

**주기 실측**: 창 저장본 대비 네 방법(`raw_ls`·`corr_ls`·`raw_pdm`·`corr_pdm`)
전부 **0.000e+00**. 문헌 대조로는 `raw_pdm` = 0.104295 일 vs YZ Boo 문헌
0.104092 일 — **0.19 %**. LS 는 0.0945 일로 별칭을 집는데, PDM 이 맞는 값을 준다.

첫 대조는 안 맞았고 **그게 코드 탓이 아니었다** — 저장본은 세 밴드를 합쳐
(`filter="all"`) 분석했는데 내 기본값은 밴드별이다. 조건을 맞추자 전부 0 이 됐다.
밴드별을 기본으로 둔 이유는 **세 밴드가 다른 주기를 주면 그게 진단**이라서다.

**요약 그림(3 단: 라이트커브·주기도표·위상접기)도 같이 나온다.** 창의 483 줄짜리
메서드를 `apex/analysis/light_curve/period_plot.py` 로 옮기고 창이 상속한다 —
17 개 중 11 개가 동일 객체, 6 개는 창이 위젯에서 읽는 것들이다. 창은 2,413 →
1,915 줄. 색은 `_colors()` 지연조회로 바꿔 **레이어 규칙(analysis → gui 금지)을
지키면서 창의 테마 변경 반영은 그대로 유지**했다.

**그림을 실제로 그려 보니 결함 넷이 나왔다** — 셋은 답이 틀리는 종류였다.

1. **별칭 분석을 안 돌려 엉뚱한 주기로 접었다.** 채택 주기가 없으면 LS 최고점으로
   떨어지는데, 두 밤 관측에서 그건 흔히 관측 창의 별칭이다. **0.094468 일
   (문헌 대비 9.2 %)** → 별칭 해소 후 **0.104209 일 (0.11 %)**. 설정
   `lightcurve.period_resolve_aliases` 를 열고 **기본 켜짐**으로 뒀다 — 창은
   체크박스지만 배치엔 체크할 사람이 없고, 꺼져 있으면 조용히 별칭이 답이 된다.
2. **밤 기준선을 지우는 보정 뒤에 보정본을 썼다.** 야간 영점 보정은 밤 사이
   기준선을 없애는데 그게 여러 밤 주기탐색이 쓰는 정보다. 창은
   `correction_preserves_nightly_baseline` 을 함께 보는데 나는 안 봤다.
   **0.105359 일 (1.22 %)** → 조건 반영 후 창과 **0.000e+00**.
3. **체크성을 내가 넘겨서 오히려 잃었다.** 필터가 `"all"` 이면 체크성 조회가
   빈다. 안 넘기면 플로터가 실제로 그린 필터로 스스로 찾는다 — 창이 하는 그대로.
4. **이관이 창의 스핀박스를 끊었다.** 접근자만 만들고 창에 안 덮어서, 창에서
   범위를 바꿔도 그림이 기본값으로 그려지는 상태가 있었다. `is` 대조로 잡았고
   그 테스트가 이 결함 때문에 존재한다.

**두 번째 대상이 정반대 결함을 드러냈다 — AE UMa** (SX Phe, 문헌 P=0.086017 일).
같은 스텝을 돌리니 `corr_ls` 는 **0.01 %** 인데 채택값이 **4.07 %** 였다.
별칭 서비스가 `status: AMBIGUOUS`(*"leave-one-night-out agreement is 50%"*)로
**「못 가렸다」고 말했는데 호출부가 그걸 무시하고 1 순위 후보를 채택**한 것이다.
`RESOLVED` 일 때만 주기도표 최고점을 덮도록 고쳤고, **창도 같은 코드를 상속하므로
같이 고쳐졌다.**

    AE UMa   AMBIGUOUS → 최고점 유지   0.086011 일   0.01 %
    YZ Boo   RESOLVED  → 후보 채택     0.104209 일   0.11 %

**대상 하나로는 이 두 오류가 구분되지 않는다.** YZ Boo 만 봤으면 「별칭 해소는
항상 켜고 항상 믿는다」가 맞는 규칙처럼 보였다.

기록: `Main/FAILURES.md` F-056·F-057·F-058.

기록: `docs/audit/LC_HEADLESS_STEPS_8_TO_11.md`

### 렌더해봐서 잡은 결함 넷

그림을 실제로 띄우지 않으면 전부 조용히 통과했을 것들이다: Step 4 빈 패널 ·
Step 4 잘못된 x 축 · Step 7 payload 프레임명 누락 · Step 12 Figure 폐기.
**하나도 예외를 안 냈다.** (`Main/FAILURES.md` F-045·F-046)

---


**2026-08-16 — 무대조 항목을 대조하기 시작했고, 구경보정을 닫았다.**

사용자 지시: *"무대조가 좀 많네, 천천히 하자, 대조할것들부터 정하면서 가고,
일차원적으로 보지말고, 각 step은 체인이니깐"* → *"대조할때는 알지? 변인통제
확실히 해야한다"* → *"구경보정정도는 끝내놔"*.

**설계**: `docs/audit/APEX_CROSSCHECK_PLAN.md` — A 축 공백을 항목별이 아니라
**오차 모드별**로 재분류했다. 프레임 스칼라는 step10 영점·LC 차등이 흡수하므로
대조 가치가 없고, **위치 의존**과 **선택 효과**만 통과한다. 착수 전 필수
**변인통제 점검표 V1~V9** 도 같은 문서에 있다(이 캠페인에서 실제로 결론을
뒤집은 것들로만 채웠다).

**성적표 정정 둘.** ① 프레임 QC 행이 `APEX_PRIOR_ART.md` 가 2026-08-10 에
철회한 내용("문헌 선례 없음")을 인용하고 있었다 — depth-cost 는 DES `t_eff`
가 직접 선례라 **대조가 가능한데 안 한 것**이다. ② 신규 구성요소에 A 축을
들이대는 건 범주 오류라 판정 기호 **`N`** 을 신설했다(초과검출 게이트).

**구경보정 A 축 ✕→○ 완결.** 팔 넷 — 재현(5e-7) · `sep` 커널(**1e-14**) ·
선택지 민감도 · IRAF `phot`(M13 −14.3 · M67 −2.0 mmag). **IRAF 와의 차이는
전부 하늘 추정기**(mode vs 시그마클립 중앙값)로 설명되고 성단 간 7 배 차까지
재현된다(예측 −16.1/−2.7 vs 실측 −16.2/−2.0).

**찾아 고친 결함** (`29c2aa0`): 성장곡선을 r_ap 가 아니라 **최근접 격자점**
에서 읽어 apcorr 이 0.1 등급 틀렸다. 부호가 **격자 위상을 그대로 따른다**
(상관 r=+0.985 — 대부분 +95~111 mmag, FWHM 5.87 인 한 프레임만 −106).
**발표 수치엔 안 닿았다** — 프레임 상수라 step10 영점이 흡수(영점 +96.7 로
상쇄, 보정등급 변화 중앙 −0.06 mmag)하고 PSF 경로는 apcorr 을 안 탄다.

**설정 도달 감사 — 사용자 지시 *"config아직도 안고쳤어? 하 다른놈들도
문제없는지 체크해 다시"***. 로더를 읽지 말고 실제 워크스페이스를 걸어 재측정한
결과는 `docs/audit/CONFIG_REACHABILITY.md`. 원인은 하나였다 — `parameter_map`
은 키를 `raw` 까지 옮기는데 **`SimpleNamespace(...)` 호출이 인자로 안 적은
이름은 조용히 버려진다.** 그러면 `P` 에 속성이 없어 모든
`getattr(P, 이름, 기본값)` 이 기본값을 집는다.

- **기기 광학 제원이 안 닿았다 — 유일하게 수치가 틀릴 뻔한 것.**
  `telescope_focal_mm` · `camera_pixel_um` 이 유실돼 `InstrumentConfig` 가
  **어느 기기든 3947 mm / 3.76 µm** 로 고정됐다. 다섯 성단은 그 기기라서 우연히
  맞았고, QHY600 은 0.740 대신 **0.197 "/px**(0.27배), Sinistro 는 0.389 대신
  0.197(0.51배)이 될 뻔했다. **저장 산출물은 무사하다** — 헤드리스는
  `InstrumentConfig` 를 안 만들고 `match.pixel_scale_arcsec` 를 파일에서 직접
  읽으며, 교차기기 설정엔 올바른 값이 적혀 있다. GUI 로 열 때만 덮어썼다.
  고친 뒤 네 워크스페이스 모두 설정값과 일치.
- **조리개 배율을 이었다 — 산출물은 안 바뀌게.** 코드 기본값과 설정 파일
  **50 개**를 모두 `0.8` / `2.4` 로 맞췄다(지금까지 모든 실행이 쓴 값).
  파일이 주장하던 1.0 / 3.0 은 예시 파일에서 복사된 값이고, 같은 블록의
  `small_scale_min = 0.8` 이 보여주듯 코드는 제 범위의 **바닥**에 앉아 있었다.
  배선 후 재현 하네스로 M13 6 프레임 대조 — 저장값과 **3.4e-7**.
  CMD/LC 불일치(LC 만 1.0/3.0 선언)도 같이 맞췄다.
  **→ 2026-08-19 에 이 항목은 실측으로 닫혔다. 아래 「구경 반지름」 절 참조.**
- **Step 7 캐시가 구경 변경을 못 알아챘다.** 무효화 서명 36 개 중 9 개가 없는
  이름이라 매번 `None` 을 해시에 넣었다 — 구경·하늘클리핑을 바꿔도 낡은 캐시가
  재사용됐다. 넷을 실제 이름으로 고치고 서명 버전 2→3.
- **남은 것은 못박았다**: CMD 27 개 · LC 47 개(`wcs_qc_*` 전부와
  `psf_profile_error_frac` 포함). `tests/test_config_settings_reach_code.py` 가
  맵의 모든 키를 넣은 설정으로 대조하므로 새 유실은 테스트가 잡는다.

**헤드리스가 Qt 없이 돈다 — 실측으로 확인** (커밋 `2f1165e`·`a5710cd`·`17311c5`·
`4220c4b`, 기록 `docs/audit/HEADLESS_WITHOUT_QT.md`). 사용자: *"gui없이도
스크립트만으로 측광할수있어야 한다"*.

Step 8·10 의 계산이 GUI 모듈 안 `QThread` 서브클래스라 Qt 없는 설치에서 두 스텝이
`NOT_IMPLEMENTED` 로 멈췄다. 두 워커 모두 **`QThread` 메서드를 하나도 안 썼고**
Qt 는 기반 클래스와 신호 12 개뿐이라, 계산부를 `apex/analysis/` 로 내리고 GUI 가
그것을 상속해 스레드·신호만 얹게 했다. **파이프라인에 GUI 의존 0 곳.**

**실측**: `pip install -e .`(gui extra 없이, PyQt5 미설치)로 M13 15 프레임.
- 파이프라인 전체 임포트 → 로드된 PyQt5/apex.gui 모듈 **없음**
- Step 10 완주 46.4 초, 표 **48 개 전부 최대차 0.0e+00**
- Step 8 15/15 완주, 별 22,305 개
- 이관이 본문을 안 건드렸음: HEAD 와 문자 대조 **정의 46 개 전부 동일**

**Step 8 잔차 — 추적 완료, 원인은 scipy.** 첫 실행에서 `mag_psf` 최대차 4.8e-05,
1e-6 등급 초과가 22,305 개 중 5 개(0.02 %)였다. 그 5 개는 전부 이미 불량 표시된
측정(`CROWDING_UNRELIABLE` 넷 · `NONCONVERGENCE` 하나)이었고 flags 는 두 실행에서
같았다.

통제 실험이 **"실행 간 잡음"이라는 첫 판정을 뒤집었다** — Qt 있는 환경 재실행은
저장본과 0.0e+00 으로 일치했다. 즉 파이프라인은 결정론적이고, 차이는 환경에 붙어
있었다. 이관·난수·BLAS 스레딩·워커 수·입력 파일·numpy/astropy/photutils/pandas/sep
을 전부 배제하니 두 환경에서 다른 건 **scipy 1.17.1 vs 1.18.0** 하나였다.

**확정**: scipy 만 1.17.1 로 내리고 Qt 없이 재실행 → **표 48 개 전부 값 0.0e+00**
(측광표 15 개 · 별 22,305 개 · 44 열 포함). 어긋난 건 벽시계 시간 열과 프레임 완주
순서뿐이고, 프레임명으로 정렬하면 과학 수치는 전부 일치한다.

그래서 **실행 매니페스트가 자기 환경을 기록한다**(커밋 `b027939`) — 파이썬·플랫폼과
답을 바꿀 수 있는 패키지 버전. 이게 없어서 이 추적에 통제 실행 한 번과 패키지
93 개 대 49 개 대조가 들었다.

`pyproject.toml` 주석이 `requirements.txt` 를 "the pinned set" 이라 부르고 있었는데
**둘 다 범위 지정이라 사실이 아니다**(`scipy>=1.10,<2`). 주석을 사실로 고치고 왜
범위를 유지하는지를 함께 적었다. 실제로 핀을 걸지는 **사용자 판단**으로 남긴다.

**실행해서 잡은 결함**: 채널로 이름을 바꾼 뒤 따라오지 않은 호출부 넷
(`worker.error.connect`, `worker._log =`, `QCoreApplication`). 정적 검사로는 안
보였고 Qt 없는 첫 실행에서 나왔다. 회귀 추가.

**전체 스위트가 잡은 결함 — 창에서 쓰는 워커가 생성조차 안 됐다** (커밋 `64ae8f1`).
`class Worker(QThread, Runner)` 에서 sip 의 초기화가 협조적 체인을 타고 내려가
러너의 `__init__` 을 인자 없이 부른다 → `TypeError: missing 4 required positional
arguments`. 헤드리스는 멀쩡했고 Qt 없는 실행이 저장본을 0.0e+00 으로 재현했는데도
**창 쪽 생성 경로는 아무도 안 밟고 있었다.** 네 가지 구성을 실제로 만들어 돌려
확인했고, **기반 순서를 뒤집으면**(`(Runner, QThread)`) 이것도, 조용한 쌍둥이
(`QThread.run` 이 계산을 가려 `start()` 가 아무것도 안 함)도 같이 사라진다.

> **교훈**: 표적 테스트는 내가 바꾼 경로만 밟는다. 이관은 **밟지 않은 쪽**을 깨뜨린다.
> 머신 여유가 없다고 전체 스위트를 미룬 대가가 이거였다.

**구조 수리 — 설정 정의를 한 군데로** (커밋 `ae0dc13`). 사용자: *"저걸 하나하나
꼭 p로 넣어야돼? 다른 좋은 최적화된 방법없어?"* → *"알아서 하쇼"*. 어제 고친
넷은 증상이고, 원인은 같은 설정이 **네 군데**에 적혀 있던 것이다 — 키 맵(정본) ·
생성자 452 줄(손으로 동기화) · **즉시 덮어써져 버려지던 사본**(CMD 295·LC 264 행,
정본보다 각각 81·123 행 부족한 화석) · **아무도 안 쓰는 `schema.py` 1762 줄**.

이제 맵의 한 행이 설정의 전부를 담는다 — `(경로, 이름, 타입, 기본값)`.
`build_settings()` 가 그 행을 그대로 값으로 바꾸므로 **맵에만 넣고 생성자에
빠뜨리는 일이 불가능**하다. 337 행 전환 · **순 1,006 줄 감소**(CMD 생성자
354 → 73 개 인자).

**증명**: 실제 워크스페이스 51 개 × 두 모드 **속성 35,751 개 전량 비교, 차이 0**
(다섯 시점에서 확인). 설정을 만들거나 읽는 테스트 전 파일 141 개 + 파이프라인·
캐시 계열 174 개 통과. `coerce_setting` 이 옛 헬퍼의 문자열 수용·불리언 처리를
그대로 따르는지도 회귀로 못박았다.

**남은 것**: `schema.py` 1762 줄은 여전히 미사용 — 지울지 되살릴지 판단 필요.

**검증** (커밋 `0758089`): 변경이 닿는 21 파일 **170 개 통과(32.7 초)** ·
`reproduce_apcorr.py` 로 M13 6 프레임 3.4e-7 · M67 5 프레임 5.4e-7 (배선 후에도
저장 산출물과 동일) · `InstrumentConfig` 픽셀스케일이 네 워크스페이스 모두
설정값과 일치 · `compileall` 통과.

**전체 스위트 완주 (2026-08-17)** — 1,191 개 중 1 실패, 그 하나가 위의 GUI 워커
생성 결함이었다. 수정 후 재실행 전량 통과. 소요 10 분 46 초.
(전날 밤에는 여유 1.6 GB 로 10 분에 56 개 속도라 미뤘던 것이다.)

```
.venv-deploy/Scripts/python.exe -X utf8 -m pytest tests/ -q
```

**Step 12(이소크론)를 헤드리스로 올리고, 그 과정에서 탐색상자 결함을 찾았다**
(커밋 `41f7f4f`·`64ae8f1`). 적합 서비스는 애초에 Qt 를 안 썼고, 막고 있던 건
**답을 정하는 설정을 설정 파일에 쓸 수 없었다**는 것이다. 이제 config 행이 되었고,
결정적 설정이 없으면 스텝이 **실행을 거부**한다.

파리티를 확인하다 나온 결함: **창이 탐색 상자를 코드에 박아두고 있었다**
(`mh_bounds=(-1.0, 0.5)`). M13 은 [M/H] ≈ −1.5 라 이 상자에 안 들어간다. 게다가
서비스는 사전값 주위로 상자를 **좁히기만** 하므로, 금속 결핍 사전값을 주면
`[-1.000, -1.360]` 처럼 **뒤집힌다** → 격자 마스크가 아무것도 안 고른다. 증상은
"데이터가 나쁜가"로 읽힌다.

고침 셋: ① 번역을 `apex/analysis/cmd/isochrone_config.py` 한 곳으로(창은 자기
위젯이 소유한 것만 `replace`), ② 기본 상자를 사실에 맞게([M/H] 하한 −2.2,
E(colour) 상한 1.0), ③ `check_bounds()` 로 **MCMC 전에** 뒤집힘을 잡는다.

**남은 결정 — 구경 반지름 0.8/2.4 유지 vs 1.0/3.0 상향.** 올리면 다섯 성단
Step 7→8→10 재처리가 필요하고 오늘 밤 구경보정 대조 수치가 전부 다시 움직인다.
지금은 산출물을 지키는 쪽으로 고정해 뒀다.

**A 축이 빈 것은 이제 셋**: 강제측광 오케스트레이션 · 초과검출 게이트(`N`) ·
다중밤 앙상블. depth-cost 는 선례가 있고 미대조다.

**다섯 성단 판본 통일 완료** — 전부 수정된 apcorr + PSF 산출물이다.
영점 산포 M13 59.0 · M67 33.0 · NGC6811 46.5 · M3 61.0 · M5 49.6 mmag.
수정 전 사본은 각 워크스페이스의 `step7_forced_phot_BEFORE_APCORRFIX` ·
`cmd_zeropoint_BEFORE_APCORRFIX`.

**열린 질문에도 답했다 — 다만 반쪽이다.** 45 프레임·주입 18,000 으로
`mode` vs 시그마클립 중앙값을 진리값과 견줬다. **고립별에서 `mode` 가
확실히 나쁘다**(15σ/27σ). 그러나 **APEX 가 항상 옳지는 않고**(4~8 FWHM 에서
순위 역전) **고립별에서는 클리핑을 끈 무보정 중앙값이 APEX 보다 낫다**
(3.6σ/6.5σ). 초판(프레임 하나·주입 400)은 "APEX 가 옳다" 로 읽혔고 표본을
30 배로 늘려 정정했다 — **한 프레임으로 추정기를 판정하지 말 것.**

**곁가지로 설정 배선 결함 셋** (`886edc7`). Step 7 이 `phot_sigma_clip` ·
`phot_max_iter` 를 읽는데 그 이름을 세우는 곳이 없어 하드코딩 3.0/5 로
고정돼 있었다 — 같은 config 키가 **Step 4 에는 닿고 Step 7 에는 안 닿았다**.
그룹 조절값·모드 프리셋에 이어 **세 번째 같은 모양**이라 일반 가드를 만들었고,
그 가드가 둘을 더 찾았다(아래 결정 항목 · CMD/LC 형상컷 비대칭).

---

**2026-08-15 — 논문을 미루고 개발 완성으로 순서를 바꾼다 (사용자 결정).**

사용자 원문: *"논문을 좀 미루고, 먼저 다 만들고 리뷰하고 최종 검토하고
논문작업을 해야할듯? 즉, 개발에 더 집중하라는 뜻"*.

근거는 이번 감사가 보여줬다 — PSF A 축 결론이 하루 사이 **세 번** 뒤집혔다
(픽셀 규약 결함 → 게이트 미정합 → 프레임 영점). 지금 원고에 넣으면 그때마다
다시 쓴다. 순서를 **개발 완성 → 리뷰 → 최종 검토 → 논문**으로 둔다.

**2026-08-15 저녁 — 사용자 지시 「일단 psf하던거 마저 하고 이소크론 마치고
ㄱㄱ」 완료. 둘 다 닫혔다.**

| 항목 | 결론 | 커밋 |
|---|---|---|
| `profile_error_frac` 기본값 | **0 유지** — 기기마다 부호가 뒤집힌다 | `43b3ba9` |
| 씨앗 수정 성단 반영 (D-009) | **닫음** — 5성단 137프레임, 논문 수치 불변 | `8f5ad07`·`57a2a00` |
| M13 dm−E 축퇴 (후속 ③) | **영점 척도로 규명** — 색 추가로는 못 고침 | `7c05a08`·`ec7a3be` |
| 이소크론 수렴 판정 | 고장나 있던 기간 발견, 수치는 안정 | `c7870c4`·`887c984` |

곁가지로 잡은 결함 넷: step10 **단일 프레임 별의 등급오차가 0.000**
(`91ce0b3`, 다섯 성단 재실행으로 등급 불변 확인) · step12 콤보가 "B-V"를
**"B-\"로 그림**(`975e78b`) · CMD 범례가 거성가지를 덮음(`87a2d00`) ·
저장값 없을 때 밝기축이 관례와 다름(`9d804ab`). 경계 railing 경고 신설.
테스트 1,095 → **1,121 passed**.

**다음에 결정이 필요한 것**: M13 (m−M)₀·E(B−V) 를 고치려면 **Stetson 표준장
(CADC) 취득**이 필요하다 — VizieR 키워드 경로엔 M13 표준 측광이 없음을
후보 4개를 직접 열어 확인했다. 외부 자료 취득이라 사용자 승인 사항.
안 하면 「규명됨, 이 자료로는 보정 불가」로 닫고 [M/H]·나이만 인용한다.

**0-a 완료: GUI 워크스페이스 선택** (2026-08-06 잔여, 최우선이던 항목)
File 메뉴에 `Open Workspace…`(Ctrl+O) · `New Workspace…` · `Open Recent`.
`~/.apex/recent.json` 영속화, 사라진 항목 자동 제외, 현재 것은 비활성.
창 제목이 대상과 폴더를 보여준다.

이 과정에서 **공유 상태 결함**을 하나 잡았다: `project_state` 가
`apex/.state/<mode>/` 에 있어 워크스페이스와 무관하게 공유됐다. 대상 전환이
재시작을 뜻하던 동안에는 안 보였지만, 메뉴로 바꾸자 **손도 안 댄 성단이
"9/12 완료"** 로 뜬다. 상태를 워크스페이스의 `result_dir` 로 옮겼다(그동안
미러를 쓰던 바로 그 자리라 기존 워크스페이스는 진행도가 그대로 살아난다).
전환 시 열린 스텝·도구 창은 전부 닫는다 — 낡은 `params` 를 쥔 창이 이전
대상의 경로에 계속 쓰는 것을 막는다. 창 탐색은 속성 이름 규칙이라 새 도구가
추가돼도 빠지지 않는다.

**2026-08-14 밤 — step8 씨앗 병합 결함 발견·수정. 그리고 IRAF 대조는 픽셀
규약부터 틀려 있었다.**

두 가지가 확정됐다. 상세·재현은 `validation/psf_engines/README.md` 와
`docs/audit/APEX_ENGINE_SCORECARD.md` 의 2026-08-14 절.

1. **IRAF 픽셀 규약 (첫 픽셀 1 vs 0) 미변환** — A 축 인공별 대조가 1.414 px
   어긋난 채 채점돼 "APEX 가 더 건진다(0.76 vs 0.65)" 는 결론이 결함의
   산물이었다. 재산출 결과 **ALLSTAR 가 완전도(0.90 vs 0.70)·정밀도
   (같은 완전도 전 구간) 모두 앞선다.** 논문 Fig5 쪽은 행 순서 매칭이라
   오매칭은 없고 수치는 보수적(고치면 더 좋아짐) — 재실행 대상.
2. **씨앗 병합이 겹친 쌍의 검출별을 지움** — forced 별이 8.7 px 안의 검출을
   차지해 좌표를 덮어쓰면, 검출된 이웃(후보 목록에 없음)의 씨앗이 소멸.
   이웃 빛을 흡수해 과대 측광(−106 mmag), 플래그 없음. `context_xy` 로 수정,
   값이 바뀐 29 개의 |오차| 중앙 296.9 → 80.5 mmag. **남은 +29.6 mmag 는
   미해결**(쌍 해의 잔여 계통, 수정 후 +32.4 mmag 어두운 쪽).

**생산 영향**: 이 결함은 실측에도 있었다 — M13 영점·CMD 등 step8 기반
산출물은 수정 전 코드 결과다. 혼잡 성단 재처리 여부는 사용자 판단 필요에.

**2026-08-12 — Phase 3 자료가 PSF 를 안 쓰고 있었다. 그리고 step8 은 어떤 외부
엔진과도 맞대 본 적이 없었다.**

이 세션의 목적은 **논문화를 위한 native 엔진 감사**이고, 판단 기준은 사용자
지시대로 **"이미 존재하는 엔진들에 비해 얼마나 좋고, 목적성에 맞고 범용적인가"**
다. 완료 여부가 아니다.

**1. 배치가 step8 을 건너뛰고 있었다.** `scripts/reprocess_batch.py` 가 1–7 다음에
step10 을 바로 부른다. 그 함수 docstring 에 이유가 적혀 있다 — *"Step 8/9 not
needed on the forced-aperture path"*. **열린 성단에서는 맞는 판단이었고 혼잡한
구상성단에서 재검토된 적이 없었다.** 배선은 이미 완성돼 있었다:
`resolve_lightcurve_photometry_source()` 가 step8 서명을 검사해 완전하면
`mag_psf` 로 자동 전환한다. step8 을 돌리자 step10 이 말없이 갈아탔다.

M13 실측(같은 프레임·검출·WCS·마스터·step10, step8 여부만 다름):

| 밴드 | 영점 산포 | 보정성 N | 혼잡/한산 잔차비 |
|---|---|---|---|
| B | 40.8 → 35.8 mmag | 433 → 513 | 1.46 → 1.12 |
| V | 37.8 → 31.9 mmag | 575 → 684 | 3.30 → 2.05 |
| R | 38.5 → 36.2 mmag | 593 → 722 | 2.45 → 1.84 |

적색거성열 폭 **76 → 58 mmag**, CMD 에 수평가지 출현. 한산한 사분위는 오히려
아주 약간 나빠진다(V 16.4 → 19.9) — 이웃 없는 밝은 별에서는 조리개가 모형
불일치를 안 겪으므로 예상된 거동이다. 그림·스크립트:
`validation/fig_psf_vs_aperture_m13.py` · `validation/crowding_aperture_vs_psf.py`.

**네 성단을 다 돌리자 결론이 바뀐다** (`validation/PSF_VS_APERTURE_ALL_TARGETS.md`).
혼잡 진단은 확정 — 혼잡/한산 잔차비가 열린 성단(NGC 6811 · M67) 1.0~1.3,
구상성단(M3 · M13) 2.1~3.5 이고 PSF 가 구상성단에서만 절반 가까이 줄인다.
**그러나 영점 산포가 좋아진 건 M13 뿐이다** (NGC 6811 V 24 → 51,
M67 r 19 → 45 mmag). 원인의 절반: **프레임 내부 산포가 12/12 조합에서 PSF 가
크다.** PSF 는 프레임 안에서 잡음이 크고 대신 혼잡 상관 오차를 없애므로,
혼잡이 지배하면 순이득·아니면 순손실. **잡음이 큰 이유는 미해결로 기록.**
논문 문장은 "혼잡에서는 PSF, 아니면 조리개 — 그래서 둘 다 제공한다".

**사용자 결정: 조리개와 PSF 를 둘 다 논문에 넣는다** (2026-08-12, *"구경측광
psf측광 다 넣어야지"*). 한쪽으로 갈아타는 것이 아니다. `scripts/phase3_psf_batch.py`
가 전 성단에 step8 을 돌리되 조리개 영점을 `cmd_zeropoint_APERTURE` 로 보존한다.
**진행: NGC6811 · M67 완료(둘 다 step10 이 PSF 채택), M3 · M5 남음.**

**2. step8 의 A 축(기존 엔진 대조)이 ✕ 였던 이유가 좁았다 — 그리고 밤에 채웠다.**
APEX 에는 2,591 줄짜리 IRAF 하네스가 있는데 `phot`(조리개)만 부른다. daophot
패키지를 올려놓고 `psf`·`allstar` 는 한 번도 안 썼다. **Step 7 은 IRAF 와
맞대 봤고 Step 8 은 없었다.** `validation/psf_engines/` 가 그 구멍을 메운다.

**본실험: 인공별 400 개(2 회 × 200), 혼잡 4 구간 × SNR 5 단계, 적합창 1.7·FWHM.**

| | APEX step8 | IRAF ALLSTAR |
|---|---|---|
| 유지 (400 중) | **304 (0.76)** | 260 (0.65) |
| target SNR 10 유지 | **0.81** | 0.45 |
| 같은 별, 0.75–1.5 FWHM 산포 | 0.087 | **0.029** |
| 같은 별, 6–inf FWHM 산포 | 0.036 | **0.023** |

**A 축 판정: 우월성이 아니라 성격 차이.** APEX 는 더 건지고(특히 어두운 별)
DAOPHOT 은 건진 것을 더 정확히 잰다(1.6~3.0 배). 성적표 A 축 ✕→○.

**전체를 한 줄로 보면 동등해 보인다(0.050 vs 0.048) — 구간이 섞여 상쇄된
값이다.** 그래서 `matched` 를 혼잡 구간으로 쪼개는 행을 넣었다. 통제 없이
낸 첫 수치는 3.2 배였고 통제 후 구간별로 1.6~3.0 배다.

**이것이 위 1 번의 미해결 질문에 답한다** — 같은 픽셀에서 DAOPHOT 이 조밀하므로
APEX PSF 의 프레임 내부 잡음은 **자료가 아니라 엔진** 문제다.

SNR 5 의 −0.232 mag 는 **결함이 아니라 `snr_psf ≥ 3` 컷의 선택 효과**다
(게이트 전 +0.024 로 무편향, 잘린 것들 +0.221). 완전도 그림은 회수 정의가
Step 4 검출 기준이라 무사함을 확인했다.

**3. 재현성 결함 — 검증 스크립트 16 개가 못 돌고 있었다.** `validation/` 이
E 정션이라 `Path(__file__).resolve()` 가 레포 밖으로 나간다(TRACK 「함정」에
이미 있던 항목). **논문 그림 fig14·15·16 포함.** 전부 `absolute()` 로 고치고
회귀 테스트를 붙였다(`tests/test_validation_script_paths.py`). **무인 실행
전 작은 값으로 스모크를 돌려서 잡았다 — 안 했으면 밤새 실패했다.**

**4. 부수 발견 — step10 표준화 발산 버그 (수정 `1134260`).** `solve_standard_colors`
의 수렴 논증이 선형 모형의 것인데 2차항이 붙으면서 축소율이 `|ct + 2·ct2·C|`,
즉 **계수가 아니라 별의 색에 달린 값**이 되었다. 계수만 묶는 기존 방어로는 못
막는다. M13 에서 `mag_std_B = -844` 로 터졌다. 같은 결함이 구경 경로에도 있었고
그쪽은 최소 색이 우연히 수렴 영역 안이었을 뿐이다. 적합 색 범위 클램프 +
미수렴 시 NaN 으로 고쳤다. 회귀 2 개 추가, `test_step10_color_solve.py` 9 passed.

**정정** — 2026-08-11 인라인 혼잡 상관 `r=-0.459`, `4.62 배`는 스크립트를 안 남겨
재현되지 않는다. 정본은 `crowding_aperture_vs_psf.py` 이고 밴드별로 낸다(위 표).

---

**2026-08-11 야간 — Phase 3 clean run 완료. 논문 수치의 출처가 정해졌다.**

채택된 세 최적화(O1 스트리밍 마스터 결합 · O2 단계별 worker 상한 · 강제측광
프로세스 풀)를 전부 켜고 raw 부터 다시 돌렸다. 산출: `E:\APEX_validation\phase3\`
(기존 `reprocess\` 는 baseline 으로 보존). 상세: `benchmark/perf/20260811/PHASE3_RESULTS.md`.

| 대상 | 프레임 | Step 0 픽셀 | Step 7 측광 | CMD 표 이동/MAD |
|---|---:|---|---|---|
| NGC 6811 | 21 | 21/21 동일 | 42,000 · 변경 0.00 % | 0.00 / 0.00 mmag |
| M67 | 30 | 30/30 동일 | 30,180 · 변경 3.18 % | 0.70 / 1.47 mmag |
| M13 | 15 | 15/15 동일 | 22,515 · 변경 0.00 % | 0.00 / 0.00 mmag |
| M3 | 30 | 30/30 동일 | 35,280 · 변경 0.00 % | 0.00 / 0.00 mmag |
| M5 | 41 | **상속** (raw 없음) | 75,194 · 변경 0.00 % | 0.00 / 0.00 mmag |

**측정 205,169 건 중 움직인 것은 M67 의 960 건뿐**이고 그것도 B4 잡음 바닥 안이다.
**최적화는 과학을 바꾸지 않는다** — phase3 트리를 논문 수치의 출처로 쓸 수 있다.
오라클 `pytest tests -q` → **996 passed**.

**M5 는 Step 0 을 재실행할 수 없다 — raw light 이 이 기계에 없다.** 3 중 확인했다
(`E:\observe_DSY\M5\M5_20250308\light5\` 확장자 무관 0 개 · 그 밤 전체 24 개가
dark 10 + flat 14 · `E:\observed_Analysis\M5\light` 도 빈 폴더). 보존된 보정본에서
Step 1 부터 이어 붙였다(`scripts/phase3_m5_resume.py`). Step 0 이 비트동일임이
증명돼 있어 과학적으로 잃는 것은 없으나 **"이번 실행에서 재생성했다"는 주장은
M5 에 대해 성립하지 않는다.**

**LCO NGC 6811 U 밴드는 존재하지 않는다 — 성적표의 최우선 계획이 틀린 전제였다.**
`lco_ngc6811_nights.txt` 의 U 96 / B 66 / V 94 는 LCO API 의 추정값
(`count_estimated`)이었고 전수 페이징하면 U 는 0 장이다(B 4 · V 4). 대신
**2023-09-07 fa05(LCO 1m Sinistro) 103 장 gp/rp/ip/zs** 가 있고, APEX 가 이미
자기 카메라로 처리한 같은 성단이라 **같은 별을 두 기기로 측광한 직접 비교**가
되므로 C 축 자료로는 원래 계획보다 낫다. 8 GB · 1 시간 이내.
상세·취득 처방: `validation/psf_crossinstrument/LCO_NGC6811_REALITY.md`.
정정 반영: 성적표 · `validation/paper/AUDIT_INTAKE_20260810.md` · 원본 txt 배너.

**U 밴드 논거는 되살아난다 — 다른 성단에 SDSS u′ 가 있다.** 16 개 성단을
조회한 결과 NGC 7789(u′ 12 장, kb88 단일 기기 2 밤) · M5(u′ 18 장, 기기 분산,
**APEX 가 이미 처리한 성단**) · M45(u′ 18 장, kb55 에 16 장)가 후보다.
Johnson U 는 사실상 없고 u′ 가 그 역할을 한다(u′−g′ ≈ U−B). **수치는 하한**
이며 질의 실패로 M67·NGC 6811·M13·M3 는 측정되지 못했다.
상세·먼저 확인할 것 셋: `validation/psf_crossinstrument/LCO_U_SEARCH_RESULT.md`.

**규명 완료 — M67 영점 보정성 수 흔들림은 Gaia 서버가 바뀐 탓이었다.**
clean run 에서 ESA TAP 이 타임아웃해 VizieR 로 폴백했고(`gaia_fov_meta.json`
`"esa"` vs `"vizier_fallback"`), 둘은 같은 Gaia DR3 이고, **APEX 의 VizieR
질의는 `RUWE` 를 SELECT 하는데 ESA 질의는 하지 않는다**(ESA 테이블에 열이 없는
게 아니라 질의가 빠뜨린 것). `gaia_quality_mask` 가 결측 열에 관대하므로 baseline 은
RUWE ≤ 1.4 컷을 조용히 건너뛰고 clean run 은 적용했다. 실측 ESA 1900/1900 통과
vs VizieR 1749/1900(151 제외), 보정성 910 중 RUWE>1.4 가 84 개(9.2 %)로 관측된
밴드당 59~61 개 감소와 일치한다. 적합의 결함이 아니다(산포 24.25→24.12 mmag).

**고쳤다**: `gaia_quality_report()` 신설(어느 컷이 적용됐는지·왜 안 됐는지·몇 개를
걸렀는지 반환), step10 이 미적용 컷을 경고 로그 + `gaia_quality_report.json` 으로
남긴다. 회귀 테스트 7 건, 전체 **1,003 passed**.

**남은 사용자 판단**: 논문 수치가 서버 응답에 좌우되면 안 된다. ESA 질의가
`ruwe` 를 함께 가져오게 해 두 경로를 일치시키거나, RUWE 컷을 명시적 설정으로
올려 실행마다 고정할 것.


**2026-08-05 편의성 트랙 — 디자인 시스템 이관 + 창 크기 결함 (`d142472`
`f19d89f` `c2141b4` `9aeb18c`)**

- **전 창 최소 크기 스캔 (재실행 금지 — 아래 표가 결과)**. 프로브:
  `scratchpad/win_probe.py` / 트리는 `win_tree.py`. 노트북 1366×768 의
  실사용 높이는 약 728px. 손대기 **전** 값:

  | 창 | 최소 W×H | 판정 |
  |---|---|---|
  | step4 소스검출 | 1504×1199 → **1147×760** | 1080p 도 초과했음, 해소 |
  | LC step9 라이트커브 | 1451×912 → **1016×912** | 가로 해소, 세로 미해결 |
  | CMD step12 이소크론 | 1090×1558 → **1047×819** | 해소 |
  | CMD step8 PSF | 975×861 | **미해결** (세로 +133) |
  | step7 강제측광 | 791×857 | **미해결** (세로 +129) |
  | CMD step9 마스터ID | 1039×752 | 경미 (+24) |
  | step2 크롭 | 835×748 | 경미 (+20) |
  | step3 하늘 1262×644 · step6 1088×724 · LC step8 1033×661 | | 통과 |
  | LC step10 970×580 · LC step11 1114×544 · CMD step10 712×482 | | 통과 |
  | CMD step11 525×472 · step5 670×598 | | 통과 |

- **원인은 대부분 "플롯이 아니라 옆의 컨트롤 열"**이다. step4 는 미리보기가
  777×364 로 잘 줄어드는데 오른쪽 결과 열이 630×803 을 요구했고, LC step9 는
  고정 540px 컨트롤 열 + 841px 플롯 splitter 였다. 처방은 전부
  `scroll_wrap` — 플롯 패널은 건드리지 않는다.
- **`scroll_wrap(preferred_width=…)` 추가** (`apex/gui/layout_rules.py`).
  QScrollArea 의 `sizeHint` 는 449px 로 상한이라, 고정폭 열을 그냥 감싸면
  넓은 화면에서도 가로 스크롤바가 붙는 회귀가 난다. 선호 너비를 주면
  여유가 있을 때 원래 너비를 지키고 좁아지면 스크롤로 물러선다.
- **하드코딩 색 이관**: LC step9/step10, CMD step12, step4 완료 —
  네 파일 모두 하드코딩 UI 색 0개. 다크 테마에서 안 읽히던 값들이 드러남
  (step4 "Total sources: 47373 / Median FWHM: 2.16″", step10 분석 패널 전체).
- 오라클 778 passed. 비포·애프터 캡처는 세션 scratchpad
  (`s4_shots` `s10_shots` `s12_shots` `s9_shots`).

**2026-08-04 이소크론 [M/H] rail — 해소 실증 세션 (`ec1701c`+`88263bc`)**

- **U 영점을 외부 표준(MMJ93, VizieR J/AJ/106/181)으로 재앵커하니 rail 이
  풀렸다.** m67_ubv(SBIG U·B·V) 오프셋 실측: dU −0.131 / dB +0.051(Sandquist
  2004 로 교차확인 +0.030) / dV +0.008. 같은 자료·같은 설정 재적합:
  **[M/H] −0.83 → +0.06, 나이 3.83 Gyr, E(B−V) 0.011, (m−M)₀ 9.67 — 전부
  문헌 복귀.** 소거실험: U 만 재앵커하면 rail 잔존(−0.55) — **전 밴드 영점이
  한 표준계에 앉아야 U−B 가 축퇴를 푼다.** B−V 단독은 영점이 좋아도 넓고 기움.
- **PSF 측광으로도 동일 재현** (Step8 8/8 → Step10 → 재앵커 → 적합):
  오프셋 dU −0.132, 적합 +0.06/3.86/0.005/9.65. 측광 방식 무관.
- **다성단 PSF 적합** (reprocess BVR): M13 B−V+dm prior → **[M/H] −1.56·나이
  12.76** (문헌 −1.53/12.5; dm 사후 13.97 이 prior 를 2.9σ 밀어냄 = 단색
  dm−E 축퇴 잔존, E 0.09 vs 문헌 0.02 — 미해결로 기록). NGC6811 은 E prior
  필수(없으면 나이 하한 rail·E 0.33; 있으면 0.83/+0.06/0.081 — 기존 검증 재현).
  M67 재처리본(gri)은 Step8 30프레임 완주 후 g−r 적합 실행.
- **코어 버그픽스(`88263bc`)**: Step10 U 색항 허용치 3.0 예외가
  `solve_standard_colors` 고정점 반복(수렴조건 |ct|<1)을 발산시켜 PSF 런에서
  `mag_std_U` 가 ±1000 등급 폭주. 클램프를 수렴 한도 0.8 로 통일. 구경 런은
  |ct|>3 클램프 폴백으로 우연히 살았던 것.
- 신규: `run_step12_headless --dm-prior`(구상성단용)·`--mh-bounds`,
  `recalibrate_u_mmj93.py`(--source-result/--suffix). 상세:
  `validation/psf_crossinstrument/REPORT_UB_DEGENERACY.md` 후속절.
- **Step 12 피팅 단일화 + prior UX (2026-08-04 저녁, 사용자 결정)** —
  「필요없는 피팅 다 없애고 MCMC 하나만」: Color-Color 탭·Quick Fit(grid)
  탭·Grid Heatmap·CMD거리 멤버십 내보내기 제거(-1,250줄, 3,605→2,353).
  남은 것 = CMD Viewer(수동 슬라이더, 시각화) + Auto-fit(MCMC).
  그리드 스캔은 analysis 계층에 MCMC 초기화용으로 존속(UI만 제거).
  prior UX: MCMC 설정 전체(prior on/μ/σ·색 선택·age범위·워커수)를
  ProjectState 에 영속화(재시작 유지, 실행 시점 자동저장), prior
  체크박스가 μ/σ 활성화를 연동, 분광([Fe/H])·먼지지도(E) 문헌 출처
  툴팁. **NGC 6811 류의 [M/H]는 분광 prior 가 공식 경로**(사용자 결정 —
  U 표준장 전이 사슬은 복잡해서 폐기, 아카이브 복권만 남김).
  검증: 컴파일+임포트+이소크론 테스트 20 passed. **실 GUI 구동 확인은
  미완**(offscreen 전체 창 생성이 침묵 크래시 — 기존 백로그 「GUI 실구동
  사용감」과 함께 다음 GUI 세션에서).
- **설정 체계 대수술 — TOML 완전 제거, JSON 정본 (2026-08-06, 사용자 결정)**
  — 배경: 루트 parameters.toml 하나를 대상마다 덮어쓰다 io=M13 +
  target=NGC6811 혼합 사고(제목/plot 불일치), gri 워크스페이스에 johnson
  격자, last_param.txt 는 서술만 있고 미구현. 구조 수리:
  ① `apex/config/config_io.py` 신설 — JSON 정본, 레거시 TOML 은 첫 로드에
  1회 자동 마이그레이션(`parameters_X.toml→apex_config_X.json` 충돌 없는
  매핑), 이후 TOML 이 더 새로워도 **무시+경고**(이중 진실 차단), 원자적
  쓰기, `_meta.migrated_from` 기록. ② 초크포인트 전환: parameters_cmd/lc
  (reader+save_toml+flat save), param_file.update_param_file, Step1 공통/LC
  target 저장, qa_report, benchmark runner/cmd_batch, schema.from_toml/
  to_toml — **tomli_w 의존 전부 제거**(pyproject.toml 검사·IRAF 도구 내부
  설정만 정당한 TOML 로 잔존). ③ reprocess_batch.gen_config 를 정규식
  TOML 수술 → 구조적 JSON 생성으로 바꾸고 **target.name 을 io 와 함께
  기록**(혼합 사고 원천 차단). ④ `check_workspace_identity()` — target.name
  이 io 경로에 안 보이면 경고(오늘 사고 재현 케이스로 검증, 공백/대소문자
  내성). ⑤ 진입점 --params 는 json/toml/dir 모두 수용, main.py 첫 실행은
  example→apex_config.json 생성. .gitignore 에 apex_config*.json.
  검증: config_io 단위테스트 7 + 갱신된 파라미터 스위트 42 passed, 실 E:
  워크스페이스 마이그레이션 스모크(표준/변형 이름·왕복·JSON 우선) 전부
  PASS, 헤드리스 step10 이 TOML 경로 입력으로 동일 결과(dU -0.1312).
  **잔여(2단계)**: GUI File>Open/New/Recent 워크스페이스 + 신원 경고
  다이얼로그 표면화, 매뉴얼 갱신, E: 레거시 TOML 은 lazy 마이그레이션에
  맡김(일괄 변환 불필요).
- **Step 12 재구조 — 탭 제거, Fit 별도 창 (2026-08-05, 사용자 결정 A/B/C
  중 '별도 창')** — 본문 = CMD Viewer 단독, Auto-fit(MCMC) UI 전체는
  헤더 `Fit` 버튼으로 여닫는 독립 창(`self.fit_window`, scroll_wrap +
  clamp_to_screen). **적합 완료 시 최적해 중앙값이 뷰어 슬라이더에 자동
  반영**(`IsochroneViewerWindow.apply_values` 신설 — setValue 는 시그널을
  막으므로 update 훅을 저장해 직접 재그림). prior 영속화·오염 가드·배너는
  위젯 이름 유지로 그대로. 오프스크린 하네스 전 항목 PASS(토글·배너·
  영속화·가드·교대복원) + 타깃 테스트 21. 매뉴얼 03-cmd Step12 절 갱신.
  잔여: 매뉴얼 스크린샷 실화면 재촬영, 실화면 사용감 확인.
- **GUI 실구동 검증 — 오프스크린 하네스로 완료 (2026-08-05 새벽)** —
  화면 제어 대신 capture_ui 레시피(offscreen+FONTDIR+QMessageBox 패치)로
  실검증. 통과: Step12 탭순서[MCMC, Viewer]·기본탭 MCMC·색 자동감지·
  u/U 부재 경고 배너+prior 강조(M5 gri 실렌더)·Step10 앵커 다이얼로그
  (한국어 설명·탐색 버튼 렌더). **실버그 2건 적발·수정**:
  ① prior 영속화가 영원히 미적용(41bca98) — restore_state 가
  setup_step_ui **다음**에 실행되는데 스태시를 빌드 종점에서 소비하는
  설계였음(라인 번호로 실행 순서를 추정한 오판이 원인 — 조사 엄밀성
  메모리에 등재). ② **워크스페이스 간 prior 오염**(daa10bf) —
  프라이머리 상태가 apex/.state/<mode> 에 모드당 한 파일이라 성단 A 의
  [M/H]/E prior 가 성단 B 에 복원되는 과학 오염 경로. workspace 태그
  가드 + 자기 미러(result_dir/project_state.json) 폴백 복원으로 차단,
  교대 시나리오까지 하네스 PASS. **함정: 다른 step_data(iso 경로·콤보
  등)는 여전히 프라이머리 공유** — 전면 미러 우선 로드는 설계결정으로
  보류. 매뉴얼 스크린샷(cmd_step12_auto_fit.png) 재촬영은 잔여.
- **통합 코드리뷰 + 의심 5종 조사 (2026-08-04 밤)** — 리뷰 발견: 앵커-syn
  순서(수정 `6e3c754`), 탐색 컷라인 위음성(Sprint 2), 매뉴얼 03-cmd 구탭
  잔존, step12 죽은 코드(-84줄 정리). 조사 판정: ① Gaia 부가값 로딩 3개
  실데이터 무오류. **pmem "흔적기관" 판정은 오판이었다(정정)** — 생산자는
  Step 9 master ID editor 의 3D GMM(`_fit_two_component_gmm`→`p_cluster`,
  step9_master_id_editor.py:709-860)에 살아 있고, 오늘 wide 3개에 없었던
  이유는 **헤드리스 체인이 Step 9 를 건너뛰어서**(설계)다. GUI 로 Step 9
  를 돌리면 생산됨(사용자가 "원래 썼다"고 확인). 오판 원인: 전수 grep 을
  head -12 로 자른 실수. ruwe 는 최신 재처리본만 채워짐(Step6 스키마 세대차). ② ROI
  생존·스키마 일치({ra_deg,dec_deg,radius_arcsec}) — GUI 클릭 확인만 잔여.
  ③ '자동중심 마스킹'=Step8 PSF core cut(기본 off, 구상성단 코어 ePSF
  보호용) — ROI 와 **반대 방향**(측광 제외 vs 표본 선택)이라 중복 아님,
  사용자 결정: 셋 다 유지(core cut/멤버십/ROI 역할 분담).
  **core cut 검증도(사용자 질문 "논문급이냐")**: 단위테스트 6개 + 7-15
  PSF 아카이브의 core-policy 비교는 있으나, **그 검증의 결론이 "hard core
  cut off"** — 채택된 현행 정책은 「모든 소스 적합 + 미분해 블렌드는
  마스킹 대신 플래그」(psf_archive/README 33-35행)이고 논문 수치(M13
  98.0% clean 등)도 core cut OFF 로 얻은 것. 즉 core cut 은 논문에 기대는
  기능이 아니라 검증 과정에서 기본 off 로 밀려난 보조 옵션. 논문 서술은
  블렌드 플래그 정책 쪽으로. ④ PSF 오차전파
  사슬 건전(fit 공분산→1.0857·σF/F→w=1/σ² WLS, 규약 일치). ⑤ 잔차검출
  신규별 = 음수 det_uid 로 TSV 까지만(설계) — 마스터 부재라 CMD 탈락,
  union 마스터(로드맵 B1)가 살릴 통로. — ① VizieR: 시야 내 UBV 표준
  카탈로그 **없음** (자동탐색이 올바르게 빈손 보고; 옛 광전측광은 WEBDA에만).
  ② LCO 아카이브: `target_name="NGC 6811"` 공개 프레임 2,878장, **U raw 96장
  (전체 146장)** — U 관측은 존재. 단 **오늘 API가 서비스 전반 장애**(count>0
  인데 results 항상 빈 배열, 7월에 실제 받았던 M67 kb74 세트조차 count 0)라
  밤·기기별 상세를 못 얻었다. 재개 스크립트:
  `validation/psf_crossinstrument/lco_ngc6811_ubv_survey.py` (페이지네이션
  완비 — **API limit 최대 100**, 200 넘기면 400. covers= 공간질의는 신뢰 불가).
  재확인(같은 날 저녁): 동일 증상 — count 96 인데 직렬화되는 것은 2016-12-13
  fs02 B2+V2 뿐. **일시 장애가 아니라 구세대 프레임 레코드가 현 API 에서
  직렬화 안 되는 구조적 문제로 보임** → 복권 기대치 하향. 분광 prior 가
  공식 경로(사용자 결정)라 실질 영향 없음.
  U 영점 앵커 전략(시야 내 표준성 없음): 같은 기기·같은 밤에 M67 도 찍힌
  밤을 찾으면 **MMJ93 앵커된 우리 M67 이 표준장 전이 기준점**이 된다.
  차선: 분광 [M/H] prior (APOGEE +0.03).
- **표준성 재앵커가 APEX 기능으로 승격됐다** (사용자 요청). Qt-free 코어
  `apex/analysis/cmd/standard_anchor.py`(VizieR fetch+캐시·교차매칭·오프셋·
  |offset|>1 mag 가드) + 단위테스트 7개(무네트워크) + Step 10 워커 통합
  (`[cmd.standard_anchor]` enable/catalog/match_radius_arcsec/min_stars,
  실패 시 경고 후 비앵커 진행) + GUI 파라미터 다이얼로그 그룹("External
  Standard Anchor", 한국어 설명·툴팁) + approx 참조(σ≥0.1) 로그에 재앵커
  권고 문구. 엔드투엔드 검증: m67_ubv 사본에서 VizieR 실 fetch → 오프셋이
  검증 스크립트와 소수 넷째 자리까지 일치, QC(standard_anchor_offsets.csv)
  산출. 레포트 fig 2장(`fig_ub_rail.py`): 영점 진단 + rail 해소 4적합 비교.
- **나머지 성단(M5·M37·NGC457)은 구경 wide 로만 적합** — M5 나이·dm 문헌
  일치 + [M/H] −1.50(gri 바닥 재현), M37·NGC457 은 [M/H] 상한 rail(차등적색화
  / 수직 MS 정보 부족 — 정직하게 기록).
- **M5 는 이후 E: 재처리본(calibrated/20250308 41장)으로 PSF 까지 완주** —
  reprocess_batch --only M5(1-7+Step10 2,084별) → Step8 41/41(57분) → Step10
  재구축(1,913별 psf) → 적합 12.79/−1.55/14.26 **수렴 ✓**. 3중 비교(구 AIPPI
  구경/신 APEX 구경/신 PSF)가 ±0.05 dex 안에서 일치, Gaia 밝기 드리프트
  구경 −0.144 → PSF −0.021. **함정 적발: M37 프레임 "24/24 발견"은 같은 밤
  AIPPI 넘버링(pp_NNNN) 충돌 가짜 매치** — 헤더 열어보니 전부 M5 였다.
  M37·NGC457 은 진짜 부재 → **CR 세션 때 D:\APEX_backup 에서 재처리(사용자
  결정 2026-08-04)**.
- 마지막 커밋(직전): 2026-08-01 — fix(wcs): pixel_scale_arcsec 미설정 시 float(None) 크래시
- 오라클 **722 passed, 0 failed** (2026-08-01, 5분 21초). 7-31 아침 625 → +97
- 2026-08-02 워크트리(`claude2/festive-merkle-659e92`, 논문 fig 재작업 세션)에서
  재실행: **721 passed, 1 skipped, 0 failed** (3분 48초). skip 은
  `test_predicted_m50_reproduces_injection_calibration_set` — 로컬 보정 런
  데이터(`benchmark/runs` 미추적물)가 워크트리에 없어 건너뜀. 코드 회귀 아님
  (이번 세션은 `apex/` 무변경, `validation/paper/` 스크립트만 수정).
- 하네스: `AGENTS.md` 에 세션 시작 동기화 + 종료 프로토콜. Stop hook 이 이 파일 갱신을 검사

**2026-08-01 함정 — WCS 해는 `data_dir` 의 FITS 헤더에 쓴다 (사고 + 복구 완료)**

- 엔진 교차검증을 하려고 `run_wcs_solve` 를 엔진별로 **`result_dir` 만 분리**해
  세 번 돌렸다. **소용없다.** 해는 `result_dir` 이 아니라 **`data_dir` 의 원본
  FITS 헤더에 직접 쓰인다.** 세 엔진이 같은 15장을 순차로 덮어썼고, M13 sci 의
  WCS 가 원래 해에서 **270–408 초각**(4.5–6.8 arcmin) 어긋났다.
- **복구 완료.** 원래와 같은 설정(`engine="internal"`, 원래 `result_dir`)으로
  캐시를 지우고 15장 재계산(42 s). 원래 `frame_wcs_qc.csv` 와 대조해
  **15/15 가 1초각 이내, 최대 0.340"** (픽셀 0.395"/px 이므로 1픽셀 미만).
  원본 QC·캐시는 `E:\APEX_validation\_wcs_repair_backup\` 에 백업.
- **→ 재실행 완료 (`6d83df5`).** `data_dir` 를 엔진별 사본으로 분리하고 4단계
  검출을 공유해 세 엔진 비교를 완성, 논문 3.9 절에 반영. 내장 0.77 px <
  astnet 0.93 < ASTAP 1.10 (독립 Gaia 잔차), 해 일치 0.2–1.0". 부산물:
  외부 솔버 경로의 `frame_wcs_qc.csv` `rms_px` 는 정제 후 해를 채점해 엔진
  비교에 못 쓴다(7/8 프레임 비트동일) — 칩 task_18a74404.
- 실험 자체는 세 엔진 다 해를 풀었다(status `ok`/`solved`). 실패가 아니라
  **해가 한 파일에 겹쳐 써져서 비교가 불가능**했던 것이다. 캐시도 대체가 안 된다
  — 엔진마다 저장 위치·필드가 다르다(astap 만 `cache/wcs_solve/*.json`, 게다가
  `rms_arcsec` 가 비어 있다).

**2026-08-01 검출 임계 sigma 조사 — 코드는 안 건드리기로 결정**
`validation/gui_tools/SIGMA_QC_REPORT.md` · 커밋 `56c6a41`·`69ac32a`·`a1101d5`·`9828cd7`

- **결론: `detection.py` 에 QC 를 붙이지 않는다.** 「sigma 를 낮춰 돌린 산출물
  감지」가 목적이었는데 **`sigma_used` 가 이미 검출 JSON 에 기록**된다
  (`n_raw_detections`·`threshold`·`detect_capped`·`bkg_rms` 와 함께). 사후
  추적은 이미 된다. 기본값 3.2 에서 실측 순도 98%+ 라 새로 잡을 것도 없고,
  프레임 품질은 `fwhm_within_config`·elongation 이 이미 본다.
- **방법**: 배경 뺀 영상의 부호를 뒤집어 재검출하면 그 수가 잡음 기원 가짜
  검출 추정치다. WCS·외부 카탈로그 불필요(Step 4 에서 됨), 비용 +3.6%.
  Gaia DR3 대조에서 붕괴 구간 오차 2.4% (추정 1,309 vs 실측 1,341).
- **실측 — 안전 하한이 프레임마다 다르다**: NGC6811 B 1.5 · M67 g' 1.5 ·
  M13 B 1.8 · M13 R 2.0 · M3 B 2.2 (순도 95% 기준). **같은 성단·같은 밤인데
  필터가 다르면 하한이 갈린다.** 무너지는 지점은 다섯 장 모두 1.5→1.2 사이로
  공통. sigma 1.2 에서 검출 4,066개 중 가짜 1,341개(33%) — 「한 프레임
  4,000개」 기억과 수치가 맞고 `detect_keep_max=6000` 은 이를 통과시킨다.
- **선행 연구 확립**(논문 인용 확보): Serra, Jurek & Flöer 2012 PASA 29, 296
  (방법 원전) · Serra+2015 MNRAS 448, 1922 (SoFiA) · Molino+2014 MNRAS 441,
  2891 (ALHAMBRA, 광학 반전영상) · Hopkins+2002 AJ 123, 1086 (FDR) ·
  Huynh+2012 PASA 29, 229. **새로운 건 방법이 아니라 「임계 하나로는 부족하다」
  는 실측.**
- **닫힌 방향**: SoFiA 의 매개변수 공간 KDE 는 **이식 불가**. 광학 2D 단일
  프레임은 음수 표본이 한 자릿수다(NGC6811 sigma 3.2 에서 0개). 실익이 아니라
  작동이 안 된다. FDR 은 픽셀 단위 다중검정이라 `minarea` 와 안 맞물린다.
- **가정 검증**: 「잡음 0 대칭」은 광학에서도 성립. skew +0.058(M13)·+0.051
  (NGC6811) 로 포아송 예상(+0.045·+0.048)과 일치, 중앙값 치우침 0.02σ.
  **단 CR·hot pixel 은 음수 짝이 없어 완전한 사각지대** — CR 제거가 따로 담당.
  그래서 이 값은 「순도」가 아니라 **잡음 기원 가짜 검출 비율**이다.
- **재검토 조건 3가지**: ① 새 기기 도입(LCO 등 하늘 배경이 다름) ② 타인이
  헤드리스 대량 처리 ③ 기본 sigma 에서 오염 프레임이 실제로 발견될 때.
- **미결 — 기본 sigma 를 3.2 에서 2.0 으로 낮출까**: 실측상 2.0 은 안전 범위
  (순도 0.941~0.999, 최저는 M3 B). 검출은 M13 R 기준 1,515 → 1,955 로 +29%.
  다만 **① 그 +29% 가 isolation 필터·refbuild 를 거쳐 최종 CMD 에 얼마나
  남는지 안 쟀고, ② 완전도 보정값 `PEAK_SN50_DETECTION = 4.05` 가 sigma 3.2
  기준이라 낮추면 인공별 주입 재보정이 필요**하다. 이득을 먼저 재고 판단할 것.

**2026-08-01 Step 0 dark 매칭 — 결함 2건 수정, 전 타겟 감사, YZ Boo 재처리**

- **같은 밤 dark 가 공용 라이브러리를 막고 있었다** (`8cd2f7f`). `group_for_night`
  이 그 밤 dark 가 하나라도 있으면 전역 풀을 통째로 배제해서, `E:\darks` 에
  딱 맞는 dark 가 있어도 절대 안 뽑혔다. YZ Boo 20250430 의 283장이 −5 °C 인데
  −10 °C dark 를 쓴 진짜 원인. `dark_library` 폴백 + `DarkMatch.frames/source`.
- **YZ Boo 2밤 재처리·교체 완료** (D-006). ΔT 5.01 → 0.101. 옛 트리 삭제로
  45.4 GB 회수. 산출 프레임 차이는 sky 중앙값 −0.03 ADU(불변)에
  **밝은 픽셀 median Δ=0.000** — 변화가 hot pixel 꼬리에만 몰린 의도한 결과.
- **전 타겟 감사 결과 논문 성단은 무사** (D-008). M3 에서 60장이 ΔT 5.01 °C 로
  같은 결함을 겪었지만 전부 SDSS g/i/r 라 `FILTER_ALLOW={b,v,r}` 에 걸려
  `sci/` 에 못 들어갔다. **잘못된 dark 가 과학 산출물에 들어간 건 0건.**
- **마스터 에폭 혼합 경고** (D-007, `04b1391`) · **디스크 가드 C: 검사**(`5195016`)
- 재현 스크립트 일습: `E:\APEX_validation\_session_20260731_step0_multinight\`
  (`DISK_NOTES.md` 에 디스크 실태와 함정 정리)

**2026-07-31 편의성 개선 P1~P8 구현 완료 — `docs/convenience-step0-multinight-plan.md`**

- 오라클 **689 passed, 0 failed** (8분 7초). 기존 625 → +64 테스트.
- **밤 경계를 지역 정오(해가 떠 있는 시각)로 통일** (`apex/utils/night_utils.py`).
  Step 0 과 LC Step 1 이 같은 정의를 쓴다. 우선순위는 DATE-OBS+경도 →
  DATE-OBS+tz → 경로날짜 → UTC−12h. 경도·tz 가 둘 다 없으면 정오분할이
  그리니치 규칙으로 퇴화해 동아시아 저녁을 찢으므로 그때는 예전 신호를 쓴다.
- **P2 가 실데이터 결함을 드러냈다 — 사용자 판단 필요(D-006).**
  YZ Boo raw 전체에 dark 가 30 s/−10 °C 한 세트뿐인데 night 20250430 의
  283장은 −5 °C 에서 찍혔다(ΔT 5.0 °C, 암전류 1.8배 과소). 그 283장에
  **LC 완주에 쓴 g 97장이 포함**된다. 기존 UI 는 `dark 30s/-10C` 로만 표시했다.
- **P3 로 `[calibration]` TOML 이 살아났다.** 파싱 코드가 없어 죽어 있던
  섹션이고 GUI 설정은 창 닫으면 초기화됐다. 함께 `_CalibrationWorker._run` 을
  Qt-free `apex/analysis/calibration_run.py` 로 추출해 **GUI·헤드리스 스텝·
  재처리 스크립트가 한 함수를 쓴다**(M13 15장 비트 동일 확인).
- **P5 는 현재 자료를 개선하지 않는다(정직 기록).** M13·M5 실측 카탈로그의
  최근접 이웃이 최소 3.97" 라 기본 허용오차 2" 에서 경합이 안 일어난다.
  도달 가능한 결함을 막는 방어적 수정이다.
- 그 외: 미분류 프레임 가시화+우클릭 재분류(P4), Step 9 강제 제거(P6),
  merged workspace 원본참조 모드(P7), 소품 7건(P8).

**2026-07-31 편의성 구조 검토 → 구현 계획 수립**

- Step 0 자동 분류·매칭 + LC 멀티나잇 + Multi-Night Merger 전체 구조를 읽고
  개선 계획 P1~P8 로 정리했다. 절차는 **계획(완료) → 2차 검토 → Opus 구현**.
- 사용자 확정 방향 3개: ① 밤 경계는 자정이 아니라 해가 떠 있는 시간대에서
  자를 것(저녁 flat·새벽 flat 이 같은 밤), ② dark 온도 매칭은 0.1 °C 민감
  사용자를 위한 허용오차 설정화, ③ FITS 급 무거운 파일 복사 최소화.
- 조사 사실: `[calibration]` TOML 섹션은 **파싱 코드가 없는 죽은 섹션**,
  `storage_mode` 는 `"full"` 만 쓰는 자리표시자, 머저는 FITS 미복사(TSV 만 전량
  복사), 미분류 프레임은 스캔에서 무경고 탈락, 머저 위치매칭은 행순서 greedy.
- 2차 검토 질문 Q1~Q5 는 계획 문서 말미 + 아래 D-005.

**2026-07-31 실 GUI 구동 계측 세션 — `validation/lc_gui_run/GUI_RUN_REPORT.md`**

- `scripts/gui_step_timing.py` 신설. 실제 창 클래스를 스텝 순서대로 열어
  로드 시간·표 모양·창 크기·예외를 잰다. **CMD 12/12 · LC 12/12 전부 열린다.**
- **Step 8 PSF 창 로드 17.8초의 10.9초가 `build_ap_psf_comparison`** 이었다
  (`8cf63d4`). `_refresh_qc()` 가 창을 열 때마다 캐시를 버리고 다시 병합한다.
  어제 `mag_ap` 폴백을 넣기 전에는 늘 NaN 으로 즉시 끝나 안 보이던 비용 —
  **내가 만든 회귀**다. `psf_ap_vs_psf.csv` 디스크 캐시로 해결.
- **`param_file` 을 지정해도 이전 프로젝트가 이겼다** (`6c859e3`).
  `apex/.state/<mode>/project_state.json` 이 `params.P` 를 덮어쓴다. 평소 GUI 는
  파라미터 파일을 바꾸는 UI 가 없어 사용자 문제는 아니고, **명시해서 연 경우에만**
  지정한 쪽을 따르게 했다.
- Step 0 보정 스레드 병렬화는 **시도했다가 되돌렸다** — 37.5s → 39.3s, 배속 1.0x.
  작업 프로세스 CPU 61% 라 CPU 병목이 아니었다. 단 경합 환경(메이플스토리가
  CPU 44,000초 사용 중) 측정이라 무용 증명은 아니다.
- **LC 모드 완주** (`c8b0233`). YZ Bootis g 97장, Step 0 → LC Step 11 산출물 생성.
  **변광 검출은 확실**(FAP 1e-39, 위상별 중앙값 0.71 mag 변동).
- **「주기를 0.67% 로 재현」은 정정한다.** WCS 엔진만 바꿔 재처리하니 주기가
  **−7.00%** 로 튀었다. 추적하니 **자료가 주기를 제약하지 못한다** — 기저선
  0.127일(1.22주기), 주기 분해능 ΔP≈P²/T = **문헌 주기의 82%**, 문헌 주기에서의
  검정력이 피크의 **98~100%**(구별 불가). 주기 판정은 밤을 늘려야 가능하다.
- **소요: Step 0 보정 50분(31.0 s/장) + Step 1–7 37.4분. 그중 WCS 가 30분 = 80%**
  — 유일한 병목이다. Step 8 선택 20s · Step 9 광곡선 35s · Step 11 주기 ~10분.
- Step 9 의 `mag` 는 차등이 아니라 **기기등급**이다. 타깃·비교성이 함께 1 mag
  움직이는 것은 투명도 변화이고, 직접 차등을 내면 진폭 1.329 → **0.562 mag**.

**WCS 병목은 엔진 선택 실수였다 (2026-07-31 낮)**

- 헤드리스 엔진 우선순위는 `wcs_engine 명시 → astap_enable → astnet_local_enable
  → internal(기본)`. M13 템플릿에서 딸려 온 **`astnet_local_enable = true`** 때문에
  자체 엔진이 밀리고 astrometry.net(WSL)이 30분을 먹었다.
- `engine = "internal"` 로 같은 97장: **1796 s → 398 s (캐시 적중 53 s)**,
  **WCS-QC 42/97 → 97/97**, match_rate 0.971 → 0.991, rms 0.895 px.
  Step 7 도 detected 8,760 → **10,105** · forced 10,931 → **8,810** ·
  투명도 FAIL 1 → **0**.
- **버그: 자체 엔진 경로가 `frame_wcs_qc.csv` 를 안 썼다.** ASTAP·astnet 워커만
  쓰고 `_write_internal_summary_csv` 는 요약만 남겨서, Step 6/7 이 **이전 솔버의
  낡은 판정**을 계속 읽었다. 같은 스키마로 함께 쓰도록 수정.
  단 **이 자료에서는 하류 결과가 안 바뀌었다**(낡은 QC·새 QC 모두 10,105/8,810).
- **틀렸던 가설**: 「마스터 203개가 적은 건 QC 42 탓」 → QC 를 97 로 올려도 195
  그대로. 마스터 수는 필드의 실제 별 수와 refbuild 조건이 정한다.

**2026-07-30 우주선·EPSF 오염 세션 — 코어 기본값이 둘 바뀌었다**

- **우주선 제거를 Step 0 기본으로 켰다** (`d9ad1ef`). 구현은 있었는데
  `cosmetic_enable=False` 였고 reprocess 배치도 명시적으로 껐다. BANZAI 는 자기
  e91 에서 단일픽셀 스파이크의 46.7% 를 지운다(실측). **CMOS 는 검출의 16~40% 가
  1픽셀 스파이크**(M3 15.4% · M13 7.3% · QHY600 40% · Sinistro CCD **0%**)이고,
  ePSF 참조별 기준 「밝고 고립」이 정확히 그 최적해라 M67/QHY600 에서 ePSF 가
  2.75배 좁아지고 **PSF 플럭스가 구경의 32%** 로 떨어졌다.
- **ePSF 참조별에 방어선 2겹** (`001ea94`) — FWHM 하한 + **PSF 대칭 판정**
  (`psf_symmetric_mask`, AstralImage `_extended_bright_cores` 이식). CR 로 지목된
  45,433곳의 67.3% 를 막고 별 736개는 100% 보존. 단 **CR 제거의 대체재는 아니다**
  (대조: CR끔+방어없음 0.317 / CR끔+방어있음 0.65 / CR켬+방어있음 **0.985**).
- **CR 전/후 실측 대조** (`0ec14cb`, `1fbbd5e`) — 같은 raw 를 Step 0 부터 두 번
  처리. 측광값은 **1 mmag 이내 보존**(M13 +0.0002 · NGC6811 +0.0012), 진짜
  별(전프레임 검출)은 **1개씩만** 줄고, 1프레임 검출만 24~49% 사라진다.
  → **D-004 의 전제가 바뀌었다**(카드 머리에 경고 부착).
- **Johnson U 지원** (`f70cfde`) — `PARSEC_ISO_COL` 에 U 가 아예 없었다.
  UXmag(col 28) 추가, A0V/K형 색으로 검증.
- **U-B 로 축퇴 풀기 시도 → 실패** (`fc976b7`) — 나이 4.62→2.51, [M/H]
  −0.40→**−0.83**, E(B-V) 0.11→**0.33** 로 전부 악화. 원인은 Gaia→U 변환이
  `approx`·**σ=0.200 mag**(B 0.025·V 0.030 의 7~8배). **색을 늘려도 그 색의 영점이
  나쁘면 적합은 나빠진다** — 논문에 쓸 반례.
- **GUI Step 8 에 `Forced %` 컬럼** (`6897f3c`) — 강제 측광 비율을 모르면
  PSF/구경 비교를 오독한다(실제로 오진했다). 툴팁에 해석까지.

**진행 중 / 열린 것**

- **M45 광시야 EPSF** (2.0°×1.3°, 61.7 MP×20장) — 재보정~PSF 체인 실행 중.
  작업량이 M13 의 **22.3배**(픽셀 4배 × 프레임 1.33 × 소스 4.1)라 오래 걸린다.
  사용자 결정: 20장 그대로 완주.
- **GUI 실구동 사용감** — 화면 제어 승인 대기
- **Step 8 리포트가 헤드리스에서 안 나온다** — export 가 창(`_refresh_qc`)에
  묶여 있다. Step 10 은 워커에 있어서 헤드리스도 PNG 5종이 나온다. 이관하려면
  그림 생성 로직을 `apex/analysis` 로 빼야 한다.
- **아직 CR 없이 남은 성단**: M67 · M3(오염 15.4%) · M5. M13·NGC6811 은 재처리
  완료(기존은 `result_nocr/`·`sci_nocr/` 로 보존).
  → **사용자 결정 2026-07-30: CR 은 다른 세션에서 잡는다.**
  단 **CR 자체의 검증은 끝났다**(5축: 표준성·측광영향·별손상·효과·완전도).
  남은 것은 나머지 성단에 적용하는 일뿐이다. `docs/psf-contamination.md`
- **`keep_max` 는 정상이다** (2026-07-31 확인). 한때 「정렬 없이 자른다」고
  적었으나 **오독이었다** — 자르는 줄(`detection.py:629, 833`)만 보고 그 위의
  정렬을 놓쳤다. 두 경로 모두 `np.argsort(flux)[::-1]` 로 **밝기순 정렬이
  선행**한다(610행, 783행). 즉 어두운 별부터 버리는 예측 가능한 절단이고,
  완전도 모델이 다루는 바로 그 편향이다. M45 에서 10/19 프레임이 걸렸을 때
  y 분포가 정상이었던 것(0.05/0.54/0.96)도 우연이 아니라 설계대로였다.
- **위치기반(공간 가변) EPSF — 향후 구현.** 지금은 프레임당 EPSF 하나.
  2026-08-02 재실측(3.14 그림): 광역 안/밖 중앙값 차 4 mmag(기존 4.5 재현)
  이지만 **시야 모서리 bin(정규화 반경 0.88+)은 +28 mmag** — 기존 기록이
  안 본 구간이다. 별 단위 MAD(28 mmag)와 같은 크기라 당장 필요 없다는 결론은
  유지하되, 격자 도입 판단 기준은 이 모서리 값이다. 착수
  조건과 구현 방향은 `validation/psf_crossinstrument/REPORT_WIDE_EPSF.md`
  「향후 구현」절에 적었다 (grid_size 를 모델 단위로 승격 → `GriddedPSFModel`).
- **이소크론 축퇴 — 해소 실증 완료 (2026-08-04, `ec1701c`).** 선행 조건이던
  U 표준등급을 MMJ93(VizieR)으로 확보해 영점 재앵커 → [M/H] rail 해소.
  「지금」절 참조. 남은 정직한 한계: 단색 dm−E 축퇴(M13)·faint-MS 나이 희석.

**2026-07-29 PSF 완성 세션에서 끝난 것**

- **PSF 헤드리스 완주 실증** — reprocess M13 혼잡장 **15/15 + signature** (8분17초,
  단일워커 환산 ~60초/프레임, M13급 1,400소스). PSF vs 구경: 13/15 프레임
  Δ=−0.075±0.008 · MAD 0.038 mag. 튀는 2프레임은 그 자리에서 분해 —
  `0002-R` 추적불량 이중피크(워커 자체 경고 = PSF QC 정상작동),
  `0004-R` **구경 apcorr 단독 이상**(1.73 vs 1.46, 같은 별 −0.22 mag; PSF 는 안정) → 칩 발행
- **LCO 교차기기 PSF 완주** — Sinistro CCD 4앰프(NGC5985 rp 120s) + QHY600 CMOS(Proxima
  V 20s), steps 1-7 → step 8 전 과정. `E:\APEX_validation\psf_crossinstrument\REPORT.md`
  + fig 2장. MAD 0.027 / 0.039 mag. **§3.12 "카메라 한 대" 한계 해소 재료.**
  Sinistro 에서 형태 컷(모라비안 튜닝)이 후보 1개로 붕괴 → 완화 로직이 59개 복구(안전망 실증)
- 위 테스트가 드러낸 **헤드리스 공백 3건 수정** (`15787a9`): ① `[wcs] engine` 키 신설
  (ASTAP 이 헤드리스에서 도달 불가였음) ② `run_wcs_solve` target_coord 를 config 에서 유도
  ③ EPSF 품질 경고(이중피크·비대칭)를 `residual_meta.epsf_reference.quality_*` 로 영속화
- **GUI 코드 감사** — 파라미터 저장·로드 왕복 16/16 무손실(실측), 캐시 시그니처에 psf 60키
  완비, QThread+워커패널+ETA+Stop 정상. `parameters_cmd.py` 77-374 죽은 리터럴 맵 → 칩

**진행 중 / 열린 것**

- **GUI 실구동 사용감**(속도·파라미터 조작감) — 화면 제어 승인 대기
- **광시야 모노 축** — 로컬에 없음 확정(DSY=동일 C3-61000+3947mm, M45 npy 는 출처불명 폐기).
  ZTF 공개 프레임(0.86°, 1"/px, ~38MB/장) 다운로드 승인 대기
- PSF `flux_scale_correction`(구경 앵커) 기본 off — 프레임별 정규화 오프셋(+0.05~+0.16)을
  흡수할지 검토 후보 (CMD 는 프레임 ZP 가 흡수하므로 필수는 아님)

**2026-08-07 성능 감사 인수인계**

> **인수 완료 (2026-08-07 저녁, 사용자 지시)** — 이 작업은 Codex 에서 Claude
> 세션으로 넘어왔다. 실행 계획은 `docs/audit/APEX_PERF_DEV_PLAN.md`
> (Phase 0 계측 → 1 before 수치 → 2 최적화 O1 streaming/O2 workers/O3 LC cache
> → 3 clean run, 처리 순서 공용체인→CMD→LC, 합계 약 1주).
> baseline 메타데이터는 `validation/BASELINE_2026-08-07.md` 에 있고
> (수치 포함, peak RSS 만 미측정 — Phase 1 B1 이 채운다), native 구현의
> 탄생 이력·근거 등급은 `docs/audit/APEX_NATIVE_PROVENANCE.md` 에 있다.
> 그 근거 정렬 중 `bn.nanstd` float32 결함을 적발·수정했다(`718427f`).

- 다른 세션에서 진행 중인 Step 0 재처리는 **최적화 전 baseline**으로 끝까지 보존한다.
  raw FITS 입력·새 출력 폴더·git commit·파라미터/패키지 버전·worker 수·실행 시간·peak
  RSS를 남긴다. 기존 보정 산출물이나 기존 run을 입력/출력으로 재사용하지 않는다.
- 최적화의 목적은 단순한 속도 경쟁이 아니다. Step 0의 image-count-scaled OOM을 막고,
  LC의 반복 CSV/pandas 경로와 worker의 CPU/RAM/I/O oversubscription을 줄여 동일한
  과학 결과를 더 안정적이고 재현 가능하게 만드는 것이다.
- 감사 문서: `docs/audit/APEX_PERFORMANCE_AUDIT.md`,
  `docs/audit/APEX_LC_PERFORMANCE_REVIEW.md`,
  `docs/audit/APEX_BOTTLENECK_REVIEW.md`. 세 문서에 baseline → parity 검증 → 최종
  clean run 절차와 Bottleneck/native 구현의 사용 경계를 기록했다.
- baseline 완료 후 P0 후보(Streaming calibration, 공용 LC cache/compact matrix,
  bounded resource-aware workers)를 한 종류씩 적용하고, 고정 subset에서 검출원 수,
  calibration 통계, WCS, finite mask, 광도, CMD/LC를 비교한다. parity가 확인된 뒤에만
  raw Step 0부터 전체 clean run을 수행하며, 그 결과만 논문 최종 수치로 쓴다.

## 사용자 의견

- **validation/ 20 GB 를 최종적으로 어디에 둘지** — E로 옮겨  
  `07-27 23:45` 대시보드 결정. 카드 원문: `Main/DECISIONS.md`
- **APEX 논문 §3 을 지금 쓸지, 재검출 결과를 기다릴지** — 논문써 근데 내가 fig들 엄청 지적했던거 알아?
  `2026-07-27 23:37` 대시보드 결정. 카드 원문: `Main/DECISIONS.md`
  → **§3 을 쓴다(A).** 다만 **그림 지적 이력을 먼저 확인하고 시작할 것** — 사용자가
  fig 들을 여러 번 지적했다. 무엇을 지적했는지 모르면 같은 그림으로 다시 올라간다.
  찾을 곳: `Main/harvest/apex/` (15건) · `TRACK_PAPER.md` · `validation/paper/captions/`.
  (이 항목은 8731 인스턴스가 낡은 코드라 자동으로 안 내려와서 손으로 넣었다 — F-018)

## 다음 3개

1. **큐에 올리기 전에 전제를 확인한다 — 규율 항목.** 오늘 큐 항목 셋의 전제가
   차례로 틀렸다: 「R 밴드만 2 차항이 켜진다」(실제 15 중 10), 「밤이 늘면 표가
   짧아진다」(실제 늘어난다), 「창에 선별 표가 없다」(있었다). 다음 항목을 쓸 때
   **주장 한 줄마다 확인 명령 하나**를 같이 적는다. 새 작업 아님, 다음 큐 작성 때
   적용.

2. **GUI 파라미터 창 6 개 이관** — step4·5·8·9cmd·9lc·10. `choice` 종류는
   2026-08-18 에 준비됐다(값 저장·값으로 선택·모르는 값 보존). 저 여섯은 `ParamSpec`
   을 안 쓰고 **Qt 위젯을 손으로 짠 대화상자**라 목록 교체가 아니라 재작성이다.
   사용자가 다듬어 둔 UI 라 비포/애프터 캡처 확인이 필요하다.

3. **헤드리스 워크스페이스로 스텝 11 주기창을 열어 본다.** 이제 근거 파일 넷이
   다 생기니, 창이 실제로 그걸 읽어 「비교성 N 개, 체크성 ID」를 표시하는지 확인한다.
   파일이 생겼다는 것과 창이 그걸 읽는다는 것은 다른 주장이다. 30 분.

### 오래 밀린 것 (날짜 붙은 채로 유지)

- **(2026-08-21) 논문 이소크론 수치를 2 차 색항 변경으로 재확인** — D-012 를 닫으면서
  등급이 최대 0.086 mag 움직일 수 있다고 쟀는데, **논문 수치는 MMJ93 표준성으로
  영점을 재앵커한 실행에서 나왔고 여기서 쓴 `reprocess/` 워크스페이스가 아니다**
  (여기 M67 은 [M/H] −0.50, 수렴 실패). 원고에 넣기 전에 그 실행을 다시 돌려
  영향을 봐야 한다. 「다음 3개」가 아니라 여기 두는 이유: 원고 작업을 재개할 때
  하는 일이고, 지금 개발 순서(개발→리뷰→최종검토→논문)에서는 뒤다. 2~3 시간.
- **(2026-08-06) GUI 워크스페이스 선택** — File 메뉴에 `Open Workspace…` /
  `New Workspace…` / `Recent`(`~/.apex/recent.json` 영속화), 열 때
  `check_workspace_identity()` 경고를 다이얼로그로. 지금은 `--params` 로만
  전환 가능(반쪽). 코어는 완성(`apex/config/config_io.py`), GUI 배선만 남았다. 2 시간.
- **(2026-08-05) 앱 세그폴트 미해결** — 실행본이 exit 139. MCMC 전 경로를
  스크립트로 재현했으나 **안 죽었다**. 창 닫힘·Fit 창 토글·다른 스텝 진입 중
  어느 시점인지 사용자 확인 필요. **단서 없이 추적하지 말 것.**
  (참고: 2026-08-18 에 pytest 안에서 QApplication 을 teardown 하면 Windows
  access violation 이 나는 것을 확인했다. 같은 원인일 수 있으나 앱 경로에서
  재현되지는 않았다.)
- **(2026-08-05) 창 크기·하드코딩 색 잔여** — CMD step8(975×861)·step7(791×857)이
  노트북 화면을 130px 안팎 넘는다. LC step9(912px)는 구조가 원인이라 레이아웃
  재설계가 필요하고 **사용자 승인 대상**. 하드코딩 색 16 파일 약 79 곳.

### 이 트랙의 위생

**「지금」이 908 줄이다.** 세션 시작 주입이 예산 초과 경고를 내는 이유이고, 완료된
작업이 계속 쌓인 결과다. 다음에 손볼 때 08-07 이전 항목을 `docs/audit/` 로 옮기고
「지금」은 최근 두 세션만 남길 것.

### 측정 완료 — `n_det_frames` 분포 · 두 성단 (2026-07-28)

**데이터: M67 (Moravian C3-61000 · 2026-02-08 밤 · g′ 60 s × 10장 · `E:\APEX_validation\reprocess\M67\sci`)**
실행: `apex run --mode lc` steps 1–4 + 6 (헤드리스, `union_min_frames=1`).
검출 10/10 프레임 · 총 6,399 검출 · 중앙값 FWHM 7.85 px · union 마스터 **766 소스**.
산출물: `E:\APEX_validation_output\_ndet_m67g\` (config·log·`ref_catalog.tsv`).

| n_det_frames | 개수 | | min_frames | 남는 수 | 비율 |
|---:|---:|---|---:|---:|---:|
| 10 | **560** | | ≥1 | 766 | 100% |
| 9 | 18 | | ≥2 | 688 | 89.8% |
| 2–8 | 110 | | ≥3 | 669 | 87.3% |
| **1** | **78** | | ≥5 | 630 | 82.2% |

**분포는 쌍봉이다.** 73.1%가 10장 전부에서 검출(진짜 별), **10.2%가 딱 1장에서만**
검출(우주선·잡음 피크·문턱 근처 깜빡임), 중간(2–9장)은 얇다(bin당 ~1.7%).
중앙값 10 · 평균 8.29. → `ref_union_min_frames` 판단 재료가 준비됐다 (아래 D-004).

**전수 분류 (07-28, 78개 전부):** 각 소스를 10장 전부에서 잘라 「가장 밝은 프레임의
유의도」×「나머지 9장의 지속성」으로 나눴다 —
**확실한 우주선/핫픽셀류 34** (한 방 스파이크 + 나머지는 잡음뿐) ·
**확실한 문턱 별 17** (지속성 ≥3.0σ — 늘 희미하게 보임) ·
경계·애매 27 (그중 13은 25σ급 스파이크 동반이라 우주선 쪽에 가깝다).
⚠️ 판정선 민감: 지속성 2.5σ 로 낮추면 문턱 별이 30까지 늘어난다 — 경계가 연속이라
단정하지 않고 구간(17~30)으로 쓴다. `n1_classification.csv` · `n1_scatter.png`

### M3 추가 측정 — "한 시야로는 애매하다"는 지적의 답

사용자 지적(07-28): *"저 대상 하나만으로는 좀 에매하네"*. **맞았다.**
같은 코드(`classify_any.py`)로 M3 를 재 보니 숫자가 눈에 띄게 움직인다.

**데이터: M3 (Moravian C3-61000 · B · 60 s × 10장 · 2026-05-15 · `reprocess/M3/sci`)**
검출 10/10 · 6,172 검출 · FWHM 7.44 px · union 마스터 **874 소스**.

| | M67 · g′ (산개성단) | M3 · B (구상성단) |
|---|---:|---:|
| 마스터 소스 | 766 | 874 |
| 전 프레임 검출 | 560 (**73.1%**) | 311 (**35.6%**) |
| 1프레임 검출 | 78 (**10.2%**) | 128 (**14.6%**) |
| └ 우주선/핫픽셀류 | 34 | 44 |
| └ 문턱 별 (≥3.0σ) | 17 | 40 |
| └ 문턱 별 (≥2.5σ) | 30 | 69 |
| 컷2 잔존 | 688 (89.8%) | 746 (85.4%) |
| **컷2가 버리는 진짜 별** | **17~30 (2.2~3.9%)** | **40~69 (4.6~7.9%)** |
| 대조군 지속성(중앙) | 7.6σ | 6.1σ |

**읽는 법:** 쌍봉 자체는 두 시야 다 살아 있다(1과 최대값에 봉우리). 그러나
**골짜기 깊이가 다르다** — M67 은 중간 구간이 bin 당 ~1.7% 로 텅 비지만, M3 는
중간이 훨씬 차 있고 전 프레임 검출이 35.6% 밖에 안 된다. M3 가 더 붐비고(구상성단)
더 얕게 찍혔기 때문이다(대조군 지속성 7.6→6.1σ).
→ **컷 2 의 비용이 시야에 따라 2배 넘게 움직인다.** 한 시야로 숫자를 고정할 수 없다는
사용자 지적이 실측으로 확인됐다. 그림: `E:\APEX_validation_output\fig_two_targets.png`

## 사용자 판단 필요

**열려 있는 것 1 개.** 아래 「닫힌 것」은 결론만 한 줄로 색인하고 원문은 그
아래에 그대로 둔다 — 이 절의 일은 «지금 무엇이 사용자를 기다리는가» 를 한 눈에
보이게 하는 것이고, 닫힌 것들이 그 위에 쌓이면 그 일을 못 한다.

### 열림

**없다.** D-012 가 2026-08-21 에 닫히면서 열린 항목이 0 이 됐다.

### 닫힘 — 결론만 (원문은 그 아래)

- **D-012** · 2 차 색항 — 시야마다 적합해 쓰던 것을 멈춘다 (2026-08-21)
- **D-011** · 의존성 — 범위는 두고 잠금 파일을 더한다 (2026-08-20)
- **D-015** · 중복 삭제 — 순 30.0 GB 회수 (2026-08-20)
- **D-014** · 별칭이 안 갈릴 때 — 고르지 않고 후보 표를 낸다 (2026-08-20)
- **D-013** · 옛 워크스페이스 빈 커브 — 원인과 처방을 말하고 막는다 (2026-08-20)
- **D-010** · 구경 반지름 — 측정이 0.8 을 지지 (2026-08-19)
- **D-009** · step8 씨앗 수정 후 혼잡 성단 재처리 — 5 성단 137 프레임 완주 (2026-08-18)
- **D-008** · 전 타겟 dark 매칭 감사 — 논문 성단은 무사 (2026-08-01)
- **D-007** · dark 전역 폴백이 관측 시기를 섞는다 — 경고 추가 (2026-08-01)
- **D-006** · YZ Boo 재처리 — 완료, 45.4 GB 회수 (2026-07-31)
- **D-005** · 편의성 개선 계획 P1~P8 — 구현 완료 (2026-07-31)
- **D-004** · 별 후보 최소 프레임 수 (`ref_union_min_frames`) (2026-07-30)

<details>
<summary>닫힌 항목 원문</summary>

- **D-012 · 2 차 색항 — 「R 밴드만」이 아니었고, 시야마다 적합하면 안 되는 값이었다
  (2026-08-21, `2eaf783`).** 사용자: *"d12는 뭔소리인지 모르겠음 알아서 ㄱㄱ"*.

  **전제부터 틀렸다.** 실측 5 성단 15 개 적합 중 2 차가 켜진 건 10 개이고 밴드를
  가리지 않았다. 진짜 문제는 값이었다 — 카메라 한 대(Moravian C3-61000)로 찍은
  다섯 시야에서:

      R(V−R) ct2 = -0.243, -0.003, +0.309    ← 같은 색지수인데 부호가 반대
      B(B−V) ct2 = -0.088, -0.197, -0.048    ← 부호도 크기도 일관

  색항은 필터와 검출기가 표준 통과대역과 얼마나 다른지를 말하는 **기기 속성**이라
  어느 성단이 화면에 있느냐로 달라질 수 없다. 한 시야의 곡률을 다른 시야에
  빌려주면 24 쌍 중 **10 쌍이 곡률을 안 쓰느니만 못해졌다**(최악 +16.9 mmag).

  **한 시야 안의 어떤 통계로도 안 갈린다.** 가장 유의한 곡률(NGC 6811 R,
  |ct2|/σ = 46.3)이 하필 가장 안 옮겨가는 것이었다 — F 검정이든 AIC 든 더 확신을
  갖고 채택했을 것이다. 색 범위·|ct2| 크기도 진짜/가짜를 가르지 못했다.

  옛 규칙 「산포가 나빠지지만 않으면 채택」은 애초에 검사가 아니었다. 2 차는 자유
  파라미터가 하나 더 많아 최소제곱 잔차가 나빠질 수가 없다(직선 자료 40/40 확인).
  실제로 걸러 낸 건 임의의 상한 |ct2| ≤ 0.25 였고, 어느 밴드에 켜지느냐가
  **비물리적인 값이 그 문턱 위냐 아래냐**로 정해지고 있었다. 사용자가 «판정 기준이
  경계에 있다»고 적어 둔 그 경계다.

  **처방:** 적합은 그대로 하고 `ct2_fitted`·`ct2_sigma` 로 기록만 한다. 적용은
  `cmd.zp.quadratic_color_term = "B=-0.10"` 으로 측정해서 넣을 때만이고 기본값은
  비어 있다. 기기마다 다른 값이라 하드코딩하지 않는다.

  **비용과 이득을 같이 잰다.** 시야별 적용은 이 자료에서 최대 0.086 mag(M3 V)의
  색 의존 오차를 등급에 넣고 있었다. 반대로 진짜인 B 곡률을 잃는 손해도 있다 —
  M13 Step 10 헤드리스 재실행:

      직선만            B 산포 0.0547
      그 시야 자기 2 차  B 산포 0.0408   ← 옛 동작
      설정한 −0.11      B 산포 0.0373   ← 시야 넘어 공유한 값이 자기 적합보다 낫다

  즉 B 는 곡률이 진짜이고, 제대로 쓰는 길은 **시야를 넘어 측정해 설정에 넣는 것**
  이다. 그림 `validation/fig_d012_color_term.png`,
  재현 `validation/color_term_quadratic_*.py`.

  **아직 안 본 것:** 논문 이소크론 수치는 MMJ93 표준성으로 영점을 재앵커한 실행에서
  나왔고 여기서 쓴 `reprocess/` 워크스페이스가 아니다(M67 여기선 [M/H] −0.50,
  수렴 실패). 원고에 넣기 전에 그 실행을 다시 돌려 이 변경의 영향을 확인해야 한다.

- **D-011 · 의존성을 실제로 핀할지 → 「둘 다」로 닫힘 (2026-08-20).** 사용자:
  *"의존성? 알아서 최적화해"*. **범위와 고정은 서로 다른 일을 한다** — 범위는
  «무엇 위에서 도는가», 고정은 «무엇 위에서 돌았는가». 둘 중 하나를 고르면 한쪽을
  잃는다. 그래서 `pyproject.toml`·`requirements.txt` 의 범위는 그대로 두고
  **`requirements-lock.txt`** 를 더했다 — 논문 수치를 낸 환경 91 개를 그대로
  고정하고, 어느 파이썬·어느 커밋에서 뜬 것인지 머리에 적었다.

      새로 설치     pip install -e .[gui]          ← 범위, 지금까지대로
      수치 재현     pip install -r requirements-lock.txt

  실행 매니페스트는 이미 실행마다 환경을 적고 있으므로, 결과는 «무엇으로 만들어졌나»
  를 스스로 지니고 잠금은 «거기로 돌아가려면 무엇을 설치하나» 를 준다.

  **둘이 어긋나는 것**이 이 구조의 유일한 위험이라 테스트 5 개로 못박았다:
  잠금이 정확히 고정하는가 · 파이썬·커밋을 적었는가 · 핵심 의존성이 전부 잠겼는가 ·
  **범위가 잠긴 판을 여전히 허용하는가** · **`pyproject` 가 고정으로 바뀌지 않았는가**.

- **D-015 · 중복 삭제 → 실행함, 순 30.0 GB 회수 (2026-08-20).** 사용자: *"중복 지워"*.
  545 개 지웠다가 **10 개를 되돌렸다**. E 여유 105 → **135 GB**. 표(csv·tsv)
  163 개 0.21 GB 는 측정값이라 처음부터 뺐고, AstrylStudio 중복 12.8 GB 는 다른
  프로젝트라 안 건드렸다.

  **안전 검사가 두 번 모자랐고, 둘 다 되돌려서 구했다.**
  ① `validation/` 이 `E:\APEX_validation_output` 정션이라 **레포 추적 파일 3 개**가
  「삭제 가능」 구역 안에 있었다 (F-061). `git checkout` 으로 복구했고, 삭제
  스크립트에 **`git ls-files` 가드**를 넣어 같은 목록을 다시 넣으면 거부한다.
  ② 도달성 검사가 `validation/` 만 훑어서 **`tests/` 가 이름으로 적어 둔 보정
  프레임 4 개**를 지웠다 (F-062). `test_detection_limit.py` 가 `3/7 runs` 로
  **스킵**했고 — 실패가 아니라 스킵이라 총계만 보면 통과처럼 보였다. 단서는
  **스킵 수**였다(직전 0, 테스트를 하나 더했는데 1,307 아닌 1,306). 레포 전체를
  다시 훑어 7 개를 남은 사본에서 복원했고 그 파일은 24/24 로 돌아왔다.

- **D-014 · 별칭이 안 갈릴 때 무엇을 답으로 낼지 → 「고르지 않는다」로 닫힘
  (2026-08-20).** 사용자: *"애초에 정보가 부족한건데, pdm이 맞는지 LS가 맞는지
  전공자가 아니라서 몰라"*. **두 밤으로는 원리상 안 갈리므로 코드가 하나를 고르는
  것 자체가 틀린 설계다.** 규칙 대신 후보 표를 낸다 —
  `period_candidates_<filter>_ID<n>.csv` 에 주기도표 최고점과 별칭 후보를 전부,
  일·시간 두 단위로. YZ Boo 에서 가까운 셋이 2.50~2.53 시간에 모이고 나머지는
  2.08·2.27·2.78 로 흩어져, 문헌 2.498 시간 한 줄만 알면 즉시 고른다.
  **내가 질문을 잘못 냈다** — 「관측 판단이니 선생님이」로 올렸는데 그건 이 사용자의
  전공 밖이다 (OPERATOR C-036, C-034 재발).

- **D-013 · 옛 워크스페이스 빈 커브 → 「말하게」로 닫힘 (2026-08-20).** 사용자:
  *"13은 알아서 해"*. 두 줄로 될 줄 알았고 선택 지도는 실제로 살아났지만
  (0 → 3 필터), **여전히 0/77 이었다** — 옛 측광표에는 `source_id` 도 `ID` 도
  없고 `det_uid` 뿐이라, 잇는 데 옛 스텝 8 의 위치 매칭을 재구현해야 한다.
  해당 워크스페이스 9 개가 전부 YZ Boo·AE UMa 이고 둘 다 현행 판이 따로 있어
  비례에 안 맞는다. **진짜 결함인 「조용함」만 없앴다** — 이제 원인과 처방을
  말하고 막는다(GUI 로 한 번 열거나 스텝 1-7 재실행).

- **D-010 · 구경 반지름 0.8/2.4 vs 1.0/3.0 → `0.8` 유지로 닫힘.** 사용자 지시대로
  1.0/3.0 으로 올려 다섯 성단 137 프레임을 재처리했고, 외부 참조등급 대조에서
  **15 밴드 중 13 이 나빠졌다**(중앙 +0.008 등급). 분리 실험으로 재는 구경이
  원인임을 확인했고, 고립 인공별 SNR 스윕(최적 0.9×FWHM, 밝기별 0.6~0.8)과
  성단 정밀도(5/5)까지 같은 방향이었다. 근거: `docs/audit/APERTURE_RADIUS_DECISION.md`.
  **남은 실무 선택 하나**: `large_scale` 은 3.0 이 2.4 보다 0.001 등급 낫다.
  그것만으로 다섯 성단을 다시 돌릴 값은 아니라 2.4 로 뒀다 — **다음에 재처리할
  일이 생기면 그때 3.0 으로 올린다.**

### 새로 열린 것

- **D-009 · step8 씨앗 수정 후 혼잡 성단 재처리 — 실행함, 논문 수치는
  안 바뀐다 (2026-08-15)** — M13 15 프레임을 다시 돌려 수정 전 산출물과
  대조했다(`validation/psf_engines/seedfix_product_delta.py`).
  - **집계는 그대로다.** 프레임 영점 산포 B 69.6→70.4 · R 57.2→58.2 ·
    V 53.1→53.3 mmag, 별당 등급오차 중앙값 세 밴드 다 ±0.1 mmag,
    참조별 수 719/996/935 → 720/995/935. **논문 수치를 고쳐야 할 이유가 없다.**
  - **바뀌는 건 겹친 쌍뿐이다.** 21,023 별 중 107 개가 10 mmag 넘게 움직였고
    최대는 530 mmag. 표의 별은 오히려 늘었고(한 프레임 1,499→1,509),
    생산 게이트 통과분이 21,108→21,053 으로 준 것은 되살아난 짝이
    **혼잡 불신 플래그(8192)를 제대로 달았기** 때문이다 — 그 별들의 이웃거리
    중앙값은 1.03 FWHM(전체 3.09). 수정 전에는 짝이 지워져 살아남은 쪽이
    고립된 것처럼 보이며 오염된 등급으로 게이트를 통과했다.
  - **효과는 혼잡도에 비례한다 — 다섯 성단 137 프레임 완료.** 프레임당
    10 mmag 초과 이동 별 수: M5 10.0 · M13 7.1 · M3 5.8 (구상) vs
    NGC6811 0.3 · M67 **0(비트 동일)** (산개). 영점 산포는 다섯 다 1 mmag
    안이고 M3·M5·NGC6811 은 오히려 미세 개선.
  - **꼬리를 쫓다 별개 결함을 잡았다** (`91ce0b3`): 한 프레임에서만 잡힌 별의
    등급오차가 `1.4826·MAD/√n` 특성상 **정확히 0.000** 이었다(다섯 성단
    전부, 항목의 0.4~1.0 %). 이제 그 별 자신의 측광오차로 떨어진다.
  - 나머지 네 성단(NGC6811·M67·M3·M5, 122 프레임)은 판본을 맞추려고 같은
    날 배경으로 재처리했다. 수정 전 사본은 각 워크스페이스의
    `cmd_psf_PRE_SEEDFIX` · `cmd_zeropoint_PRE_SEEDFIX` 에 있다.

> 형식: `### D-00N · 제목` + `무엇 / 지금 / 선택지 / 근거 / 권고 / 되돌리기`.
> 근거가 비면 결정이 아니라 **선행 작업**이다. 대시보드가 「결정 불가」로 분리한다.

### D-008 · 전 타겟 dark 매칭 감사 — **논문 성단은 무사하다 (2026-08-01)**

- **왜 했나**: YZ Boo 에서 5 °C 어긋난 dark 가 몇 달간 안 보이고 쓰였다.
  같은 결함이 논문 그림의 근거인 성단에도 있었는지 확인이 필요했다.
- **방법**: 각 타겟의 기존 `calibration.json` 이 **실제로 기록한** dark 와,
  같은 light 에 대해 현재 매처가 고르는 dark 를 대조
  (`_session_20260731_step0_multinight/audit_cluster_darks.py`). 옛 규칙을
  재구현하지 않고 실행 기록을 기준으로 삼았다.
- **결과**:

  | 타겟 | 변화 | 내용 |
  |---|---|---|
  | NGC6811 · M13 · M5 | **0** | 이상 없음 |
  | M67 | 2 | `F-HA-*` 10 s light 에 60 s dark → 20 s (Δexp 10 s) |
  | M3 | 60 | SDSS g/i/r, **ΔT 5.01 °C** (60s/0C → 60s/5C, 새 ΔT 0.06) |

- **핵심**: **잘못된 dark 가 과학 산출물(`sci/`)에 들어간 건 0건이다.**
  M3 의 60장은 전부 SDSS g/i/r 로 `FILTER_ALLOW={b,v,r}` 에 걸려 reorg 에서
  버려진다. CMD 에 쓰인 Johnson B/V/R 30장은 원래부터 `60s/5C` 로 옳게 맞았다.
  M67 의 2장도 Ha 라 `sci/`(g·i·r 각 10장)에 없다.
- **결론**: 재처리가 필요한 타겟은 없다. 실제 피해는 YZ Boo 뿐이었고 처리됐다(D-006).
- **되돌리기**: 읽기 전용 감사 — 아무것도 안 바꿨다.

### D-007 · dark 전역 폴백이 에폭을 섞는다 — **경고 추가됨 (2026-08-01)**

- **무엇**: `group_for_night` 의 `_pool()` 은 그 밤에 dark 가 없으면 전역 풀로
  떨어지는데, 전역 풀은 `(노출, 온도)` 만으로 묶으므로 **취득 시기가 몇 달
  다른 dark 가 한 마스터로 쌓인다.**
- **실제 사례**: YZ Boo 재처리에서 night 20250429 의 마스터 다크가
  10장 → **30장**이 됐다(2025-04-28 의 YZ Boo dark 20장 + `E:\darks` 의
  2024-11-06 dark 10장). P1 이 dark 를 실제 취득밤으로 옮기면서 그 밤에
  같은 밤 dark 가 없어졌기 때문이다.
- **실측 영향(작다)**: 산출 프레임 비교에서 sky 중앙값 변화 −0.026 ADU
  (−0.018 e-), **밝은 픽셀 median Δ = 0.000** (실제 별은 안 건드림).
  변화는 hot pixel 꼬리에 몰려 있다(p99.9 ≈ 8~11 ADU). 프레임 수가 10 → 30 이라
  마스터 읽기잡음은 오히려 √3 개선. **중립~약간 이득**으로 판단해 교체를 막지 않았다.
- **처리(③ 채택)**: 마스터가 `EPOCH_WARN_DAYS`(30일)를 넘겨 섞이면 경고하고,
  `calibration.json` 의 masters 항목에 `epoch_nights`·`epoch_span_days` 를 남긴다.
  bias·dark·flat 셋 다 적용. 실데이터 확인: night 20250429 의 dark 가
  `20241106×10 + 20250428×20`, span **173일** 로 경고가 뜬다. 같은 자료의
  night 20250430(단일 에폭 `20241004×10`)은 조용하다. 인접한 두 밤(1일 차)은
  정상 운용이므로 문턱 아래다.
- **남은 선택지 ②**(전역 폴백을 가장 가까운 에폭으로 좁히기)는 **안 했다** —
  영향이 작다고 실측됐고(sky −0.026 ADU, 밝은 픽셀 median Δ=0), 프레임 수가
  늘어 마스터 읽기잡음은 오히려 개선된다. 경고가 있으니 이제 보이기라도 한다.
- **되돌리기**: 커밋 하나.

### D-006 · YZ Boo 재처리 — **완료 (2026-07-31)**

- **무엇**: 실행 기록(`E:\APEX_validation\reprocess\YZBoo_g,YZBoo_2n` 의
  `calibration.json`)이 night 20250430 의 **283장 전부 `master_dark_30s_-10C`**
  를 썼음을 보여준다. 그 밤의 light 는 **−5 °C** 에서 찍혔다(ΔT 5.0 °C).
  그 283장에 **LC 완주에 쓴 g 97장이 포함**된다.
- **원인 (자료가 아니라 코드였다)**: `E:\darks\dark-30s-5` 에 30 s/−5 °C
  dark 10장(2024-10-04)이 **있었다**. `group_for_night` 이 같은 밤 dark 가
  하나라도 있으면 전역 풀을 통째로 배제해서, YZ Boo 폴더 안의 30 s/−10 °C
  dark 가 라이브러리를 막았다.
- **수정 완료**: `8cd2f7f` (라이브러리 폴백) + `451e659` (P1 night 재배정).
  이제 같은 조건에서 30 s/−5 °C 를 고른다(ΔT 5.01 → **0.16 °C**).
  덤으로 20260325 의 120 s light 31장이 40 s dark(Δexp 80 s) 대신
  120 s dark(Δexp 0)를 쓴다.
- **실측한 비용**: 벌크 암전류는 무시 가능(30 s 에 median 0.1 ADU = 0.07 e-,
  read noise 2.1 e-). 문제는 **hot pixel 마스크**다 — APEX 는 마스터 다크에서
  이걸 만드는데, −10 °C 다크 기준 마스크가 −5 °C 에서 실제로 hot 인 픽셀의
  **29.4%(34,022 px)를 놓친다**(놓친 픽셀 median 11.3 · max 899 ADU).
  온도가 노후화보다 중요하다: 같은 온도 6개월 차 마스터끼리 차이 std 7.29 ADU,
  5 °C 차 1개월 차는 11.03 ADU.
- **완료**: 2026-07-31 재처리·교체 끝. `E:\APEX_validation\reprocess\YZBoo_2n`
  이 새 산출물이다(sci 364장, masters 9개). 옛 트리는 삭제(45.4 GB 회수,
  E: 78.8 → 101.9 GB). 소요 7,209초.
  - night 20250430 283장 → `master_dark_30s_-5C_20241004` (ΔT 5.01 → **0.101**)
  - night 20250429 81장 → `master_dark_30s_-10C_20250428` (ΔT 0.070)
  - temp_mismatch 0 · missing_flat 0
- **바뀐 정도(실측)**: sky 중앙값 −0.03 ADU(−0.02 e-)로 사실상 불변,
  **밝은 픽셀 median Δ = 0.000** (실제 별 보존). 변화는 hot pixel 꼬리에 집중
  (20250430 p99.9 ≈ 21~35 ADU, 20250429 ≈ 8~11 ADU) — 의도한 그대로다.
- **Steps 1–11**: 2026-08-01 **다른 세션이 새 프레임으로 완주**했다
  (`result/` 에 step1~7 · lc_lightcurve · lc_detrend · lc_period · lc_selection ·
  qa_report, 11:55 까지). 결과 검증은 아직 — 「다음 3개」 3번.
  폴더 안 `REPROCESS_NOTE.md` 의 "Steps 1-11 미실행" 문구는 이제 낡았다.
- 재현 스크립트: `E:\APEX_validation\_session_20260731_step0_multinight\`
  (`reprocess_yzboo_2n.py`, `swap_yzboo_2n.py`, `compare_old_new.py`)

### D-005 · 편의성 개선 계획(P1~P8) — 구현 완료 (2026-07-31)

- **무엇**: Step 0 분류·매칭 + 멀티나잇 개선 (`docs/convenience-step0-multinight-plan.md`).
- **지금**: **P1~P8 전부 구현·검증 완료**. Q1~Q5 는 권고안대로 확정해 구현했다
  (정오분할·JD-gap 폴백 강등·불명 프레임 제외·reference opt-in·tol 1.0 °C).
  결정을 되돌리고 싶으면 말할 것 — 각 P 가 독립 커밋이라 되돌리기 쉽다.
- **검토에서 바뀐 것**: ① P1 "DATE-OBS 무조건 우선" 철회 → 경도/tz 가용성 4단
  폴백(UTC −12h 는 동아시아에서 밤을 찢음), ② P3 는 기존
  `scripts/_reprocess_step0.py`(GUI 워커 offscreen 구동)를 Qt-free 코어로 통합하는
  경로로 구체화, ③ P7 로더 중앙화 성립 확인(step9 는 index `path` 미사용,
  직접 glob 은 qa_report 뿐).
- **다음**: 남은 후보는 D-006(YZ Boo dark) 판단, Step 0 재실행 시 이어하기
  (현재는 덮어쓸지 묻기만 함), reference 모드를 실 멀티나잇에서 실측 검증.
- **되돌리기**: 커밋 `451e659`(P1) ~ `b69a974`(P7) 각각 독립.

### D-004 · 별 후보를 몇 장 이상에서 보여야 진짜 별로 인정할까 (`ref_union_min_frames`)

> ⚠️ **2026-07-30: 이 결정의 전제가 바뀌었다.** 우주선 제거가 Step 0 기본으로
> 켜졌다(`d9ad1ef`). 같은 raw 를 두 번 처리해 비교하니 1프레임 검출이
> **M13 233→176(−24%) · NGC6811 357→183(−49%)** 로 줄고, **전프레임 검출은
> 1개만** 줄었다(측광값은 1 mmag 이내 보존). 즉 아래 논의가 「1프레임 검출의
> 절반은 우주선」이라는 전제 위에 서 있었는데, 그 우주선이 이제 컷 이전에
> 걸러진다. 남은 176~183개는 진짜 문턱 별 비중이 높아졌으므로 **컷 2 의 비용이
> 커졌다** — 완화 또는 1 유지 쪽 논거가 강해졌다. 근거: `docs/psf-contamination.md` §5.

- 무엇: 같은 시야를 찍은 10장을 겹쳐 "별 후보 명단"(마스터)을 만들 때, **10장 중
  몇 장 이상에서 잡힌 것만 명단에 올릴지**. 현재 기본값 1 = 한 번만 보여도 올린다.
- 지금: M67 g′ 10장으로 실측했다. 후보 766개마다 "10장 중 몇 장에서 잡혔나"를 셌더니 —
  **560개(73%)는 10장 전부**에서 잡혔고 (진짜 별은 매 장 같은 자리에 있으니 당연),
  **78개(10%)는 딱 1장에서만** 나타나고 나머지 9장에선 안 보였다. 한 번 찍히고
  사라지는 건 대개 우주선 히트, 잡음이 우연히 별처럼 보인 것, 검출 문턱에 걸쳐
  깜빡이는 아주 어두운 놈이다. 중간(2~9장)은 얇다 — 즉 후보들이 "매번 보이는 놈"과
  "한 번만 보인 놈" **두 집단으로 갈라져 있다** (히스토그램에 봉우리가 둘 = 쌍봉).
- 선택지:
  **A. 2장 이상** — "두 번 이상 보인 것만 인정". 한 번만 보인 78개(10%)가 정확히 잘린다.
  **B. 3장 이상** — 97개(13%) 제거. 두 번 보인 경계 후보 19개가 더 빠진다.
  **C. 1 유지** — 아무것도 안 버린다. 어두운 진짜 별 17~30개를 지키는 대신 우주선류 34~47개도 명단에 남는다.
- 근거: 두 집단 사이 골짜기가 1과 2 사이에 있어서, 컷을 2에 두면 가짜 집단만 깨끗하게
  갈라진다 (분포가 밋밋했으면 컷이 임의 숫자가 됐을 것). 논문 인용 선례와도 맞다 —
  `stetson1994`(ALLFRAME)가 "상당 비율의 프레임에 나타나는 것만 인정"을 쓴다.
  **두 시야에서 확인했다 (M67 산개성단 g′ · M3 구상성단 B).** 쌍봉은 둘 다 유지되지만
  비용이 다르다 — 컷 2 가 버리는 진짜 별이 M67 은 2.2~3.9%, **M3 는 4.6~7.9%** 다.
  M3 가 더 붐비고 얕아서(전 프레임 검출 73%→36%, 대조군 지속성 7.6→6.1σ) 문턱 별이
  더 많이 생긴다. **즉 컷 값은 하나로 못 박고 시야 성격에 따라 달라져야 할 수 있다.**
- 실물 확인 (07-28, 질문 "우주선히트인지 뭔지 이미지로 보여줘야" 답변):
  무작위 표본(seed=0)으로 1장짜리 4개 + 10장짜리 2개를 골라 **같은 하늘 좌표를
  10장 전부에서 잘라** 비교했다. 그림: `E:\APEX_validation_output\_ndet_m67g\answer_cutouts.png`
  · **3개(ID 483·125·157)** — 딱 한 프레임에만 1~2픽셀짜리 날카로운 점(중심 유의도
    10~27σ), 나머지 9장은 배경뿐. 이 데이터의 별 상은 ~8px 뭉툭한 원반이라
    모양부터 다르다 → **우주선 히트/핫픽셀류. 하늘의 별이 아니다.**
  · **1개(ID 633)** — 모든 프레임에 3~6σ로 희미하게 **항상 보이는** 실제 별.
    검출 문턱에 걸쳐 공식 검출은 1번뿐이었던 것.
  · **전수 분류(78개 전부)로 표본 인상을 보정하면:** 우주선류 34~47 ·
    **진짜 문턱 별 17~30** (마스터 766개의 2.2~3.9%) · 나머지 애매.
    → **A안(컷 2)의 실제 비용 = 어두운 진짜 별 17~30개를 명단에서 뺀다.**
    기준 명단(마스터)의 목적이 "믿을 수 있는 앵커"라면 지불할 만한 비용이고,
    "하나라도 놓치기 싫다"면 C안 논리가 선다.
- **선택지를 다시 짠다 (07-28) — 사용자: *"뭐가 나은건지는 모르겠네"***
  못 고르는 게 당연했다. **두 비용을 맞바꾸라고 물었는데, 한쪽 비용은 없앨 수 있다.**
  C안의 유일한 실측 피해는 「진짜 별 6~12개가 붐빔으로 오판」이고, 그건
  `crowding_flag` 계산에서 1프레임 검출을 빼면 사라진다. `n_det_frames` 는
  1158줄에서 이미 설정되고 crowding 은 1379줄이라 **그 시점에 값이 있다.**
  KDTree 를 `n_det_frames >= 2` 로만 만들고 질의는 전체 행에 하면 된다
  (신뢰 행은 자기 자신이 잡히므로 두 번째 거리를 쓴다).
  ⚠️ `ref_master_union=False`(단일 앵커) 경로에는 `n_det_frames` 가 없으므로 가드 필요.
  **그러면 남는 질문은 하나뿐이다 — 이 마스터가 기계 밖으로 나가는가?**
  | | 어두운 진짜 별 | 붐빔 오판 | 명단 순도 |
  |---|---|---|---|
  | A (컷 2) | **17~69개 삭제** | 없음 | 가짜 0 |
  | C (현행 1) + crowding 수정 | 전부 보존 | **없음** | 가짜 10~15% 남음 |
  · **내부 측광 기준으로만 쓴다** → C + 수정. 잃는 게 없다. **게다가 C 는 현재 기본값이라
    설정을 안 바꿔도 된다** — 결정 없이 버그 하나만 고치면 끝난다.
  · ~~논문 카탈로그·외부 공개로도 낸다 → A~~ **철회 (07-28).** 사용자: *"논문 카탈로그라는건
    뭔소리야 카탈로그를 왜내"*. 확인하니 원고의 Data availability 는 **소스코드(MIT) +
    `run_all.py` 그림 재생성 + Pan-STARRS 교차매칭 캐시**뿐이다. 별 카탈로그 공개도
    VizieR/CDS 등재도 없다. 검증 논문이지 카탈로그 논문이 아니다.
    → **내가 A 를 살리려고 없는 시나리오를 만든 것이다.** A 의 남은 근거가 없다.
- **결정됨 (07-28) — 기본값은 1 유지, `crowding_flag` 오염만 고쳤다.**
  A 의 유일한 근거였던 "공개 카탈로그 순도" 가 사실이 아니었으므로(위 철회) 컷을 올릴
  이유가 없다. 마스터는 파이프라인 내부용이고, 가짜는 하류(비교성 선정 QC·깊이 QC·
  구경보정 SNR≥40)에서 이미 걸러진다. 유일하게 안 걸러지던 `crowding_flag` 오염을
  코드로 막았다.
  **수정:** `refbuild.py` 의 이웃 계산을 `neighbor_distances()` 로 분리하고,
  KDTree 를 `n_det_frames >= 2` 행으로만 만든다(질의는 전 행). 회귀 검사 6건
  (`tests/test_refbuild_neighbor_trust.py`).
  **실데이터 확인 (M67 g′ · M3 B):**
  | | 수정 전 | 수정 후 |
  |---|---:|---:|
  | 1프레임 소스가 유발한 붐빔 | M67 3 · M3 13 | **0 · 0** (구조상 불가능) |
  | 붐빔 표시된 진짜 별 | M67 38 · M3 238 | 35 · 225 |
  ⚠️ **부작용을 재서 적는다.** `n_det_frames` 가 여기서 쓸 수 있는 유일한 신호라
  확실한 우주선뿐 아니라 **문턱 별도 트리에서 빠진다.** 꺼진 플래그의 정체:
  M67 3개는 전부 우주선 원인(순이득), **M3 13개는 우주선 6 · 문턱 별 6 · 애매 1**.
  그 6개는 진짜 별인데 이웃으로 안 세어진다 — 검출한계 소스가 이웃 조리개에 주는
  플럭스가 작아 받아들일 만하지만 **공짜가 아니라 맞바꾼 것**이다.
  남은 여지: 공개 카탈로그를 내기로 하면 그때 A 를 다시 검토한다. 지금은 계획이 없다.
- 되돌리기: 설정 한 줄이라 언제든 바꿀 수 있다. 명단 재생성은 1분이 안 걸린다
  (이번 실측: 검출 50초 + 병합 4초).

</details>

## 함정

- **「동일하다」를 보고하기 전에 그 파일이 이번 실행에서 다시 쓰였는지 본다.**
  `--force` 를 줘도 스텝이 산출물을 **일부만** 쓸 수 있다. LC 스텝 11 은 필터별만
  쓰고 합본을 안 써서, 08-01 창 실행 파일이 그대로 남아 있었고 나는 그걸 자기
  복사본과 비교해 「17 자리까지 동일」이라고 했다. **입력이 다른데 출력이 같으면
  그 자체가 경보다** (2026-08-21, F-082).
- **창과 헤드리스가 같은 것을 다른 파일에 쓰고 있지 않은지 본다.** 창은
  `selection_<필터>.json`, 스텝은 `lc_target_selection.json` — 둘 다 「스텝 8 선택」
  인데 서로 안 읽었다 (2026-08-21). **이 함정을 적은 날 같은 걸 새로 만들었다** —
  `comparison_screening_<필터>` 를 창은 `.csv`, 내가 만든 서비스는 `.tsv` 로 썼다.
  **새 산출물 파일은 확장자 없이 기본 이름으로 grep 하고 만든다** (F-085).
- **큐 항목의 전제를 확인하고 적는다.** 오늘 셋이 차례로 틀렸다 — 「R 밴드만
  2 차항이 켜진다」(15 중 10), 「밤이 늘면 표가 짧아진다」(늘어난다), 「창에 선별
  표가 없다」(있었다). **주장 한 줄마다 확인 명령 하나**를 같이 적는다 (2026-08-21).
- **중첩 모형을 잔차 산포로 비교하면 안 된다.** 파라미터가 하나 더 많은 쪽은
  최소제곱 잔차가 나빠질 수가 없어서, 「나빠지지만 않으면 채택」은 사실상 항상
  참이다. 2 차 색항이 이 규칙으로 실측 15 개 적합 중 10 개에 켜져 있었다 (2026-08-21, D-012).
- **유의성은 「이 값이 맞다」가 아니라 「이 자료에 대해 확신한다」는 뜻이다.**
  가장 유의한 2 차 색항(|ct2|/σ = 46)이 다른 시야에 가장 안 맞았다. 계수가
  **기기 것인지 시야 것인지**는 시야를 바꿔 봐야만 안다 (2026-08-21).
- **폴백을 가진 계산에 `except Exception` 을 두면 버그가 예외가 아니라 다른 답이
  된다.** LC 스텝 8 의 새 선별이 `if not values:`(numpy 배열에서 터짐) 한 줄 때문에
  통째로 꺼졌는데, 종료 상태는 `ok` 였고 비교성만 `[1,2,3,...]` 이었다. **어느 쪽
  경로를 탔는지 산출물이 말해야 한다** — `selected_by` 를 파일에 적고 나서야 눈에
  보였다 (2026-08-20, F-076).
- **`if not x:` 는 리스트·집합에만 안전하다.** numpy 배열·DataFrame 이 들어올 수
  있는 자리에서는 `if x is None:` 으로 쓰고 길이 판단은 순회에 맡긴다.
- **설정 값이 「목록의 하나」인지 「특별한 뜻의 낱말」인지 구분한다.**
  `lightcurve.filter` 의 `all` 은 표식인데 조회 키로 넘겨서 364 프레임 워크스페이스에
  «측광 없음» 이라고 했다 (2026-08-20, F-077).
- **GUI 대 헤드리스 대조는 한 프로세스에서, 설정이 스스로 말하는 경로로.** 하네스가
  `result_dir` 을 덮어써서 다른 워크스페이스 둘을 비교하고 「불일치」라고 했다.
  `prefer_current` 기본값(창 `True` · 자동선별 `False`)도 같은 함정이었다
  (2026-08-20, F-078).
- **픽스처가 만드는 파일 이름은 생산자·소비자 코드에서 가져온다.** 또 지어냈다
  (`<stem>_phot.csv` vs 실제 `photometry_<frame>.tsv`). F-054 와 같은 부류인데
  그때는 운영 코드와 픽스처가 같은 허구를 공유해 8 일간 초록이었다
  (2026-08-20, F-079).
- **오라클이 같은 코드에서 두 번 다르게 나왔다.** 한 번은 `3 failed, 1309 passed`,
  다음은 `1312 passed`. 무작위 순서 플러그인은 **설치돼 있지 않다** — 순서 의존이
  아니라 흔들림이다. 첫 실행은 내가 같은 E: 를 캡처로 두드리는 중이었고 549초,
  두 번째는 432초였다. 실패 이름을 못 건졌다(로그가 tail 로 잘림). **오라클은
  다른 E: 작업과 겹치지 않게 돌리고, 실패 목록은 `-rf` 로 파일에 남긴다**
  (2026-08-20).

- **파일을 지우기 전에 `git ls-files` 로 묻는다.** 경로 접두사로 「내 것/남의 것」을
  가르는 건 정션이 있으면 성립하지 않는다 — `validation/` 은
  `E:\APEX_validation_output` 을 가리키므로 **레포 내용이 「검증 산출물」 트리 안에
  있다.** 소유권은 경로가 아니라 인덱스가 안다 (2026-08-20, F-061).
- **삭제 도달성 검사는 레포 전체를 훑는다.** 논문 그림만 생각하면 놓친다 —
  `tests/test_detection_limit.py` 는 보정 프레임 7 개를 이름으로 적어 두고,
  `benchmark/runs/*/manifest.json` 도 프레임을 적는다 (2026-08-20, F-062).
- **스킵 수를 통과 수와 같이 본다.** 자료가 없으면 skip 하는 테스트는 파일이
  사라져도 **실패로 안 나온다.** `1,306 passed` 는 정상처럼 보이지만 직전이
  `1,306 passed, 0 skipped` 였고 그 사이 테스트를 더했다면 산수가 안 맞는다.
  `pytest -rs` 로 사유를 읽을 것 (2026-08-20).
- **창 코드의 이관 난이도는 위젯 접촉 횟수가 아니라 「읽기」의 종수다.** 출력
  (`setText`·`setItem`)은 훅으로 덮으면 끝이고 설계가 필요한 건 입력뿐이다.
  LC 스텝 10 을 「재설계·반나절 이상」으로 오판한 게 이 구분을 안 해서였다 —
  제일 큰 `result_table` 51 회는 전부 `setItem` 이었고, 실제 읽기 30 곳 중
  26 곳은 이미 `_sync_state_from_controls` 한 곳에 모여 있었다.
  **`_sync_*_from_controls` 같은 이름이 있으면 경계가 이미 있는 것이다**
  (2026-08-19, F-059).
- **옮긴 메서드는 정의된 모듈의 전역을 본다.** 창이 상속해도 이름은 새 모듈에서
  찾으므로, `QMessageBox` 나 `refresh` 를 새 모듈에 스텁으로 두면 **창의 기능까지
  죽는다.** 처방은 스텁이 아니라 훅이다 — 기반이 채널을 갖고 창이 구독한다
  (2026-08-19).
- **`savefig` 는 빈 figure 도 불평 없이 쓴다.** 그리기 호출이 `if update_ui:`
  안에 있으면 배치 경로는 백지 PNG 를 정상 저장한다. 이 세션에서 두 번 났다
  (Step 4 QC, LC step10). **그림을 붙였으면 반드시 열어 볼 것** — 파일 크기만
  봐서는 안 보인다 (2026-08-19).
- **관문 스텝은 통과하는 경로를 한 번은 실제로 밟아 볼 것.** `blocked` 는
  실패처럼 안 생긴다 — 「설정이 아직 없구나」로 읽힌다. LC 스텝 8 은 마스터
  카탈로그 이름을 확인 없이 적어서(`master_sources.csv`, 실제는
  `ref_catalog.tsv`) **만들어진 이래 어떤 워크스페이스에서도 통과한 적이 없었고**,
  테스트 11 개는 전부 초록이었다 — 픽스처가 같은 이름을 지어냈기 때문이다
  (2026-08-19, F-054).
- **픽스처를 내가 지어내면 그 테스트는 내 가정을 검사한다.** 처방은 리터럴
  비교가 아니라 **생산자의 「완료」 정의에 묶는 것**이다: 소비자가 요구하는
  파일을 쓰면 생산자의 `is_complete()` 가 참이 되어야 한다. 리터럴로 적으면
  같은 실수를 테스트에 복제할 뿐이다.
- **식별자 치환은 부분문자열을 삼킨다.** 모듈을 옮기며 이름을 같이 바꾸지 말 것.
  `ensemble_series` → `raw_lightcurve` 치환이 `_build_ensemble_series` 까지
  갈았다. 이관 직후엔 **`ast` 로 미정의 이름을 훑을 것** — `Load` 이름에서
  정의·임포트·지역을 빼면 빠진 게 남는다. 이번에 2 개가 나왔고 그 중 하나는
  호출부가 `try/except` 라 실행해도 안 보였다 (2026-08-19, F-055).
- **하드코딩 색 감사는 한 줄 grep 으로 하지 말 것.** `grep setStyleSheet |
  grep '#'` 은 여러 줄 f-string 의 색을 놓쳐 "0곳 남음" 오판을 낸다.
  또 `["\']#...` 처럼 따옴표 **직후**만 찾는 정규식도 안 된다 —
  `"QLabel { color: #607D8B; }"` 처럼 문자열 중간에 있는 게 대부분이다.
  괄호 깊이로 setStyleSheet **문장 전체**를 모은 뒤 `#[0-9A-Fa-f]{3,6}` 을
  검색할 것 (2026-08-05, 이 세션에서 두 번 다 틀렸다).
- **`setMinimumSize` 가 콘텐츠 최소치보다 작으면 위젯이 겹친다.**
  `StepWindowBase` 는 모든 스텝 창에 900×700 을 깔지만, 콘텐츠가 그보다 크면
  QLayout 이 최소치를 넘겨 짓눌러서 버튼이 잘리고 라벨이 겹친다(클리핑이
  아니라 겹침이라 눈에 잘 안 띈다). 창이 "이상하게 눌린다" 는 신고가 오면
  먼저 `minimumSizeHint()` 를 재고 `win_tree.py` 로 어느 자식이 끄는지 볼 것.
  처방은 `scroll_wrap` 이고, 고정폭 열이면 `preferred_width` 를 같이 줄 것.
- **`tame_canvas` 를 세로 초과 창에 쓰면 역효과다.** 최소 크기를 *올리는*
  함수라, 이미 Expanding 정책 + 높이 바닥이 있는 캔버스에 추가로 걸면
  창 최소치만 더 커진다. 슬라이버로 찌그러질 때만 쓸 것 (2026-08-05).
- **`validation/` 은 E 드라이브 정션이다.** 그 아래 스크립트에서
  `Path(__file__).resolve()` 를 쓰면 링크를 따라가 레포 밖(`E:\`)으로 나가서
  `apex` 를 못 찾는다. `absolute()` 를 쓸 것 (2026-07-30).
- **PSF/구경 플럭스비는 검출된 별로만 재라.** 강제 측광 위치(`forced_flag=True`)는
  구경이 거의 0 을 재므로 비율이 10~68 로 폭주한다. 섞으면 강제 비율이 높은
  자료가 「PSF 이상」으로 보인다 — 실제로 NGC6811 을 그렇게 오진했다.
  강제 비율은 필터·노출에 크게 좌우된다(같은 밤 B 74% vs R 49%).
- **재처리 후 산출물을 비교할 때 `master_id` 로 조인하지 마라.** 마스터가 다시
  만들어져 ID 가 재할당된다(M13 1574→1501). 하늘 좌표로 짝지어야 한다 —
  ID 조인은 MAD 1.24 mag, 좌표 매칭은 0.0038 mag.
- **로컬 astrometry.net 인덱스는 남천을 안 덮는다** (2mass-04 시리즈 48청크 중 31개).
  남천 대상(예: dec −62° Proxima 필드)은 blind 가 영원히 안 풀린다 — `[wcs] engine = "astap"`
  으로 지정할 것 (D50 은 전천 1,476파일). QHY600 교차기기 검증 때 발견 (2026-07-29).
- **`validation/` 은 커밋하지 않는다.** 20,088 MB / 10,990 파일. `.gitignore`에 있음.
  논문 자산(`validation/paper/figures`, `captions`, `논문작업`, `fig*.py`)만 추적한다.
- **실행은 `run.bat` 또는 `.venv-deploy\Scripts\python`.** 시스템 python 아님.
- **`step10_zeropoint_calibration.py:2199, 2804`** — pandas `DataFrameGroupBy.apply` 폐기 예정.
  pandas를 올리면 **영점보정 결과가 조용히 달라질 수 있다.** 올리기 전에 회귀 스냅샷을 뜰 것.
- 초과검출 게이트 임계는 실측 산포(실프레임 194장/24그룹, 최대 1.53배, 99분위 1.49)에서
  나왔다. REVIEW 1.8 / FAIL 2.5. **임의 숫자가 아니므로 함부로 바꾸지 말 것.**
- `n_sources`가 아니라 `n_raw_detections`를 봐야 한다. 전자는 필터·상한 뒤 값이라
  과검출이 정상처럼 보인다.
- **`max_workers = 1` 이면 플레이트솔빙이 사실상 안 끝난다.** 프레임당 80~290초라
  30장이면 **95분**이다. M3 (`E:\APEX_validation\reprocess\M3\parameters.toml`) 가
  `[parallel]`·`[wcs]` 둘 다 1 이어서, 2026-07-11 실행이 3번째 프레임에서 중단됐고
  그 결과 M3 는 30장 중 **2장만** WCS 를 갖고 있다 (`0001-B`, `0001-R`).
  같은 날 M67 은 `workers=12` 로 30/30 을 10분에 끝냈다 — **설정 차이 하나뿐**이다.
  판별법: `result/cache/astnet_solve.log` 의 헤더 줄에서 `workers=` 를 본다.
  로그가 결과 줄 없이 끊겨 있으면 실패가 아니라 **중단**이다.
  (2026-07-28 워커 8로 재실행 → 프레임당 79초, 정상 완주)
- **M3 설정의 `target.name` 이 `NGC6811` 로 잘못 적혀 있다.** 좌표(205.548, 28.377)는
  M3 가 맞다. 이름만 다른 설정에서 복사된 흔적이라 산출물 라벨이 틀릴 수 있다.
- **헤드리스 wcs 스텝은 `pixel_scale_arcsec` 미설정이면 죽는다** (`wcs_solve.py:4092`).
  `getattr(P, "pixel_scale_arcsec", nan)` 은 키가 None 으로 존재하면 기본값이 안 먹는다.
  프레임에 이미 WCS 가 있으면 step5 를 건너뛰어도 된다 — refbuild 는 헤더에서 직접 읽는다
  (`analysis/refbuild.py:391`). 단 step6 의 `required_inputs` 가 `step5_wcs` **폴더 존재**를
  요구하므로 폴더만 만들어 주면 된다 (2026-07-28 측정 때 그렇게 했고, 폴더 안에
  README_WHY_EMPTY.txt 로 사유를 남겼다).

## 최근 세션 원문

`C:\Users\bmffr\Desktop\Main\harvest\apex\` (15건, 2026-05~07)
