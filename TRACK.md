# TRACK: apex-dev — APEX 소프트웨어 v1.0 릴리즈

> 이 파일이 이 트랙 상태의 **유일한 진실**이다. 세션을 열면 여기부터 읽는다.
> 세션을 끝내기 전에 `## 지금` / `## 다음 3개` / `## 함정`을 반드시 다시 쓴다.
> 논문 트랙은 별도: [TRACK_PAPER.md](TRACK_PAPER.md)

## 완료 정의

측광 파이프라인이 실관측 데이터에서 GUI·헤드리스 양쪽으로 완주하고,
회귀 테스트 전량 통과 상태로 v1.0 릴리즈 태그가 붙는다.

## 오라클 — 이 트랙의 검증 명령

```bash
.venv-deploy/Scripts/python.exe -m pytest tests/ -q
```

- **통과 기준:** 620 passed, 0 failed  (2026-07-28 +6 — 이웃 신뢰 검사)
- **소요:** 약 7분
- **마지막 실행:** 2026-07-28 — 620 passed, 6 warnings (4분 41초)
- CI: `.github/workflows/tests.yml` 외 3개

## 지금

- 마지막 커밋: `2e4d6f3` 2026-07-31 — docs(validation): 실 GUI 구동 계측
- 오라클 **625 passed, 0 failed** (2026-07-31, 16분 59초)
- 하네스: `AGENTS.md` 에 세션 시작 동기화 + 종료 프로토콜. Stop hook 이 이 파일 갱신을 검사

**2026-07-31 편의성 구조 검토 → 구현 계획 수립 — `docs/convenience-step0-multinight-plan.md`**

- Step 0 자동 분류·매칭 + LC 멀티나잇 + Multi-Night Merger 전체 구조를 읽고
  개선 계획 P1~P8 로 정리했다. 절차는 **계획(완료) → 2차 검토 → Opus 구현**.
- 사용자 확정 방향 3개: ① 밤 경계는 자정이 아니라 해가 떠 있는 시간대에서
  자를 것(저녁 flat·새벽 flat 이 같은 밤), ② dark 온도 매칭은 0.1 °C 민감
  사용자를 위한 허용오차 설정화, ③ FITS 급 무거운 파일 복사 최소화.
- 조사 사실: `[calibration]` TOML 섹션은 **파싱 코드가 없는 죽은 섹션**,
  `storage_mode` 는 `"full"` 만 쓰는 자리표시자, 머저는 FITS 미복사(TSV 만 전량
  복사), 미분류 프레임은 스캔에서 무경고 탈락, 머저 위치매칭은 행순서 greedy.
- 2차 검토 질문 Q1~Q5 는 계획 문서 말미 + 아래 D-005.

**2026-07-31 실 GUI 구동 계측 세션 — `validation/lc_gui_run/GUI_RUN_REPORT.md`**

- `scripts/gui_step_timing.py` 신설. 실제 창 클래스를 스텝 순서대로 열어
  로드 시간·표 모양·창 크기·예외를 잰다. **CMD 12/12 · LC 12/12 전부 열린다.**
- **Step 8 PSF 창 로드 17.8초의 10.9초가 `build_ap_psf_comparison`** 이었다
  (`8cf63d4`). `_refresh_qc()` 가 창을 열 때마다 캐시를 버리고 다시 병합한다.
  어제 `mag_ap` 폴백을 넣기 전에는 늘 NaN 으로 즉시 끝나 안 보이던 비용 —
  **내가 만든 회귀**다. `psf_ap_vs_psf.csv` 디스크 캐시로 해결.
- **`param_file` 을 지정해도 이전 프로젝트가 이겼다** (`6c859e3`).
  `apex/.state/<mode>/project_state.json` 이 `params.P` 를 덮어쓴다. 평소 GUI 는
  파라미터 파일을 바꾸는 UI 가 없어 사용자 문제는 아니고, **명시해서 연 경우에만**
  지정한 쪽을 따르게 했다.
- Step 0 보정 스레드 병렬화는 **시도했다가 되돌렸다** — 37.5s → 39.3s, 배속 1.0x.
  작업 프로세스 CPU 61% 라 CPU 병목이 아니었다. 단 경합 환경(메이플스토리가
  CPU 44,000초 사용 중) 측정이라 무용 증명은 아니다.
- **LC 모드 완주** (`c8b0233`). YZ Bootis g 97장, Step 0 → LC Step 11 산출물 생성.
  **변광 검출은 확실**(FAP 1e-39, 위상별 중앙값 0.71 mag 변동).
- **「주기를 0.67% 로 재현」은 정정한다.** WCS 엔진만 바꿔 재처리하니 주기가
  **−7.00%** 로 튀었다. 추적하니 **자료가 주기를 제약하지 못한다** — 기저선
  0.127일(1.22주기), 주기 분해능 ΔP≈P²/T = **문헌 주기의 82%**, 문헌 주기에서의
  검정력이 피크의 **98~100%**(구별 불가). 주기 판정은 밤을 늘려야 가능하다.
- **소요: Step 0 보정 50분(31.0 s/장) + Step 1–7 37.4분. 그중 WCS 가 30분 = 80%**
  — 유일한 병목이다. Step 8 선택 20s · Step 9 광곡선 35s · Step 11 주기 ~10분.
- Step 9 의 `mag` 는 차등이 아니라 **기기등급**이다. 타깃·비교성이 함께 1 mag
  움직이는 것은 투명도 변화이고, 직접 차등을 내면 진폭 1.329 → **0.562 mag**.

**WCS 병목은 엔진 선택 실수였다 (2026-07-31 낮)**

- 헤드리스 엔진 우선순위는 `wcs_engine 명시 → astap_enable → astnet_local_enable
  → internal(기본)`. M13 템플릿에서 딸려 온 **`astnet_local_enable = true`** 때문에
  자체 엔진이 밀리고 astrometry.net(WSL)이 30분을 먹었다.
- `engine = "internal"` 로 같은 97장: **1796 s → 398 s (캐시 적중 53 s)**,
  **WCS-QC 42/97 → 97/97**, match_rate 0.971 → 0.991, rms 0.895 px.
  Step 7 도 detected 8,760 → **10,105** · forced 10,931 → **8,810** ·
  투명도 FAIL 1 → **0**.
- **버그: 자체 엔진 경로가 `frame_wcs_qc.csv` 를 안 썼다.** ASTAP·astnet 워커만
  쓰고 `_write_internal_summary_csv` 는 요약만 남겨서, Step 6/7 이 **이전 솔버의
  낡은 판정**을 계속 읽었다. 같은 스키마로 함께 쓰도록 수정.
  단 **이 자료에서는 하류 결과가 안 바뀌었다**(낡은 QC·새 QC 모두 10,105/8,810).
- **틀렸던 가설**: 「마스터 203개가 적은 건 QC 42 탓」 → QC 를 97 로 올려도 195
  그대로. 마스터 수는 필드의 실제 별 수와 refbuild 조건이 정한다.

**2026-07-30 우주선·EPSF 오염 세션 — 코어 기본값이 둘 바뀌었다**

- **우주선 제거를 Step 0 기본으로 켰다** (`d9ad1ef`). 구현은 있었는데
  `cosmetic_enable=False` 였고 reprocess 배치도 명시적으로 껐다. BANZAI 는 자기
  e91 에서 단일픽셀 스파이크의 46.7% 를 지운다(실측). **CMOS 는 검출의 16~40% 가
  1픽셀 스파이크**(M3 15.4% · M13 7.3% · QHY600 40% · Sinistro CCD **0%**)이고,
  ePSF 참조별 기준 「밝고 고립」이 정확히 그 최적해라 M67/QHY600 에서 ePSF 가
  2.75배 좁아지고 **PSF 플럭스가 구경의 32%** 로 떨어졌다.
- **ePSF 참조별에 방어선 2겹** (`001ea94`) — FWHM 하한 + **PSF 대칭 판정**
  (`psf_symmetric_mask`, AstralImage `_extended_bright_cores` 이식). CR 로 지목된
  45,433곳의 67.3% 를 막고 별 736개는 100% 보존. 단 **CR 제거의 대체재는 아니다**
  (대조: CR끔+방어없음 0.317 / CR끔+방어있음 0.65 / CR켬+방어있음 **0.985**).
- **CR 전/후 실측 대조** (`0ec14cb`, `1fbbd5e`) — 같은 raw 를 Step 0 부터 두 번
  처리. 측광값은 **1 mmag 이내 보존**(M13 +0.0002 · NGC6811 +0.0012), 진짜
  별(전프레임 검출)은 **1개씩만** 줄고, 1프레임 검출만 24~49% 사라진다.
  → **D-004 의 전제가 바뀌었다**(카드 머리에 경고 부착).
- **Johnson U 지원** (`f70cfde`) — `PARSEC_ISO_COL` 에 U 가 아예 없었다.
  UXmag(col 28) 추가, A0V/K형 색으로 검증.
- **U-B 로 축퇴 풀기 시도 → 실패** (`fc976b7`) — 나이 4.62→2.51, [M/H]
  −0.40→**−0.83**, E(B-V) 0.11→**0.33** 로 전부 악화. 원인은 Gaia→U 변환이
  `approx`·**σ=0.200 mag**(B 0.025·V 0.030 의 7~8배). **색을 늘려도 그 색의 영점이
  나쁘면 적합은 나빠진다** — 논문에 쓸 반례.
- **GUI Step 8 에 `Forced %` 컬럼** (`6897f3c`) — 강제 측광 비율을 모르면
  PSF/구경 비교를 오독한다(실제로 오진했다). 툴팁에 해석까지.

**진행 중 / 열린 것**

- **M45 광시야 EPSF** (2.0°×1.3°, 61.7 MP×20장) — 재보정~PSF 체인 실행 중.
  작업량이 M13 의 **22.3배**(픽셀 4배 × 프레임 1.33 × 소스 4.1)라 오래 걸린다.
  사용자 결정: 20장 그대로 완주.
- **GUI 실구동 사용감** — 화면 제어 승인 대기
- **Step 8 리포트가 헤드리스에서 안 나온다** — export 가 창(`_refresh_qc`)에
  묶여 있다. Step 10 은 워커에 있어서 헤드리스도 PNG 5종이 나온다. 이관하려면
  그림 생성 로직을 `apex/analysis` 로 빼야 한다.
- **아직 CR 없이 남은 성단**: M67 · M3(오염 15.4%) · M5. M13·NGC6811 은 재처리
  완료(기존은 `result_nocr/`·`sci_nocr/` 로 보존).
  → **사용자 결정 2026-07-30: CR 은 다른 세션에서 잡는다.**
  단 **CR 자체의 검증은 끝났다**(5축: 표준성·측광영향·별손상·효과·완전도).
  남은 것은 나머지 성단에 적용하는 일뿐이다. `docs/psf-contamination.md`
- **`keep_max` 는 정상이다** (2026-07-31 확인). 한때 「정렬 없이 자른다」고
  적었으나 **오독이었다** — 자르는 줄(`detection.py:629, 833`)만 보고 그 위의
  정렬을 놓쳤다. 두 경로 모두 `np.argsort(flux)[::-1]` 로 **밝기순 정렬이
  선행**한다(610행, 783행). 즉 어두운 별부터 버리는 예측 가능한 절단이고,
  완전도 모델이 다루는 바로 그 편향이다. M45 에서 10/19 프레임이 걸렸을 때
  y 분포가 정상이었던 것(0.05/0.54/0.96)도 우연이 아니라 설계대로였다.
- **위치기반(공간 가변) EPSF — 향후 구현.** 지금은 프레임당 EPSF 하나이고
  2.0°×1.3° 에서 가장자리 계통오차가 4.5 mmag 라 당장 필요하지 않다. 착수
  조건과 구현 방향은 `validation/psf_crossinstrument/REPORT_WIDE_EPSF.md`
  「향후 구현」절에 적었다 (grid_size 를 모델 단위로 승격 → `GriddedPSFModel`).
- **이소크론 축퇴 — 위 작업들 뒤로.** U-B 로는 실패했고(Gaia→U σ=0.200),
  U 표준등급(Landolt/Stetson) 확보가 선행 조건이다.

**2026-07-29 PSF 완성 세션에서 끝난 것**

- **PSF 헤드리스 완주 실증** — reprocess M13 혼잡장 **15/15 + signature** (8분17초,
  단일워커 환산 ~60초/프레임, M13급 1,400소스). PSF vs 구경: 13/15 프레임
  Δ=−0.075±0.008 · MAD 0.038 mag. 튀는 2프레임은 그 자리에서 분해 —
  `0002-R` 추적불량 이중피크(워커 자체 경고 = PSF QC 정상작동),
  `0004-R` **구경 apcorr 단독 이상**(1.73 vs 1.46, 같은 별 −0.22 mag; PSF 는 안정) → 칩 발행
- **LCO 교차기기 PSF 완주** — Sinistro CCD 4앰프(NGC5985 rp 120s) + QHY600 CMOS(Proxima
  V 20s), steps 1-7 → step 8 전 과정. `E:\APEX_validation\psf_crossinstrument\REPORT.md`
  + fig 2장. MAD 0.027 / 0.039 mag. **§3.12 "카메라 한 대" 한계 해소 재료.**
  Sinistro 에서 형태 컷(모라비안 튜닝)이 후보 1개로 붕괴 → 완화 로직이 59개 복구(안전망 실증)
- 위 테스트가 드러낸 **헤드리스 공백 3건 수정** (`15787a9`): ① `[wcs] engine` 키 신설
  (ASTAP 이 헤드리스에서 도달 불가였음) ② `run_wcs_solve` target_coord 를 config 에서 유도
  ③ EPSF 품질 경고(이중피크·비대칭)를 `residual_meta.epsf_reference.quality_*` 로 영속화
- **GUI 코드 감사** — 파라미터 저장·로드 왕복 16/16 무손실(실측), 캐시 시그니처에 psf 60키
  완비, QThread+워커패널+ETA+Stop 정상. `parameters_cmd.py` 77-374 죽은 리터럴 맵 → 칩

**진행 중 / 열린 것**

- **GUI 실구동 사용감**(속도·파라미터 조작감) — 화면 제어 승인 대기
- **광시야 모노 축** — 로컬에 없음 확정(DSY=동일 C3-61000+3947mm, M45 npy 는 출처불명 폐기).
  ZTF 공개 프레임(0.86°, 1"/px, ~38MB/장) 다운로드 승인 대기
- PSF `flux_scale_correction`(구경 앵커) 기본 off — 프레임별 정규화 오프셋(+0.05~+0.16)을
  흡수할지 검토 후보 (CMD 는 프레임 ZP 가 흡수하므로 필수는 아님)

## 사용자 의견

- **validation/ 20 GB 를 최종적으로 어디에 둘지** — E로 옮겨  
  `07-27 23:45` 대시보드 결정. 카드 원문: `Main/DECISIONS.md`
- **APEX 논문 §3 을 지금 쓸지, 재검출 결과를 기다릴지** — 논문써 근데 내가 fig들 엄청 지적했던거 알아?
  `2026-07-27 23:37` 대시보드 결정. 카드 원문: `Main/DECISIONS.md`
  → **§3 을 쓴다(A).** 다만 **그림 지적 이력을 먼저 확인하고 시작할 것** — 사용자가
  fig 들을 여러 번 지적했다. 무엇을 지적했는지 모르면 같은 그림으로 다시 올라간다.
  찾을 곳: `Main/harvest/apex/` (15건) · `TRACK_PAPER.md` · `validation/paper/captions/`.
  (이 항목은 8731 인스턴스가 낡은 코드라 자동으로 안 내려와서 손으로 넣었다 — F-018)

## 다음 3개

1. **헤드리스 wcs 스텝 버그** — `wcs_solve.py:4092` 가 `pixel_scale_arcsec=None` 이면
   `float(None)` 으로 죽는다. `getattr(P, "pixel_scale_arcsec", np.nan)` 은 키가
   **None 으로 존재하면** 기본값이 안 먹는다.
   고칠 것: `float(getattr(...) or np.nan)` 한 줄 + 회귀 테스트 1건.
   재현: `parameters.example.toml` 기반 config 로 `apex run --mode lc --steps 5`.
   (2026-07-28 M67 측정 때 이 버그로 step5 를 우회했다 — 「함정」 참조)
2. **4000프레임 재검출** — 원본 계수(`n_raw_detections`)가 이제 기록되므로
   재실행하면 실제 숫자가 남는다. `TRACK_PAPER.md` §3 의 사례 수를 채운다.
   ⚠️ 시작 전 `[parallel]`·`[wcs]` 의 `max_workers` 를 확인할 것 — 1 이면 안 끝난다(함정).
3. **남은 성단 CR 재처리** — M3(오염 15.4%, 가장 심함) · M67 · M5 가 아직 CR 없이
   만든 산출물이다. 방법은 M13·NGC6811 에서 확립됐다: `sci`·`result` 를
   `sci_nocr`·`result_nocr` 로 옮기고 → `_reprocess_step0.py` → `reorg_per_object` →
   steps 1-7 → step8 → step10 → `compare_cr_effect.py <target>`.
   ⚠️ reorg 는 `calibrated/` 에서 `sci/` 로 **이동**한다(복사 아님).

   (PSF 다중프레임 반복성은 M13 15장·NGC6811 21장·M67 LCO 10장으로 채워졌다 —
   M67 LCO 는 CR 켠 뒤 MAD 0.0074 · 프레임간 rms 0.045 로 최고값)
   그다음: `source_quality.py:115` All-NaN slice 경고 (테스트 4건)

## 측정 완료 — `n_det_frames` 분포 · 두 성단 (2026-07-28)

**데이터: M67 (Moravian C3-61000 · 2026-02-08 밤 · g′ 60 s × 10장 · `E:\APEX_validation\reprocess\M67\sci`)**
실행: `apex run --mode lc` steps 1–4 + 6 (헤드리스, `union_min_frames=1`).
검출 10/10 프레임 · 총 6,399 검출 · 중앙값 FWHM 7.85 px · union 마스터 **766 소스**.
산출물: `E:\APEX_validation_output\_ndet_m67g\` (config·log·`ref_catalog.tsv`).

| n_det_frames | 개수 | | min_frames | 남는 수 | 비율 |
|---:|---:|---|---:|---:|---:|
| 10 | **560** | | ≥1 | 766 | 100% |
| 9 | 18 | | ≥2 | 688 | 89.8% |
| 2–8 | 110 | | ≥3 | 669 | 87.3% |
| **1** | **78** | | ≥5 | 630 | 82.2% |

**분포는 쌍봉이다.** 73.1%가 10장 전부에서 검출(진짜 별), **10.2%가 딱 1장에서만**
검출(우주선·잡음 피크·문턱 근처 깜빡임), 중간(2–9장)은 얇다(bin당 ~1.7%).
중앙값 10 · 평균 8.29. → `ref_union_min_frames` 판단 재료가 준비됐다 (아래 D-004).

**전수 분류 (07-28, 78개 전부):** 각 소스를 10장 전부에서 잘라 「가장 밝은 프레임의
유의도」×「나머지 9장의 지속성」으로 나눴다 —
**확실한 우주선/핫픽셀류 34** (한 방 스파이크 + 나머지는 잡음뿐) ·
**확실한 문턱 별 17** (지속성 ≥3.0σ — 늘 희미하게 보임) ·
경계·애매 27 (그중 13은 25σ급 스파이크 동반이라 우주선 쪽에 가깝다).
⚠️ 판정선 민감: 지속성 2.5σ 로 낮추면 문턱 별이 30까지 늘어난다 — 경계가 연속이라
단정하지 않고 구간(17~30)으로 쓴다. `n1_classification.csv` · `n1_scatter.png`

### M3 추가 측정 — "한 시야로는 애매하다"는 지적의 답

사용자 지적(07-28): *"저 대상 하나만으로는 좀 에매하네"*. **맞았다.**
같은 코드(`classify_any.py`)로 M3 를 재 보니 숫자가 눈에 띄게 움직인다.

**데이터: M3 (Moravian C3-61000 · B · 60 s × 10장 · 2026-05-15 · `reprocess/M3/sci`)**
검출 10/10 · 6,172 검출 · FWHM 7.44 px · union 마스터 **874 소스**.

| | M67 · g′ (산개성단) | M3 · B (구상성단) |
|---|---:|---:|
| 마스터 소스 | 766 | 874 |
| 전 프레임 검출 | 560 (**73.1%**) | 311 (**35.6%**) |
| 1프레임 검출 | 78 (**10.2%**) | 128 (**14.6%**) |
| └ 우주선/핫픽셀류 | 34 | 44 |
| └ 문턱 별 (≥3.0σ) | 17 | 40 |
| └ 문턱 별 (≥2.5σ) | 30 | 69 |
| 컷2 잔존 | 688 (89.8%) | 746 (85.4%) |
| **컷2가 버리는 진짜 별** | **17~30 (2.2~3.9%)** | **40~69 (4.6~7.9%)** |
| 대조군 지속성(중앙) | 7.6σ | 6.1σ |

**읽는 법:** 쌍봉 자체는 두 시야 다 살아 있다(1과 최대값에 봉우리). 그러나
**골짜기 깊이가 다르다** — M67 은 중간 구간이 bin 당 ~1.7% 로 텅 비지만, M3 는
중간이 훨씬 차 있고 전 프레임 검출이 35.6% 밖에 안 된다. M3 가 더 붐비고(구상성단)
더 얕게 찍혔기 때문이다(대조군 지속성 7.6→6.1σ).
→ **컷 2 의 비용이 시야에 따라 2배 넘게 움직인다.** 한 시야로 숫자를 고정할 수 없다는
사용자 지적이 실측으로 확인됐다. 그림: `E:\APEX_validation_output\fig_two_targets.png`

## 사용자 판단 필요

> 형식: `### D-00N · 제목` + `무엇 / 지금 / 선택지 / 근거 / 권고 / 되돌리기`.
> 근거가 비면 결정이 아니라 **선행 작업**이다. 대시보드가 「결정 불가」로 분리한다.

### D-005 · 편의성 개선 계획(P1~P8) 2차 검토 — `docs/convenience-step0-multinight-plan.md`

- **무엇**: Step 0 분류·매칭 + 멀티나잇 개선 계획의 승인과 5개 세부 결정(Q1~Q5).
- **지금**: 계획 수립 완료, 구현 전 2차 검토 대기 (사용자 지정 절차).
- **선택지**: Q1 밤 경계 정오분할 vs 실제 일출·일몰 계산 / Q2 JD-gap 폴백 강등
  vs 병행 / Q3 JD 불명 프레임 기본 제외 vs 포함 / Q4 merged reference 모드
  opt-in vs 기본 / Q5 온도 허용오차 기본 1.0 °C.
- **근거·권고**: 계획 문서 말미에 정리 (권고: 정오분할·폴백 강등·제외·opt-in·1.0 °C).
- **되돌리기**: 문서만 있고 코드 변경 없음 — 계획 폐기로 충분.

### D-004 · 별 후보를 몇 장 이상에서 보여야 진짜 별로 인정할까 (`ref_union_min_frames`)

> ⚠️ **2026-07-30: 이 결정의 전제가 바뀌었다.** 우주선 제거가 Step 0 기본으로
> 켜졌다(`d9ad1ef`). 같은 raw 를 두 번 처리해 비교하니 1프레임 검출이
> **M13 233→176(−24%) · NGC6811 357→183(−49%)** 로 줄고, **전프레임 검출은
> 1개만** 줄었다(측광값은 1 mmag 이내 보존). 즉 아래 논의가 「1프레임 검출의
> 절반은 우주선」이라는 전제 위에 서 있었는데, 그 우주선이 이제 컷 이전에
> 걸러진다. 남은 176~183개는 진짜 문턱 별 비중이 높아졌으므로 **컷 2 의 비용이
> 커졌다** — 완화 또는 1 유지 쪽 논거가 강해졌다. 근거: `docs/psf-contamination.md` §5.

- 무엇: 같은 시야를 찍은 10장을 겹쳐 "별 후보 명단"(마스터)을 만들 때, **10장 중
  몇 장 이상에서 잡힌 것만 명단에 올릴지**. 현재 기본값 1 = 한 번만 보여도 올린다.
- 지금: M67 g′ 10장으로 실측했다. 후보 766개마다 "10장 중 몇 장에서 잡혔나"를 셌더니 —
  **560개(73%)는 10장 전부**에서 잡혔고 (진짜 별은 매 장 같은 자리에 있으니 당연),
  **78개(10%)는 딱 1장에서만** 나타나고 나머지 9장에선 안 보였다. 한 번 찍히고
  사라지는 건 대개 우주선 히트, 잡음이 우연히 별처럼 보인 것, 검출 문턱에 걸쳐
  깜빡이는 아주 어두운 놈이다. 중간(2~9장)은 얇다 — 즉 후보들이 "매번 보이는 놈"과
  "한 번만 보인 놈" **두 집단으로 갈라져 있다** (히스토그램에 봉우리가 둘 = 쌍봉).
- 선택지:
  **A. 2장 이상** — "두 번 이상 보인 것만 인정". 한 번만 보인 78개(10%)가 정확히 잘린다.
  **B. 3장 이상** — 97개(13%) 제거. 두 번 보인 경계 후보 19개가 더 빠진다.
  **C. 1 유지** — 아무것도 안 버린다. 어두운 진짜 별 17~30개를 지키는 대신 우주선류 34~47개도 명단에 남는다.
- 근거: 두 집단 사이 골짜기가 1과 2 사이에 있어서, 컷을 2에 두면 가짜 집단만 깨끗하게
  갈라진다 (분포가 밋밋했으면 컷이 임의 숫자가 됐을 것). 논문 인용 선례와도 맞다 —
  `stetson1994`(ALLFRAME)가 "상당 비율의 프레임에 나타나는 것만 인정"을 쓴다.
  **두 시야에서 확인했다 (M67 산개성단 g′ · M3 구상성단 B).** 쌍봉은 둘 다 유지되지만
  비용이 다르다 — 컷 2 가 버리는 진짜 별이 M67 은 2.2~3.9%, **M3 는 4.6~7.9%** 다.
  M3 가 더 붐비고 얕아서(전 프레임 검출 73%→36%, 대조군 지속성 7.6→6.1σ) 문턱 별이
  더 많이 생긴다. **즉 컷 값은 하나로 못 박고 시야 성격에 따라 달라져야 할 수 있다.**
- 실물 확인 (07-28, 질문 "우주선히트인지 뭔지 이미지로 보여줘야" 답변):
  무작위 표본(seed=0)으로 1장짜리 4개 + 10장짜리 2개를 골라 **같은 하늘 좌표를
  10장 전부에서 잘라** 비교했다. 그림: `E:\APEX_validation_output\_ndet_m67g\answer_cutouts.png`
  · **3개(ID 483·125·157)** — 딱 한 프레임에만 1~2픽셀짜리 날카로운 점(중심 유의도
    10~27σ), 나머지 9장은 배경뿐. 이 데이터의 별 상은 ~8px 뭉툭한 원반이라
    모양부터 다르다 → **우주선 히트/핫픽셀류. 하늘의 별이 아니다.**
  · **1개(ID 633)** — 모든 프레임에 3~6σ로 희미하게 **항상 보이는** 실제 별.
    검출 문턱에 걸쳐 공식 검출은 1번뿐이었던 것.
  · **전수 분류(78개 전부)로 표본 인상을 보정하면:** 우주선류 34~47 ·
    **진짜 문턱 별 17~30** (마스터 766개의 2.2~3.9%) · 나머지 애매.
    → **A안(컷 2)의 실제 비용 = 어두운 진짜 별 17~30개를 명단에서 뺀다.**
    기준 명단(마스터)의 목적이 "믿을 수 있는 앵커"라면 지불할 만한 비용이고,
    "하나라도 놓치기 싫다"면 C안 논리가 선다.
- **선택지를 다시 짠다 (07-28) — 사용자: *"뭐가 나은건지는 모르겠네"***
  못 고르는 게 당연했다. **두 비용을 맞바꾸라고 물었는데, 한쪽 비용은 없앨 수 있다.**
  C안의 유일한 실측 피해는 「진짜 별 6~12개가 붐빔으로 오판」이고, 그건
  `crowding_flag` 계산에서 1프레임 검출을 빼면 사라진다. `n_det_frames` 는
  1158줄에서 이미 설정되고 crowding 은 1379줄이라 **그 시점에 값이 있다.**
  KDTree 를 `n_det_frames >= 2` 로만 만들고 질의는 전체 행에 하면 된다
  (신뢰 행은 자기 자신이 잡히므로 두 번째 거리를 쓴다).
  ⚠️ `ref_master_union=False`(단일 앵커) 경로에는 `n_det_frames` 가 없으므로 가드 필요.
  **그러면 남는 질문은 하나뿐이다 — 이 마스터가 기계 밖으로 나가는가?**
  | | 어두운 진짜 별 | 붐빔 오판 | 명단 순도 |
  |---|---|---|---|
  | A (컷 2) | **17~69개 삭제** | 없음 | 가짜 0 |
  | C (현행 1) + crowding 수정 | 전부 보존 | **없음** | 가짜 10~15% 남음 |
  · **내부 측광 기준으로만 쓴다** → C + 수정. 잃는 게 없다. **게다가 C 는 현재 기본값이라
    설정을 안 바꿔도 된다** — 결정 없이 버그 하나만 고치면 끝난다.
  · ~~논문 카탈로그·외부 공개로도 낸다 → A~~ **철회 (07-28).** 사용자: *"논문 카탈로그라는건
    뭔소리야 카탈로그를 왜내"*. 확인하니 원고의 Data availability 는 **소스코드(MIT) +
    `run_all.py` 그림 재생성 + Pan-STARRS 교차매칭 캐시**뿐이다. 별 카탈로그 공개도
    VizieR/CDS 등재도 없다. 검증 논문이지 카탈로그 논문이 아니다.
    → **내가 A 를 살리려고 없는 시나리오를 만든 것이다.** A 의 남은 근거가 없다.
- **결정됨 (07-28) — 기본값은 1 유지, `crowding_flag` 오염만 고쳤다.**
  A 의 유일한 근거였던 "공개 카탈로그 순도" 가 사실이 아니었으므로(위 철회) 컷을 올릴
  이유가 없다. 마스터는 파이프라인 내부용이고, 가짜는 하류(비교성 선정 QC·깊이 QC·
  구경보정 SNR≥40)에서 이미 걸러진다. 유일하게 안 걸러지던 `crowding_flag` 오염을
  코드로 막았다.
  **수정:** `refbuild.py` 의 이웃 계산을 `neighbor_distances()` 로 분리하고,
  KDTree 를 `n_det_frames >= 2` 행으로만 만든다(질의는 전 행). 회귀 검사 6건
  (`tests/test_refbuild_neighbor_trust.py`).
  **실데이터 확인 (M67 g′ · M3 B):**
  | | 수정 전 | 수정 후 |
  |---|---:|---:|
  | 1프레임 소스가 유발한 붐빔 | M67 3 · M3 13 | **0 · 0** (구조상 불가능) |
  | 붐빔 표시된 진짜 별 | M67 38 · M3 238 | 35 · 225 |
  ⚠️ **부작용을 재서 적는다.** `n_det_frames` 가 여기서 쓸 수 있는 유일한 신호라
  확실한 우주선뿐 아니라 **문턱 별도 트리에서 빠진다.** 꺼진 플래그의 정체:
  M67 3개는 전부 우주선 원인(순이득), **M3 13개는 우주선 6 · 문턱 별 6 · 애매 1**.
  그 6개는 진짜 별인데 이웃으로 안 세어진다 — 검출한계 소스가 이웃 조리개에 주는
  플럭스가 작아 받아들일 만하지만 **공짜가 아니라 맞바꾼 것**이다.
  남은 여지: 공개 카탈로그를 내기로 하면 그때 A 를 다시 검토한다. 지금은 계획이 없다.
- 되돌리기: 설정 한 줄이라 언제든 바꿀 수 있다. 명단 재생성은 1분이 안 걸린다
  (이번 실측: 검출 50초 + 병합 4초).
## 함정

- **`validation/` 은 E 드라이브 정션이다.** 그 아래 스크립트에서
  `Path(__file__).resolve()` 를 쓰면 링크를 따라가 레포 밖(`E:\`)으로 나가서
  `apex` 를 못 찾는다. `absolute()` 를 쓸 것 (2026-07-30).
- **PSF/구경 플럭스비는 검출된 별로만 재라.** 강제 측광 위치(`forced_flag=True`)는
  구경이 거의 0 을 재므로 비율이 10~68 로 폭주한다. 섞으면 강제 비율이 높은
  자료가 「PSF 이상」으로 보인다 — 실제로 NGC6811 을 그렇게 오진했다.
  강제 비율은 필터·노출에 크게 좌우된다(같은 밤 B 74% vs R 49%).
- **재처리 후 산출물을 비교할 때 `master_id` 로 조인하지 마라.** 마스터가 다시
  만들어져 ID 가 재할당된다(M13 1574→1501). 하늘 좌표로 짝지어야 한다 —
  ID 조인은 MAD 1.24 mag, 좌표 매칭은 0.0038 mag.
- **로컬 astrometry.net 인덱스는 남천을 안 덮는다** (2mass-04 시리즈 48청크 중 31개).
  남천 대상(예: dec −62° Proxima 필드)은 blind 가 영원히 안 풀린다 — `[wcs] engine = "astap"`
  으로 지정할 것 (D50 은 전천 1,476파일). QHY600 교차기기 검증 때 발견 (2026-07-29).
- **`validation/` 은 커밋하지 않는다.** 20,088 MB / 10,990 파일. `.gitignore`에 있음.
  논문 자산(`validation/paper/figures`, `captions`, `논문작업`, `fig*.py`)만 추적한다.
- **실행은 `run.bat` 또는 `.venv-deploy\Scripts\python`.** 시스템 python 아님.
- **`step10_zeropoint_calibration.py:2199, 2804`** — pandas `DataFrameGroupBy.apply` 폐기 예정.
  pandas를 올리면 **영점보정 결과가 조용히 달라질 수 있다.** 올리기 전에 회귀 스냅샷을 뜰 것.
- 초과검출 게이트 임계는 실측 산포(실프레임 194장/24그룹, 최대 1.53배, 99분위 1.49)에서
  나왔다. REVIEW 1.8 / FAIL 2.5. **임의 숫자가 아니므로 함부로 바꾸지 말 것.**
- `n_sources`가 아니라 `n_raw_detections`를 봐야 한다. 전자는 필터·상한 뒤 값이라
  과검출이 정상처럼 보인다.
- **`max_workers = 1` 이면 플레이트솔빙이 사실상 안 끝난다.** 프레임당 80~290초라
  30장이면 **95분**이다. M3 (`E:\APEX_validation\reprocess\M3\parameters.toml`) 가
  `[parallel]`·`[wcs]` 둘 다 1 이어서, 2026-07-11 실행이 3번째 프레임에서 중단됐고
  그 결과 M3 는 30장 중 **2장만** WCS 를 갖고 있다 (`0001-B`, `0001-R`).
  같은 날 M67 은 `workers=12` 로 30/30 을 10분에 끝냈다 — **설정 차이 하나뿐**이다.
  판별법: `result/cache/astnet_solve.log` 의 헤더 줄에서 `workers=` 를 본다.
  로그가 결과 줄 없이 끊겨 있으면 실패가 아니라 **중단**이다.
  (2026-07-28 워커 8로 재실행 → 프레임당 79초, 정상 완주)
- **M3 설정의 `target.name` 이 `NGC6811` 로 잘못 적혀 있다.** 좌표(205.548, 28.377)는
  M3 가 맞다. 이름만 다른 설정에서 복사된 흔적이라 산출물 라벨이 틀릴 수 있다.
- **헤드리스 wcs 스텝은 `pixel_scale_arcsec` 미설정이면 죽는다** (`wcs_solve.py:4092`).
  `getattr(P, "pixel_scale_arcsec", nan)` 은 키가 None 으로 존재하면 기본값이 안 먹는다.
  프레임에 이미 WCS 가 있으면 step5 를 건너뛰어도 된다 — refbuild 는 헤더에서 직접 읽는다
  (`analysis/refbuild.py:391`). 단 step6 의 `required_inputs` 가 `step5_wcs` **폴더 존재**를
  요구하므로 폴더만 만들어 주면 된다 (2026-07-28 측정 때 그렇게 했고, 폴더 안에
  README_WHY_EMPTY.txt 로 사유를 남겼다).

## 최근 세션 원문

`C:\Users\bmffr\Desktop\Main\harvest\apex\` (15건, 2026-05~07)
