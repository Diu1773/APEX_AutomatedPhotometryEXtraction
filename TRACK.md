# TRACK: apex-dev — APEX 소프트웨어 v1.0 릴리즈

> 이 파일이 이 트랙 상태의 **유일한 진실**이다. 세션을 열면 여기부터 읽는다.
> 세션을 끝내기 전에 `## 지금` / `## 다음 3개` / `## 함정`을 반드시 다시 쓴다.
> 논문 트랙은 별도: [TRACK_PAPER.md](TRACK_PAPER.md)

## 완료 정의

측광 파이프라인이 실관측 데이터에서 GUI·헤드리스 양쪽으로 완주하고,
회귀 테스트 전량 통과 상태로 v1.0 릴리즈 태그가 붙는다.

## 오라클 — 이 트랙의 검증 명령

```bash
.venv-deploy/Scripts/python.exe -m pytest tests/ -q
```

- **통과 기준:** 614 passed, 0 failed
- **소요:** 약 7분
- **마지막 실행:** 2026-07-27 — 614 passed, 6 warnings
- CI: `.github/workflows/tests.yml` 외 3개

## 지금

- 마지막 커밋: `95af5b2` 2026-07-27 — docs(harness): add harness protocol section to AGENTS.md
- 하네스 설치됨 (2026-07-27): AGENTS.md에 세션 종료 프로토콜, Stop hook이 이 파일 갱신을 검사
- 미푸쉬: 0 · 미커밋: 0 (2026-07-27 하네스 구축 세션에서 정리)
- 진행 중: 프레임별 예측 검출한계 QC 게이트 완성됨. 원고 §3 반영 대기
- 막힌 것: **`n_det_frames` 측정이 입력 데이터에서 막혔다** (D-003). 코드는 다 파악됐고
  1회 실행이면 되는데 다중 프레임 그룹의 FITS 경로를 모른다.
- 2026-07-27 21:xx 야간 배치(첫 실행, 계정 `max`) — 코드 변경 없음.
  「다음 3개」 1번을 실행 가능한 형태로 좁히고 D-003 을 올렸다. 오라클 재실행 안 함(변경 없음).
  같은 세션 앞에서 오라클을 한 번 돌렸고 **614 passed / 7분 41초** 통과.

## 사용자 의견

## 다음 3개

1. **`n_det_frames` 분포 측정** — `ref_union_min_frames` 판단의 선행 작업.
   **입력이 정해졌다. 지금 바로 실행 가능하다** (D-003 해소, 2026-07-27 22:31).

   ```
   E:\APEX_validation\reprocess\M67\sci     pp_Messier67-{0001..0010}-{g,r,i}.fit
   ```
   실측 확인: g 10 · r 10 · i 10 = 30장. **g 10장으로 돌린다** (한 필터가 한 그룹).
   같은 위치의 다른 성단은 BVR 이다 — M3 30 · NGC6811 21 · M13 15 · M5 0.
   BVR 로 하려면 M3(10장)가 가장 크다.

   **왜 성단인가:** `n_det_frames` 분포는 시야 별밀도에 좌우된다. 이 임계는 성단 관측에
   쓰이므로 성단 데이터로 정해야 한다. (은하 M60 안을 쓰지 않기로 한 이유)

   2026-07-27 코드 확인으로 좁혀진 것:
   - `refbuild.py:1158` 이 `n_det_frames` 를 **필터 전에** 계산하고,
     `1164` 가 `>= ref_union_min_frames` 로 거른다.
     → **`ref_union_min_frames=1` 로 한 번만 돌리면 N=1/2/3/5 분포가 전부 나온다.**
       4번 돌릴 필요 없다. 마스터 카탈로그의 `n_det_frames` 열을 세기만 하면 된다.
   - 전제: **한 그룹에 프레임이 여러 장**이어야 한다. union 이 프레임을 가로질러 병합한다.
   - 기존 산출물로는 못 잰다 — `validation/real_gui_run/{M13,M36}_g_cluster` 는
     `step4_detection` 만 있고 그것도 프레임 1장(`detect_cg.fits.csv`)이다.
     step6 ref build 출력이 어디에도 없다.

   측정할 것: N=1/2/3/5 각각의 마스터 별 수, 그중 1프레임 검출만인 비율
2. 4000프레임 재검출 — 원본 계수(`n_raw_detections`)가 이제 기록되므로 재실행하면 실제 숫자가 남음
3. `source_quality.py:115` All-NaN slice 경고 처리 — roundness 계산에서 발생 (테스트 4건)

## 사용자 판단 필요

> 형식: `### D-00N · 제목` + `무엇 / 지금 / 선택지 / 근거 / 권고 / 되돌리기`.
> 근거가 비면 결정이 아니라 **선행 작업**이다. 대시보드가 「결정 불가」로 분리한다.

### D-001 · validation/ 20 GB 를 최종적으로 어디에 둘지

- 무엇: APEX 검증 실행 산출물. 현재 `.gitignore` 로 git 에서 빠졌고 로컬 디스크에만 있다.
- 지금: `C:` 에 20,088 MB / 10,990 파일. `C:` 여유는 43 GB.
  내역 — `real_gui_run` 19,259 MB · `paper/` 660 MB · `reports/` 113 MB · `psf_archive/` 33 MB
- 선택지:
  **A. 현행 유지** — 비용 0. `C:` 여유 43 GB 가 계속 얇고 백업이 없다.
  **B. `E:` 로 이동** — `E:` 여유 205 GB, 관측 실데이터가 이미 거기 있어 성격이 맞다. 경로 참조 수정 필요.
  **C. 논문 근거만 남기고 삭제** — `C:` 19 GB 회수. 재생성에 GUI 전체 재실행이 필요하다.
- 근거: 19 GB 중 대부분이 `real_gui_run`(GUI 실행 산출물)이고 논문에 직접 인용되지 않는다.
  논문 자산은 `paper/figures` 49개 + `논문작업` 25개 = 약 23 MB 이고 이미 git 이 추적 중이다.
- 권고: **B**. 삭제(C)는 되돌릴 수 없고, 현행(A)은 `C:` 43 GB 로 버티는 게 불안하다.
  `E:` 는 이미 관측데이터용이라 성격이 맞는다.
- 되돌리기: 폴더 이동이라 되돌리기 쉽다. 경로를 쓰는 코드가 있으면 그것만 수정.

### D-002 · APEX 논문 §3 을 지금 쓸지, 재검출 결과를 기다릴지

- 무엇: 원고 §3(운영 QC 절)에 완전도 QC + 검출수 게이트를 서술하는 작업.
- 지금: 그림·캡션·문헌(43건)·실증 수치가 전부 준비됨. **§3 본문만 비어 있다.**
- 선택지:
  **A. 지금 쓴다** — 재료가 가장 잘 갖춰진 시점. 4000프레임 재검출 결과가 나오면 수치만 갱신.
  **B. 재검출 후 쓴다** — 한 번에 확정 수치로. 다만 재검출 일정이 미정이다.
- 근거: 게이트 임계(REVIEW 1.8 / FAIL 2.5)는 실프레임 194장/24그룹 실측 산포에서 나왔고
  이미 고정됐다. 재검출은 그 임계를 바꾸지 않고 사례 숫자만 더한다.
- 권고: **A**. 임계가 이미 확정돼 §3 구조가 재검출 결과에 좌우되지 않는다.
- 되돌리기: 원고 문단이라 언제든 수정 가능.

## 함정

- **`validation/` 은 커밋하지 않는다.** 20,088 MB / 10,990 파일. `.gitignore`에 있음.
  논문 자산(`validation/paper/figures`, `captions`, `논문작업`, `fig*.py`)만 추적한다.
- **실행은 `run.bat` 또는 `.venv-deploy\Scripts\python`.** 시스템 python 아님.
- **`step10_zeropoint_calibration.py:2199, 2804`** — pandas `DataFrameGroupBy.apply` 폐기 예정.
  pandas를 올리면 **영점보정 결과가 조용히 달라질 수 있다.** 올리기 전에 회귀 스냅샷을 뜰 것.
- 초과검출 게이트 임계는 실측 산포(실프레임 194장/24그룹, 최대 1.53배, 99분위 1.49)에서
  나왔다. REVIEW 1.8 / FAIL 2.5. **임의 숫자가 아니므로 함부로 바꾸지 말 것.**
- `n_sources`가 아니라 `n_raw_detections`를 봐야 한다. 전자는 필터·상한 뒤 값이라
  과검출이 정상처럼 보인다.

## 최근 세션 원문

`C:\Users\bmffr\Desktop\Main\harvest\apex\` (15건, 2026-05~07)
