# Phase 1 before 수치 — 2026-08-07 야간 배치

계측: `apex bench` (`8bf6d51`), psutil 폴링 0.1 s, 자식 프로세스 합산.
환경: Python 3.12.3 · 논리코어 16 · numpy 2.4.4 (각 JSON 의 `env` 블록이 정본).

## B1 — Step 0 peak RSS vs 프레임 수 (M67 raw 부분집합, in-process)

| lights | wall (s) | peak RSS (MB) |
|---:|---:|---:|
| 5 | 259.7 | 5,041 |
| 10 | 337.6 | 5,161 |
| 20 | 516.4 | 5,263 |
| 30 | 643.3 | 5,204 |

**가설이 뒤집혔다.** "메모리가 light 프레임 수에 비례한다"는 계획의 전제였는데,
실측 peak RSS 는 **5.0–5.26 GB 로 평평**하다. light 는 이미 한 장씩 처리되고
있고, 지배 항은 **보정 풀 로딩** — 매 실행이 bias 102 장·dark 196 장·flat 18 장을
스캔해 마스터를 만들며, 그 결합 입력(한 그룹의 전체 프레임 리스트)이 통째로
메모리에 올라간다. 첫 재처리에서 봤던 1.71 GiB 할당 실패(sigmaclip 작업 배열)도
같은 계열이다.

wall time 은 lights 에 선형: **약 187 s 고정(스캔+마스터) + 15.4 s/frame**.

**O1 streaming 의 표적이 바뀐다** — light 경로가 아니라 **마스터 결합의 입력
경로**다. 그룹 프레임을 전량 로드하는 대신 행 밴드 단위로 파일에서 직접 읽어
누적하면(PTC 의 `HDU.section` 기법과 동일 계열) peak 를 수백 MB 로 묶을 수 있다.
결합 연산 자체는 이미 밴드 단위(`_COMBINE_CHUNK_BYTES`)라 산술은 그대로다.

**O1 판정선(before 확정)**: peak RSS 5.0–5.3 GB → 목표 < 1 GB, calibrated
프레임·마스터 비트동일.

한계: n ≤ 30, 보정 풀 고정(M67 fixture). light 수백 장 규모에서 다른 항이
나타날 가능성은 clean run 계측이 확인한다.

## B2 — worker 훑기 (NGC 6811, steps 1–7, 21 프레임)

| workers | 전체 wall | peak RSS(자식 합산) | n_detect 합 |
|---:|---:|---:|---:|
| 1 | **439.6 s** | 3,792 MB | 21,211 |
| 2 | 934.9 s | 4,882 MB | 21,211 |
| 4 | 1,161.3 s | 6,645 MB | 21,209 |
| 8 | 1,162.4 s | 6,876 MB | 21,207 |
| 12 | 1,067.3 s | 6,982 MB | 21,210 |
| 16 | 1,045.8 s | 6,714 MB | 21,207 |

**w=1 이 가장 빠르다 — 병렬이 2.4× 느리다.** 단계별로 쪼개면 원흉이 하나다.

| 단계 | w=1 | w=2 | w=4 | w=8 | w=12 | w=16 |
|---|---:|---:|---:|---:|---:|---:|
| 4 detect | 91.8 | **41.9** | 45.2 | 53.1 | 57.6 | 51.3 |
| 5 wcs | 106.5 | **39.0** | 42.6 | 40.9 | 42.1 | 42.0 |
| 7 forcedphot | **222.6** | 836.2 | 1,055.2 | 1,052.0 | 950.5 | 935.7 |

- detect·wcs 는 병렬로 2–2.7× 벌고 **w=2 에서 포화**한다
- **강제측광은 어떤 병렬에서도 4× 안팎 느려진다** (222.6 → 836–1,055 s).
  production 기본값 12 worker 는 이 기계에서 이 단계를 **serial 보다 4.3×
  느리게** 돌리고 있었다. peak RSS 도 worker 와 함께 3.8→7.0 GB 로 커진다.
- CLAUDE.md 의 "numpy/C 연산이 GIL 을 놓으므로 스레드가 돕는다"는 서술은
  step7 에 대해 **실측으로 반증**됐다. 기전(GIL 경합·메모리 대역폭·E: 동시
  읽기) 규명은 별도 추적.

**O2 함의**: 단일 worker 수가 아니라 **단계별 worker** 가 맞다 —
detect/wcs ≈ 2–4, forcedphot = 1 (병렬 경로가 고쳐지기 전까지).

## B4 — 재현성 (같은 입력, w=12, 3회)

step7 표 SHA-256 은 세 실행이 모두 다르다 — **비트 재현은 아니다.**
`source_id` 매칭으로 정량화하면 (r0 vs r1, 39,868 측정):

| 지표 | 값 |
|---|---|
| mag_inst 이 조금이라도 다른 측정 | 1,025 (2.57 %) |
| 바뀐 것들의 중앙 |Δ| | 0.016 mmag |
| 최대 |Δ| | 15.9 mmag |
| 전체 중앙 MAD | **0.0000 mmag** |
| 좌표가 다른 행 | 1,107 (최대 **1.51 px**) |
| 마스터 목록 크기 | 2,000 vs 2,001 (±1 별) |
| n_detect 합 | 21,208 로 3회 동일 |

w=1 vs w=12 매칭 대조도 같은 규모다(바뀐 측정 6.5 %, 중앙 |Δ| 0.001 mmag,
최대 7.7 mmag). 즉 **통계적으로는 재현되지만**(중앙 MAD 0) 소수 별의 좌표가
1.5 px 까지 튀며 꼬리를 만든다.

### 근원 규명 — SEP 의 블렌드 디블렌딩 (2026-08-07)

단계별 digest 로 좁히면 **4 단계 검출에서 이미 갈라진다**(21 프레임 중 9).
`NGC6811-0005-V` 를 세 실행에서 대조한 결과:

| 지표 | r0 | r1 | r2 |
|---|---:|---:|---:|
| `n_raw_detections` | 1,323 | 1,323 | 1,323 |
| `bkg_median`·`fwhm_px` | 동일 | 동일 | 동일 |
| `n_after_shape_filter` | 1,207 | **1,206** | 1,207 |

**SEP 의 검출 자체(개수·배경·FWHM)는 결정적**이고, 모양 필터 코드도
설정 임계 + 마스크라 결정적이다. 그런데 공통 검출원의 값을 비교하면:

| 열 | 결과 |
|---|---|
| `x`·`y`·`peak_adu`·`fwhm_px` | **비트동일** |
| `elongation`·`dao_flux`·`sharpness` | **32 행이 다름** (최대 Δflux 2,594) |

그 32 개는 **전부 `sep_flag = 1`(블렌드 병합)** 이고 — 프레임의 flag=1 인 36 개 중
32 개 — 밝고(중앙 flux 212 k vs 전체 24 k) 붐비는(최근접 14.9 px vs 53 px) 별이다.
그리고 **r0 과 r2 는 완전히 일치**하며 r1 만 어긋난다(간헐적).

**결론: 비결정성의 근원은 APEX 코드가 아니라 SEP 의 디블렌딩이다.** 위치·peak·
FWHM 은 정확하고, 병합된 블렌드의 플럭스·모양만 흔들린다. 이것이 모양 필터
경계선의 별 하나를 뒤집고(1,207↔1,206), 마스터 목록 ±1 과 다운스트림 1.5 px
좌표 차이로 전파된다. 시드 없는 RANSAC 가설은 **반증** — 내장 솔버의 RANSAC 은
정렬된 후보를 순회하는 결정적 구현이고 난수를 쓰지 않는다.

**논문 함의**: 재현성 서술은 "비트 재현"이 아니라 정확히 이렇게 써야 한다 —
*검출·측성·측광은 블렌드로 표시된 소수 천체(약 2.7 %)를 제외하면 비트 재현되며,
그 예외는 외부 검출 라이브러리(SEP)의 디블렌딩에서 비롯한다. 전파된 영향은
매칭 등급 중앙 MAD 0.0 mmag, 꼬리 최대 15.9 mmag 다.*

**parity 게이트 함의**: step 4 이후 산출물에 비트동일(digest) 게이트는
쓸 수 없다. 실측 잡음 바닥에 근거한 통계 게이트로 바꾼다 —
매칭 mag 중앙 MAD < 0.1 mmag · 바뀐 측정 비율 < 5 % · 최대 |Δ| < 30 mmag ·
n_detect ±5 · 목록 크기 ±2. 마스터(step 0)는 단일 스레드 결합이므로
비트동일 게이트를 유지한다.

## 실행 사고 기록

1차 배치의 B2/B4 는 여섯 값 전부 `Config not found` 로 즉사 — 트리 승격 때
`cp … 2>/dev/null` 이 조용히 실패했다(clean run 설정은 TOML 이 아니라 JSON
정본). **오류를 버리는 복사는 실패도 버린다.** 2차 시도는 하네스 밖 `&`
분리 실행이라 부모 정리 때 함께 죽었다 — Windows 에서 긴 작업은 하네스
추적(run_in_background)으로만 건다. 3차(정본)가 위 결과다.


---

# O1 — Streaming 마스터 결합 (2026-08-08)

`combine_frames_streaming()`: 프레임 전량을 메모리에 올린 뒤 결합하는 대신,
열어 둔 파일 핸들에서 **행 밴드만 읽어 누적**한다. flat 은 정규화 median 이
전역 통계라 2-pass(1패스에서 프레임 하나씩 median, 2패스에서 밴드 결합).
오버스캔은 열 방향 처리라 켜져 있으면 기존 경로로 빠진다(`can_stream_combine`).

## 지표를 잘못 재고 있었다

첫 측정은 "메모리 2.4배 감소, 시간 23 % 악화"로 읽혔고, 밴드를 줄이면
**메모리가 오히려 늘고 시간이 줄어드는**(128 MB: 3,302 MB·119 s vs
384 MB: 2,653 MB·216 s) 거꾸로 된 결과가 나왔다.

원인은 **Windows 의 RSS 가 메모리 매핑된 파일의 상주 페이지를 포함**한다는
것이다. 스트리밍은 입력을 mmap 하므로 OS 페이지 캐시가 RSS 로 잡히고, 그
페이지는 압박이 오면 OS 가 그냥 버린다 — OOM 위험과 무관하다. 실제 압박을
재려면 공유·파일 기반 페이지를 제외한 **USS** 를 봐야 한다.

`resources.measure()` 에 `peak_uss_mb` 를 추가하고 다시 쟀다.

## 결과 (bias 100 장 마스터 결합)

| 경로 | RSS | **USS** | wall |
|---|---:|---:|---:|
| 기존 (전량 로드) | 5,617 MB | **5,571 MB** | 274.8 s |
| 스트리밍 | 3,565 MB | **550 MB** | **142.6 s** |

**USS 10.1 배 감소, 동시에 1.9 배 빠르다.** 판정선(<1 GB)도 충족한다.
느려 보였던 것은 지표 착오였다 — 기존 경로는 5.6 GB 를 실제로 할당하고
스트리밍은 550 MB 만 쓴다.

## parity

| 검사 | 결과 |
|---|---|
| bias 12 장 median | **비트동일** (최대 Δ 0.000e+00) |
| dark 8 장 (bias 차감) | **비트동일** |
| mean · sigmaclip_mean | **비트동일** |
| 밴드 크기 불변성(8 MB vs 1 KB) | 동일 — 밴드는 메모리 손잡이일 뿐 결과 손잡이가 아니다 |
| 전체 오라클 | 918 passed |

회귀 테스트 `tests/test_calibration_streaming.py` 7 건 신설.

## 교훈

B1 의 "프레임 수 비례" 가설에 이어 **두 번째로 측정이 예상을 뒤집었다.**
이번에는 측정 도구 자체가 틀렸고, 그것을 잡아낸 것은 "밴드를 줄였는데
메모리가 늘었다"는 **설명되지 않는 수치**였다. 설명 안 되는 수치를 그 자리에서
추적하지 않았다면 O1 을 "시간을 내주고 메모리를 얻은 절충"으로 잘못 기록했을
것이다.


## O1 게이트 판정 — 통과

전체 Step 0 경로(M67 30 lights, 보정 풀 포함)를 스트리밍으로 돌리고
보존된 baseline(`E:/APEX_validation/reprocess/M67/sci`)과 화소 비교:

**calibrated 프레임 30/30 비트동일, 최대 |Δ| = 0.**

마스터 결합만이 아니라 **과학 산출물 전체가 바이트 단위로 같다.** 게이트의
"step 0 산출물은 비트동일" 조건을 충족하므로 O1 을 채택한다.

---

# O2 — 단계별 worker 상한 (2026-08-08)

B2 가 이미 근거를 냈다. 단일 worker 수 하나로 파이프라인 전체를 돌리는 것이
문제였다.

| 단계 | 1 worker | 최선 병렬 | 판정 |
|---|---:|---:|---|
| detect | 91.8 s | 41.9 s @ 2 | 이득, 2 에서 포화 |
| wcs | 106.5 s | 39.0 s @ 2 | 이득, 2 에서 포화 |
| forcedphot | **222.6 s** | 935.7 s @ 16 | **모든 병렬에서 4× 느림** |

`get_parallel_workers(params, stage=...)` 에 측정 기반 상한을 넣었다 —
`{detect: 4, wcs: 4, crop: 4, forcedphot: 1}`.

- `max_workers` 는 이름 그대로 *최대*이므로 단계 상한을 그 아래로 두는 것이
  설정의 의미와 일치한다. 사용자가 12 를 넣어도 forcedphot 은 1 로 간다
- **`APEX_MAX_WORKERS` 는 예외** — 스윕이 상한 위를 탐침할 수 있어야 그
  상한을 다시 도출할 수 있다
- 강제측광이 병렬에서 느린 기전(GIL 교대·메모리 대역폭·같은 볼륨 동시 읽기)은
  별건으로 추적한다. 현재는 "직렬이 실측 최적"이 채택 근거다

회귀 테스트 `tests/test_stage_workers.py` 7 건. 전체 오라클 933 passed.
