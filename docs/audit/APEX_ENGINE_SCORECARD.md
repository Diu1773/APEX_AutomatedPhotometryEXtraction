# Native 엔진 성적표 — 좋은가 · 목적에 맞는가 · 범용적인가

작성 2026-08-10. `APEX_NATIVE_PROVENANCE.md` 는 **왜 만들었나**(동기),
`APEX_PRIOR_ART.md` 는 **대안이 있는가**(선행연구)를 답한다. 이 문서는 남은
질문에 답한다 — **"감사했다"가 아니라 "얼마나 좋고, 목적에 맞고, 범용적인가".**

세 축을 따로 채점한다. 셋은 독립이며, 하나가 좋다고 나머지가 따라오지 않는다.

| 축 | 묻는 것 | 증거의 형태 |
|---|---|---|
| **A. 얼마나 좋은가** | 기존 엔진과 **실측 대조**했는가 | 같은 입력·같은 지표의 수치 |
| **B. 목적성** | 표방한 용도(소·중형 천문대, 오프라인, 통합)에 맞는가 | 요구가 실재함을 보인 근거 |
| **C. 범용성** | 이 카메라·이 성단·이 밴드 밖에서도 성립하는가 | **다른 기기·다른 밴드**의 실측 |

---

## 성적표

| 엔진 | A. 대조 | B. 목적성 | C. 범용성 |
|---|---|---|---|
| **Step 0 검출기 보정** | **○ ccdproc 비트동일 · IRAF · BANZAI** | ○ raw→science 통합 | **○ 3 기기** (Moravian C3-61000 · QHY600 CMOS · Sinistro CCD 4-amp) |
| 내장 WCS 솔버 | **○ ASTAP·astrometry.net 실측 대조** — M13 8 프레임에서 잔차 0.346 vs 0.493 px, 8/8 우위. 단 속도는 ASTAP 가 3.6 배 빠르고 매칭 수 상한 교란 미해결 (`validation/wcs_engines/`) | ○ 오프라인(AIJ 는 웹, ASTAP·anet 은 인덱스 DB 필요) | **✕ 1 기기·1 화소축척(0.395″/px)·1 시야** |
| 강제측광 오케스트레이션 | ✕ 커널은 photutils(설계상 대조 대상 아님). **재중심·포화플래그·깊이 QC 는 무대조** | ○ | **✕ 1 기기** |
| **PSF 측광 (step8)** | **○ IRAF DAOPHOT ALLSTAR · photutils 실측 대조 — 같은 정책으로 맞추면 ALLSTAR 와 구분되지 않는다.** 인공별 400 개(진리값 존재), 사후 게이트를 모든 엔진에서 끄고 채점: **완전도 APEX 0.98 vs ALLSTAR 0.90 vs photutils 0.85**, 같은 별 산포 0.056 vs **0.051** vs 0.078, **최혼잡 산포 APEX 0.073** vs 0.083 vs 0.091. 짝지은 검정에서 ALLSTAR 와의 차이는 전 구간 유의하지 않고(전체 +0.8 mmag [−3.0, +4.1]), photutils 대비는 −10.9 mmag (p=1.4e−07) 로 APEX 우세. **단 속도는 photutils 가 6 배 빠르다**(~30 s vs ~190 s). (2026-08-14 이전 "APEX 완전도 0.68" 은 APEX 게이트만 켠 채 비교한 것이었다.) 파라미터 전표 `PARAMETERS.md` | **○ 정밀도↔완전도 다이얼** — 사후 게이트로 완전도 0.98↔0.68 을 사용자가 고른다. 모형 방식(ePSF·Moffat·하이브리드)·보간 차수·그룹 예산·프로파일 오차도 파라미터 | **✕ 1 기기·1 프레임** |
| 프레임 QC 게이트 | **✕ 문헌 선례 대조 없음** (`APEX_PRIOR_ART.md` §5-3) | ○ | **✕ 임계값이 한 자료에서 조정됨** |
| 구경보정 워크플로 | ✕ 무대조 | ○ | ✕ 1 기기 |
| PTC 검출기 특성화 | △ 방법은 Janesick 표준·LSST `cp_pipe` 존재, 무대조. **결과(EGAIN 14 배 오류)는 독립 가치** | ○ | **방법은 범용, 결과는 정의상 1 기기** |
| 영점·색항 (step10) | **○ PS1 DR2 직접 측광 대조** — M67 968 별. 엔진은 참조를 ≤6 mmag 로 전달하고, 계통오차는 Jordi+2010 앵커 자체에서 온다(g +45 · i −31 mmag). PS1 도 SDSS 와 동일계가 아니므로 "두 참조 선택지 간 차이의 실측" (`validation/paper/data/zp_anchor_ps1_m67.json`) | ○ | ✕ 1 기기·SDSS/Johnson 각 1 조합 |
| **이소크론 MCMC (step12)** | **○ ASteCA 실측 대조** — 어느 쪽도 우월하지 않음 | **△ g/r/i 에서는 해가 사전분포에 지배됨** | **✕ 1 성단·1 밴드조합** |
| SYSREM | **○ PySysRem 핵심 루프 대조** — 지배 성분 \|corr\| 0.90, 꼬리 성분은 가중 저계수 비유일성으로 구현 의존 (`validation/lc_crosscheck/`) | ○ | ✕ |
| PDM · 별칭 해소 | **○ PyAstronomy pyPDM 대조** — 최적 주기 격자 한 칸 이내, θ 상관 0.996 (`validation/lc_crosscheck/`) | ○ | ✕ |
| 다중밤 앙상블 | ✕ 무대조 | ○ | ✕ |

○ 근거 있음 · △ 부분 · ✕ 없음

---

## 읽는 법 — 세 줄 요약

1. **A 축(대조)이 채워진 것은 일곱이다** — Step 0(ccdproc 비트동일)·WCS
   솔버(ASTAP·astrometry.net)·step10 영점(PS1)·**PSF 측광(DAOPHOT ALLSTAR,
   2026-08-12)**·step12(ASteCA)·SYSREM(PySysRem)·PDM(pyPDM).
   > **2026-08-14 정정** — PSF 행은 재산출됐고 결론이 뒤집혔다. **ALLSTAR 가
   > 완전도·정밀도 둘 다, 그것도 완전도 전 구간에서 앞선다**(0.70 에서 0.0386
   > vs 0.0495). "APEX 는 완전도, DAOPHOT 은 정밀도" 라는 성격 구분은 픽셀 번호
   > 규약 결함이 만든 것이었다. **"APEX 만 다이얼을 제공한다" 도 아니다** —
   > ALLSTAR 출력을 `merr` 로 정렬하면 같은 다이얼이 되고, 모든 지점에서 낫다.

   **그중 우월성을 보인 것은 WCS 하나뿐**이고(잔차 1.42 배, 단 속도는 짐),
   나머지는 **동등성·무승부이거나 계통오차의 출처를 특정한 것**이다.
   PSF 는 **DAOPHOT 이 이긴다**(완전도 0.90 vs 0.70 · 산포 0.039 vs 0.050). 비어 있는 것은 강제측광 오케스트레이션 · 프레임 QC 게이트 ·
   구경보정 · 다중밤 앙상블이다.

   > **2026-08-12 정정** — 이 요약과 위 표가 2026-08-10 갱신 이후 4 개 행에서
   > 자기 갱신 이력과 어긋나 있었다(WCS·step10 이 표에서는 △, 이력에서는 ○;
   > 요약은 "둘뿐"). **표만 읽으면 APEX 의 근거를 실제보다 약하게 본다.**
   > 갱신 이력에 항목을 추가할 때 표와 요약을 같이 고칠 것.
2. **C 축(범용성)이 가장 약하다.** Step 0 을 빼면 **측광→CMD 전 사슬이
   한 카메라(Moravian C3-61000)에서만 돌았다.** 성단은 여럿이지만
   기기는 하나다 — 다중 성단은 다중 기기가 아니다.
3. **B 축(목적성)은 대체로 채워진다.** 오프라인 측성, raw 통합, 헤드리스
   재현성은 요구가 실재하고 APEX 가 답한다. 다만 **목적성만으로는 Section 15
   가 요구하는 "코드의 신규 요소 + 벤치마크 검증"에 미달**한다.

---

## 그래서 무엇을 하면 성적표가 바뀌는가

> **2026-08-11 정정 — 아래 U 밴드 논거는 사실이 아니다.**
> `lco_ngc6811_nights.txt` 의 U 96 / B 66 / V 94 는 프레임 수가 아니라 LCO
> API 의 추정값(`count_estimated`)이었다. 전수 페이징하면 **NGC 6811 의 U 는
> 0 장**이다(B 4 · V 4). 실사는 `validation/psf_crossinstrument/
> LCO_NGC6811_REALITY.md`.

**한 자료가 두 축을 동시에 올린다 — 다만 그 자료는 U 세트가 아니다.**
실제로 있는 것은 **2023-09-07 fa05(LCO 1m Sinistro, ELP) 103 장, gp/rp/ip/zs**
이고 raw 1.10 GB + BANZAI 4.23 GB = **5.33 GB · 약 30 분**이다. 이것을 쓰면:

- **C 축** — 측광→CMD 사슬이 **두 번째 기기**를 거친다. Step 0 만 가진
  다기기 근거가 사슬 전체로 확장된다
- **A 축** — BANZAI 산출물이 같이 오므로 **독립 파이프라인 대조가 공짜**다
- **원래 계획보다 강한 점** — APEX 가 **이미 자기 카메라로 처리한 같은
  성단**이다. "다른 데서도 돈다"가 아니라 **같은 별을 두 기기로 측광한 직접
  비교**가 되고, 필터계도 SDSS 로 일치해 영점·색항 논의가 그대로 이어진다
- **잃은 것** — U 밴드. step12 의 g/r/i 축퇴를 U 로 푸는 경로는 NGC 6811 로는
  불가능하다. 다른 성단을 찾아야 하며, `fetch_lco_clusters.py` 의 `m67_ubv`
  (M67/kb74/2015-04-01)가 후보이나 **미확인 — 확인 전 논문 문장 금지**

**받기 전 확인할 것**: 그 밤의 bias/dark/flat 공개 여부. 없으면 APEX 는
RLEVEL 91 에서 Step 1 부터 시작해야 하고, 그러면 "raw→science 통합"이 아니라
"측광 사슬만"의 다기기 근거가 된다.

나머지 A 축 구멍은 2026-08-10 에 대부분 메워졌다(아래 갱신 이력).
C 축은 여전히 자료가 없으면 어떤 작업으로도 못 메운다.

---

## 다음으로 확인할 것

2026-08-12 재점검에서 이 목록의 세 항목 중 둘이 **이미 해소된 것이었다**
(바로 아래 갱신 이력에 결과가 있다). 실제로 남은 것으로 교체한다.

1. ~~PSF 측광 A 축 — 표본 확대~~ → **2026-08-12 밤 완료** (400 개 재채점, 아래
   갱신 이력). ~~해석적 모형 주입 후속~~ → **2026-08-14 완료.** 독립 적합
   Moffat 으로 다시 심어도 APEX 완전도 불변(0.76→0.76)·DAOPHOT 정밀 우위
   유지(4/4 구간) — **두 결론 모두 주입 편향의 산물이 아니다.** PSF A 축의
   교란요인은 전부 닫혔고, 남은 한계는 1 기기·1 프레임뿐이다.
2. ~~PSF 모형 방식 다이얼 — 잠금 해제 + 검증~~ → **2026-08-14 완료.**
   잠금 두 곳(GUI 콤보박스·런타임 강제 되돌림)을 풀고 3 자 재실험을 돌렸다.
   같은 주입 자료·같은 별(n=187)에서 **APEX-Moffat 이 산포 0.051 → 0.041 로
   ALLSTAR(0.029)와의 격차를 절반 메우고, 완전도는 0.76 → 0.70 으로 내준다.**
   즉 모형 방식이 원인의 절반이다. **나머지 절반은 "같은 Moffat" 이 사실
   같지 않기 때문이다** — DAOPHOT 의 `psf.fits` 는 해석 Moffat **+ 119×119
   경험적 잔차 룩업표**(0 아닌 화소 79.7 %)이고 모수도 `PAR1=2.97 · PAR2=3.09 ·
   PAR3=−0.013` 로 **타원**이다. APEX 의 Moffat 은 순수 해석·원형이다.
   **DAOPHOT 은 APEX 두 모드의 중간이라 둘 다 이긴다.** (처음에 이 차이를
   "혼잡 처리" 로 적었으나 구간 패턴이 단조롭지 않아 철회했다 — 3–6 FWHM 처럼
   한산한 칸에서도 진다.)
   **정밀도↔완전도 다이얼이 수치로 실증됐다.**
   → **2026-08-14 후속: 원인은 모형이 아니라 보간이었다.** ePSF 를 그대로 두고
   격자 표본화만 선형→3차로 바꾸자(`psf_interp_order`) 산포가 0.050 → 0.041 로
   **해석 Moffat 과 같은 값**이 됐다. 하이브리드(`moffat_hybrid`)도 구현했으나
   0.039 로 이득이 작다. 즉 세 모형이 0.041/0.041/0.039 로 모이고, **모형 선택
   보다 보간 차수가 싸고 효과가 같다.** ALLSTAR 와의 남은 1.32× 는 모형으로도
   보간으로도 설명되지 않는다 — 적합 방식(동시 그룹 적합)이 다음 후보다. 산출물에 `psf_build_mode` 를
   기록하도록 했다(그전에는 ePSF 판과 Moffat 판이 사후 구분 불가였다).
   `validation/psf_engines/README.md` · `recovery_three_engines.csv`.
   → **2026-08-14 후속 2: 동시 그룹 적합도 아니었다 (음성 결과).** 그룹 반경을
   1.5→2.5 FWHM, 예산을 프레임의 10%→전부로 넓혀 **동시에 풀리는 별을 95 →
   564 개로 6 배** 늘렸다(같은 프레임 좌표에 `_build_groups` 를 직접 돌려 확인).
   결과는 **전체 산포 0.039 → 0.039 로 무변화**. 같은 별을 두 설정이 다 쟀으므로
   짝지어 물으면(162 개 중 실제로 값이 바뀐 별) **좋아짐 85 / 나빠짐 77,
   부호검정 p = 0.58**, |오차| 중앙 변화 −0.0009 mag [−0.0012, +0.0012] 로
   0 을 품는다. 비용도 178 s → 157 s 로, 다른 앱이 도는 노트북에서 각 1 회
   측정이라 판별 불가 — 즉 **이득도 대가도 없다**.
   1 회차만 봤을 때는 최혼잡 구간이 0.034 → 0.024 로 좋아 보였으나, 그 구간에서
   실제로 값이 바뀐 별은 23 개 중 2 개였고 2 회차를 합치자 사라졌다(0.084 →
   0.079). **강건 산포는 표본이 작으면 별 두 개로 움직인다** — 짝지은 검정을
   같이 보지 않으면 속는다.
   부수적으로 그룹 상한 60 은 무의미했다: M13 밀도에서 실제 최대 그룹은 4 라
   기존 상한 25 에도 안 닿는다. 효과를 내는 손잡이는 **반경과 예산**이다.
   `validation/psf_engines/grouper_paired_test.py` · `grouper_paired_test.csv`.
   → **2026-08-14 후속 3: "남은 1.32×" 라는 문제 설정 자체가 틀렸다.**
   그 수치에 불확실도를 붙이자 95% 구간이 [1.00, 1.72] 로 1 에 닿았다(위
   A 축 절의 정정 상자). 하나씩 분해한 결과는 이렇다.

   | 후보 | 판정 | 근거 |
   |---|---|---|
   | PSF 모형 (ePSF·Moffat·하이브리드) | 닫힘 | 셋이 0.039~0.041 로 모임 |
   | 보간 차수 | 닫힘 | 3 차로 모형 선택 이득 전부 회수 |
   | 동시 그룹 적합 | 닫힘 | 6 배 넓혀도 무변화, 짝검정 p=0.58 |
   | 적합창·구경보정 | 닫힘 | 별마다 같은 값(창 19 px, 보정 미적용) |
   | 배경 추정 | **모양 불일치로 탈락** | 배경 오차는 더하기라 어두울수록 커져야 하는데, 격차는 밝은 쪽에 있고 어두운 쪽에선 APEX 가 이긴다 |
   | 픽셀 위상 (별이 픽셀 안 어디에) | 닫힘 | \|잔차\| 와 무상관 (r≈0, p>0.5) |
   | 주입 자리 잔차광 | **공통항** | 두 엔진 기울기 구분 불가 — 상쇄됨 |
   | 겹친 별 처리 | **남음** | 최혼잡 구간에서만 +41 mmag (보정 p=0.038), 최한산 구간은 +0.2 mmag |

   **그래서 남은 것은 전역 정밀도 결손이 아니라 최혼잡 구간 하나다.** 그리고
   그 구간 별들은 넓히기 *전* 반경으로도 이미 함께 풀리고 있었으므로(그 구간
   23 개 중 값이 바뀐 별 2 개), **묶어서 푸는데도 진다** — 남은 후보는 묶는
   범위가 아니라 묶은 뒤의 처리, 즉 가중치 정의(APEX 1/σ² vs DAOPHOT 의
   잔차 기반 재가중)와 이웃 차감이다. 그리고 Moffat 은 원형이라(astropy Moffat2D 에 축비 없음)
   이 프레임의 타원율 중앙 1.085·p90 1.44 를 못 담는다.
   또 이 실험 과정에서 **그룹 상한과 예산이 호출부에 숫자로 박혀 있어**
   설정한 60 이 조용히 25 로 깎이고 프레임의 10% 만 그룹으로 풀리고 있었다.
   셋 다 설정으로 뺐다(`psf_grouper_budget_frac` · `psf_grouper_budget_cap`,
   기본값은 종전 동작 유지). 사용자가 못 만지는 손잡이는 감사도 못 한다.

3. **LCO fa05 103 장의 보정 프레임 공개 여부** — C 축의 유일한 실행 경로.
   없으면 RLEVEL 91 에서 Step 1 부터 시작해야 하고, 그러면 "raw→science 통합"이
   아니라 "측광 사슬만"의 다기기 근거가 된다.
4. **A 축이 아직 빈 넷** — 강제측광 오케스트레이션(재중심·포화·깊이 QC) ·
   프레임 QC 게이트 · 구경보정 워크플로 · 다중밤 앙상블. 앞 셋은 인공별
   주입으로 가능하고, 앙상블은 독립 대조 상대가 없어(AIJ 는 수동 GUI)
   **주장 자체를 하지 않는 쪽이 맞다.**

### 이 문서를 고칠 때의 규칙 (2026-08-12 추가)

갱신 이력에 항목을 넣으면 **성적표 표와 「읽는 법」 요약을 같은 커밋에서 함께
고친다.** 2026-08-10 갱신이 이력에만 반영돼 4 개 행이 두 달 가까이 어긋나
있었고, 그 표를 읽은 사람은 APEX 의 근거를 실제보다 약하게 본다.

---

## 갱신 이력

### 2026-08-12 · PSF 측광(step8)을 성적표에 추가 — 그리고 논문 자료가 이걸 안 쓰고 있었다

**구조적 발견 두 개가 먼저다.**

1. **step8 은 이 성적표에 아예 없었다.** WCS 다음으로 큰 자작 엔진(8,589 줄)
   인데 채점 대상에서 빠져 있었다. 위 표에 행을 추가했다.
2. **Phase 3 자료가 PSF 를 안 썼다.** `scripts/reprocess_batch.py` 가 1–7 단계
   다음에 step10 을 바로 부른다. 그 함수의 docstring 이 이유를 적어 두었다 —
   *"Step 8/9 not needed on the forced-aperture path"*. step10 이 Step7 TSV 와
   Step6 카탈로그만으로 돌아가니 맞는 말이고, **열린 성단에서는 합리적인
   선택이었다. 혼잡한 구상성단에서 재검토된 적이 없을 뿐이다.**

배선 자체는 이미 완성돼 있었다. `resolve_lightcurve_photometry_source()` 가
step8 서명(step7 인덱스 + 프레임별 tsv 크기·mtime)을 검사해 완전하면
`mag_psf` 를, 아니면 조리개를 쓴다. step8 을 헤드리스로 돌리자 step10 이
말없이 갈아탔다 — `Photometry = PSF | MAG = mag_psf`. **기능의 부재가 아니라
호출의 부재였다.**

**M13 실측** (Moravian C3-61000 · 2026-05-15 · 15 프레임 B/V/R · 60 s ·
시잉 2.72″). 같은 원본 프레임 · 같은 검출 · 같은 WCS · 같은 마스터 카탈로그 ·
같은 step10 코드. 바뀐 것은 step8 을 돌렸는지 뿐이다.

| 밴드 | 영점 산포 | 보정성 N | 혼잡/한산 잔차비 |
|---|---|---|---|
| B | 40.8 → **35.8** mmag | 433 → **513** | 1.46 → **1.12** |
| V | 37.8 → **31.9** mmag | 575 → **684** | 3.30 → **2.05** |
| R | 38.5 → **36.2** mmag | 593 → **722** | 2.45 → **1.84** |

적색거성열 폭 **76 → 58 mmag** (13 < V < 16.5, 능선 대비 MAD). CMD 에서
수평가지가 드러난다. 그림 `validation/paper/figures/fig_psf_vs_aperture_m13.png`,
스크립트 `validation/crowding_aperture_vs_psf.py` · `validation/fig_psf_vs_aperture_m13.py`.

**한산한 사분위는 오히려 아주 약간 나빠진다** (V 16.4 → 19.9 mmag). 결함이
아니라 예상된 거동이다 — 이웃이 없는 밝은 별에서는 조리개가 모형 불일치를
겪지 않는다. PSF 의 이득은 전적으로 혼잡한 쪽에서 나온다.

> **같은 날 정정 — 위 표는 M13 하나다. 네 성단을 다 돌리면 결론이 바뀐다.**
> 상세: `validation/PSF_VS_APERTURE_ALL_TARGETS.md`.
>
> 혼잡 진단 자체는 확정됐다 — 혼잡/한산 잔차비가 열린 성단(NGC 6811 · M67)
> 에서 1.0~1.3, 구상성단(M3 · M13)에서 2.1~3.5 이고 PSF 가 구상성단에서만
> 그걸 절반 가까이 줄인다. **그러나 영점 산포가 좋아진 것은 M13 뿐이다.**
> NGC 6811 V 는 24 → 51, M67 r 은 19 → 45 mmag 로 두 배가 된다.
>
> 원인의 절반은 찾았다. **프레임별 영점의 내부 산포가 성단×밴드 12 개 조합
> 전부에서 PSF 가 크다 — 예외가 없다.** PSF 는 한 프레임 안에서 조리개보다
> 잡음이 크고 대신 혼잡과 상관된 오차를 없앤다. 상관된 오차는 프레임을
> 합쳐도 안 지워지고 잡음은 지워지므로, 혼잡이 지배하면 순이득·아니면
> 순손실이다. **왜 잡음이 큰지는 규명 못 했고 문서에 미해결로 적었다.**
>
> 따라서 B 축 근거의 문장은 "PSF 가 낫다" 가 아니라 **"혼잡한 시야에서는
> PSF, 아니면 조리개 — 그래서 둘 다 제공한다"** 다.

**위 수치는 B 축 근거다.** 조리개 대 PSF 는 APEX 내부 비교이고, A 축이 묻는
것은 기존 엔진과의 대조다. **A 축은 같은 날 밤에 따로 채웠다 — 아래.**

### 2026-08-14 밤 · **원인 확정: 씨앗 병합이 겹친 쌍의 검출별을 지우고 있었다 — 수정·검증 완료**

최혼잡 구간의 +41 mmag 를 픽셀→씨앗→코드로 추적해 원인을 확정하고 고쳤다.

**메커니즘**: `merge_forced_catalog_seeds` 가 forced/미검출 부분집합만 후보로
받아서, 겹친 쌍에서 검출이 하나만 잡히면 8.72 px 반경 안의 forced 별이 그
검출을 차지해 좌표를 덮어썼다. **검출된 이웃은 후보 목록에 없어 자기 검출을
지킬 수 없었고, 씨앗이 소멸했다.** 이웃 빛은 모형에 없고, 주입별 적합이 그걸
흡수해 과대 측광(부호 중앙 −106 mmag)이 되고, 적합은 정상 수렴이라 플래그가
없다. 실측: 크게 남은 30 개 중 26 개가 씨앗에서부터 없음, 로그 matched=116 중
~27 개가 이 사례.

**수정**: 검출된 카탈로그 별을 문맥(`context_xy`)으로 일대일 경쟁에 참여시킴.
자기 검출을 먼저 차지하므로 forced 별은 추가 경로로 빠져 둘 다 씨앗을 받는다.
고립 사례 동작 불변(함수 테스트 고정). `apex/analysis/psf_policy.py`.

**검증 (400 개 재실행)**: 남은 빛 +0.236 → +0.060(ALLSTAR +0.015) · 값이
바뀐 29 개의 |오차| 중앙 296.9 → 80.5 mmag(좋아짐 25/나빠짐 4, p=0.0001) ·
짝지은 격차 +41.2 → +29.6 mmag. 추가로 통과→탈락 8 개의 원래 오차 중앙이
235 mmag — **조용히 틀리던 측정이 이제 게이트에 걸린다**(완전도 0.70→0.68 은
그 값이다).

**후속 (같은 날): 위치 잠금 비대칭도 원인이 아니었다 — 음성 결과.** 남은
편향은 쌍의 **합이 보존된 채 나누기만** 틀린 것이고(합 1.063, 별 1.036),
산포는 이웃의 위치 오차가 플럭스로 흡수되는 것이었다(이웃이 주입별 쪽이면
+18, 반대면 −29.8 mmag). 가설은 `position_fixed_mask=_cur_forced` —
카탈로그 별만 잠기는 비대칭이었다. `psf_forced_position_lock=never` 로
재실행하니 **셋 다 나빠진다**: 매칭 별 83→58(카탈로그 별이 정확한 위치에서
떠남), 나누기 0.976→**1.017**(과교정), 최혼잡 산포 0.055→**0.067**.
**잠그는 현재 동작이 옳다** — 기본값 유지, 손잡이만 남겼다.
덤으로 `_cur_fit_valid` 장부 버그(세대가 한 걸음 밀림)를 잡았다 — 잠금이
가리고 있었을 뿐 기본 설정에서도 잠재적이었다.

**후속 2 (같은 날): 원인은 가중치였다.** 가설을 하나씩 시험하는 대신 IRAF 가
실제로 쓴 파라미터 전체를 APEX 코드와 대조했다. APEX 의 적합 분산은
`배경² + 광자잡음` 뿐이고, IRAF `daopars` 에는 여기 없는 항이 넷 있다 —
**`proferr=5%`**(모형 자체의 오차) · `flaterr=0.75%` · `cliprange=2.5σ` ·
`clipexp=6`.

`proferr` 가 결정적이다. 실행값으로 계산하면 별 중심 화소 가중치가
**어두운 별에서는 1.26 배 차이인데 밝은 별(peak 20,000)에서는 66.5 배** 다.
1 FWHM 안쪽 가중치 비중은 11.8 % vs **2.0 %** — **DAOPHOT 은 밝은 별의 중심을
버리고 날개로 맞춘다.** APEX 는 광자잡음만 보므로 중심을 가장 신뢰한다.
어두운 별에서 APEX 가 이기고 밝은 별·겹친 별(중심이 곧 겹치는 곳)에서 지는
패턴이 그대로 설명된다.

`psf_profile_error_frac` 로 넣고(기본 0 = 종전 동작) IRAF 기본값 0.05 로
재실행하니 최혼잡 구간 편향 +24.7 → **+17.9 mmag**, 산포 0.0506 → **0.0461**.
ALLSTAR 와의 짝지은 격차는 **+41.2 → +29.6 → +21.3 mmag [−4.0, +39.1]** 로
**구간이 0 을 품고**, 이제 어느 혼잡 구간도 다중검정 보정 후 유의하지 않다.

**후속 3 (같은 날): 게이트를 맞추자 결론이 다시 뒤집혔다.** 위 수치들은 APEX
   게이트만 켠 채 ALLSTAR 의 무필터 출력과 비교한 것이었다 — **엔진이 아니라
   정책 차이를 잰 것이다.** 모든 엔진에서 사후 게이트를 끄고 채점하면 완전도는
   **APEX 0.98 > ALLSTAR 0.90 > photutils 0.85**, 최혼잡 산포는 **APEX 0.073 <
   ALLSTAR 0.083 < photutils 0.091**, 전체 산포만 ALLSTAR 0.051 vs APEX 0.056
   으로 10 % 앞선다. 짝지은 검정에서 ALLSTAR 와의 차이는 **전 구간 유의하지 않고
   전체가 0 을 품는다**(+0.8 mmag [−3.0, +4.1]). photutils 대비는 −10.9 mmag
   (p=1.4e−07) 로 APEX 우세. `--no-gates` 표가 A 축 정본이다.

   **남은 것**: 전체 같은 별 산포 0.056 vs ALLSTAR 0.051(유의하지 않음). 나머지 세 항
(`flaterr`·클리핑)과 원형 적합영역(DAOPHOT 452 px 원 vs APEX 361 px 정사각)은
아직 구현하지 않았다. 두 별을 다 적합해도
ALLSTAR 가 최혼잡 구간에서 조밀하다(0.037 vs 0.060). 수정 후 부호 중앙
+32.4 — 겹친 쌍 해에 잔여 계통이 있다. 다음 후보는 쌍 축퇴 처리(재중심 등).

**생산 영향 — 사용자 판단 필요**: 이 결함은 혼잡 시야의 실측에도 있었다.
M13 영점·CMD 등 step8 기반 산출물은 수정 전 코드로 계산된 것이므로,
혼잡 성단 재처리 여부와 시점을 정해야 한다.

### 2026-08-14 · **A 축 재산출 — IRAF 와 픽셀 번호 규약이 달랐고, 결론이 뒤집혔다**

아래 A 축 수치는 전부 다시 냈다. 원인은 통계도 모형도 아니고 한 가지다.

**IRAF 는 첫 픽셀을 1 로 세고 APEX·numpy·photutils 는 0 으로 센다. 두 하네스
모두 변환 없이 좌표를 주고받고 있었다.** 실제 픽셀로 확인했다 — 밝은 별 60 개의
중심은 APEX 좌표에서 0.13 px, 그 좌표를 1 부터로 읽으면 1.40 px 떨어진다.
재중심을 켠 ALLSTAR 가 진짜 별을 찾아낸 뒤 정확히 +1.0 을 보고한 것이 직접
증거다.

**피해가 큰 쪽은 인공별 대조다.** 이 비교는 매칭 반경 1.5 px 를 쓰는데,
규약 차이만으로 거리가 √2 = **1.414 px** 다. 반경을 거의 다 써버린다.

| 회차 | 보정 | 매칭수 | 거리 중앙 | 0.6 px 초과 |
|---|---|---|---|---|
| 1 | 없음(그동안) | 128 | 1.365 px | 119 |
| 1 | 있음 | **176** | 0.243 px | 43 |
| 2 | 없음(그동안) | 108 | 1.360 px | 102 |
| 2 | 있음 | **181** | 0.249 px | 50 |

ALLSTAR 완전도가 **236/400 = 0.59 → 357/400 = 0.89** 로 바뀐다. 아래 표의
헤드라인인 "APEX 가 더 건진다(0.76 vs 0.65)" 가 성립하지 않는다. 살아남은
매칭도 대부분(119/128)이 0.6 px 넘게 떨어져 있어 **이웃 별에 붙었을 수 있다.**
따라서 완전도·정밀도·혼잡 구간·짝지은 효과 크기까지 A 축 수치 전부가 이
결함 위에서 계산됐다.

**Step 7 대 IRAF 대조(논문 Fig 5 · fig_iraf_crosscheck)에도 같은 결함이 있다.**
다만 성격이 다르다. 그쪽은 위치가 아니라 행 순서(`iraf_id`)로 짝지으므로 엉뚱한
별에 붙는 사고는 없고, 재중심이 꺼져 있어 IRAF 조리개가 1.41 px 옆에 놓인다.
균일한 이동은 PSF 가 일정하면 모든 별에서 같은 비율로 빛을 잃어 **거의 순수한
영점**이 되고, 그 영점은 비교가 이미 빼고 있다. 남는 것은 산포뿐인데, 같은
프레임에서 직접 재보면 조리개 반지름에 따라 이렇다(M13, FWHM 7.05 px).

| 조리개 반지름 | = FWHM 배 | 등급차 중앙 | 산포 |
|---|---|---|---|
| 3 px | 0.43 | +94.6 mmag | 32.3 mmag |
| 5 px | 0.71 | +47.2 mmag | 14.6 mmag |
| 8 px | 1.13 | +14.2 mmag | 12.8 mmag |
| 10 px | 1.42 | +7.1 mmag | 17.9 mmag |

이 산포는 혼잡한 구상성단 값이고, 논문 실행(NGC 457 · NGC 6811)은 성긴
산개성단이라 더 작다 — 실제 보고된 산포가 4.3 · 9.7 mmag 로 위 표보다 작은
것이 그 증거다. **즉 논문의 일치도 수치는 과장이 아니라 보수적이다**(제대로
맞추면 더 좋아진다). 그래도 결함이므로 재실행 대상으로 남긴다.

**고친 곳**: `apex/benchmark/iraf_crosscheck.py` 의 `write_iraf_coords` ·
`parse_txdump`, `validation/psf_engines/daophot_allstar.py` 의 입력·출력 양쪽.
새 ALLSTAR 표에는 `coord_origin` 을 찍고, 그 표시가 없는 낡은 표는
`load_daophot()` 가 읽기를 거부한다. 회귀 테스트
`tests/test_iraf_pixel_origin.py` 6 개.

**재산출 결과 (인공별 400 개, 2 회차 합산)**

| APEX 게이트 | 건진 수 | 완전도 | 산포 | 같은 별에서 |
|---|---|---|---|---|
| 현재 기본 (qfit≤3 · chi2≤25) | 279 | 0.70 | 0.054 | APEX 0.0496 / ALLSTAR 0.0390 |
| 관대 (qfit≤6 · chi2≤50) | 340 | 0.85 | 0.066 | 0.0620 / 0.0504 |
| 더 관대 (qfit≤20 · chi2≤200) | 347 | 0.87 | 0.068 | 0.0620 / 0.0506 |
| 게이트 해제 | 391 | 0.98 | 0.083 | 0.0651 / 0.0590 |
| **IRAF ALLSTAR** | **358** | **0.90** | **0.059** | |

**ALLSTAR 가 완전도·정밀도 둘 다 앞선다.** APEX 를 완전도 0.87 까지 풀어도
산포 0.068 로 ALLSTAR 의 0.90/0.059 에 못 미친다. 게이트를 어떻게 놓아도 같은
별 비교에서 1.10~1.27 배 뒤진다 — **게이트 탓이 아니다.**

**"APEX 는 더 건진다" 는 사실이 아니었다.** APEX 는 400 개를 *전부* 측정하고
자기 게이트로 121 개를 버린다(유한등급 9 · `flags_psf` 43 · **`qfit` 65** ·
`reduced_chi2` 4). 버려진 별의 qfit 중앙은 3.80 으로 문턱 3.0 바로 위이고
SNR 중앙 6.01 이다. 즉 **능력이 아니라 정책**이고, 그 정책이 관대함 다이얼이다.

**A 축에 남는 참인 문장은 하나뿐이다 — 이 프레임에서 DAOPHOT ALLSTAR 가
APEX step8 보다 낫고, 그것도 완전도 전 구간에서 낫다.**

처음에는 "APEX 만 완전도를 고르는 다이얼을 제공한다" 를 차별점으로 적었으나,
**그것도 사실이 아니다.** ALLSTAR 의 출력에도 같은 다이얼을 걸 수 있다 — 자기가
보고한 오차(`merr`) 로 정렬해 잘라내면 된다. 같은 완전도에서 비교하면:

| 완전도 | APEX (`qfit` 순) | ALLSTAR (`merr` 순) |
|---|---|---|
| 0.50 | 0.0343 | **0.0267** |
| 0.60 | 0.0407 | **0.0317** |
| 0.70 | 0.0495 | **0.0386** |
| 0.80 | 0.0596 | **0.0486** |
| 0.87 | 0.0713 | **0.0557** |

**전 구간에서 진다.** 다이얼은 APEX 의 우위가 아니라 두 엔진 모두 가능한 사후
처리다. (ALLSTAR 의 `chi` 로 정렬하면 반대로 나쁘다 — 0.50 에서 0.0784. 판별자
선택이 곧 성능이며, 여기서 이기는 것은 `merr` 다.)

부수 정정: **APEX 의 현재 게이트 판별자 `qfit` 은 기본 동작점에서 이미 최적이다.**
완전도 0.70 에서 0.0495 로 `qfit_noise_ratio`(0.0574)·`reduced_chi2`(0.0675)를
앞선다. 앞서 "qfit 이 나쁜 판별자" 라고 적은 것은 완전도 0.895 한 지점만 보고
내린 판단이었다 — 그 지점에서만 역전된다(0.0720 vs 0.0672).

**짝지은 효과 크기는 구조가 유지된다**(공통 별 274 개, |APEX|−|ALLSTAR| 중앙):

| 혼잡 구간 | N | 중앙 차 | 95% 구간 | 보정 p |
|---|---|---|---|---|
| 0.75–1.5 FWHM | 37 | **+41.2 mmag** | [+13.0, +51.8] | **0.011** |
| 1.5–3 FWHM | 72 | −4.3 (APEX 우세) | [−13.8, +0.4] | 0.070 |
| 3–6 FWHM | 84 | +6.6 | [+0.6, +13.3] | 0.156 |
| 6–inf FWHM | 81 | −0.3 | [−6.3, +6.6] | 1.00 |
| 전체 | 274 | +2.2 | [−0.8, +6.8] | 0.40 |

좌표 보정 전(+41.4 · 보정 p=0.038)과 값이 같고 표본이 27→37 로 늘어 더
유의해졌다. **겹친 별 하나에 몰려 있고 한산한 별에서는 차이가 없다**는 결론은
결함과 무관하게 살아남았다.

**교훈**: 두 엔진을 비교하기 전에 좌표가 같은 것을 가리키는지부터 확인한다.
이건 로그에 아무 표시도 안 남고, 통계 분해를 아무리 정교하게 해도 안 나온다 —
모형·보간·그룹·배경·픽셀위상까지 다섯 후보를 닫고 나서야 좌표를 봤다.

### 2026-08-12 밤 · PSF 측광 A 축 ✕→○ — DAOPHOT ALLSTAR 대조

`validation/psf_engines/`. IRAF DAOPHOT 의 `phot`→`pstselect`→`psf`→`allstar`
사슬을 WSL PyRAF 로 돌리고, **같은 프레임에 심은 인공별 400 개**(진리값 존재,
혼잡 4 구간 × SNR 5 단계)로 두 엔진을 채점했다. 적합창은 APEX 에 맞춘
1.7·FWHM(이 교란요인이 결론을 안 바꾼다는 것은 별도 확인).

**전체를 한 줄로 보면 동등해 보이지만(같은 별 223 개에서 0.050 vs 0.048)
구간으로 쪼개면 갈린다.**

| | APEX step8 | IRAF ALLSTAR |
|---|---|---|
| 유지 (400 중) | **304 (0.76)** | 260 (0.65) |
| target SNR 10 유지 | **0.81** | 0.45 |
| 같은 별, 0.75–1.5 FWHM | 0.087 | **0.029** |
| 같은 별, 6–inf FWHM | 0.036 | **0.023** |

**APEX 는 더 건지고(특히 어두운 별), DAOPHOT 은 건진 것을 더 정확히 잰다
(같은 별에서 1.6~3.0 배).** 우월성 주장이 아니라 성격 차이다.

> **2026-08-14 정정 — 위 "1.6~3.0 배" 는 불확실도 없이 적힌 수치다.**
> 산포 비에 짝지은 부트스트랩 구간을 붙이자 전체 1.34 의 95% 구간이
> **[1.00, 1.72]** 로 1 에 닿고, 짝지은 |오차| 부호순위 검정은 **p = 0.14** 로
> 유의하지 않다. 산포 비는 짝짓기를 버리는 약한 통계라 이 표본 크기에서
> "차이 없음" 과 "1.5 배" 를 구분하지 못한다.
>
> 같은 별에서 별마다 물으면 구조가 드러난다(2 회차 · 공통 별 186 개):
>
> | 혼잡 구간 | N | \|APEX\|−\|ALLSTAR\| 중앙 | 95% 구간 | 보정 p |
> |---|---|---|---|---|
> | 0.75–1.5 FWHM | 27 | **+41.4 mmag** | [+5.5, +54.7] | **0.038** |
> | 1.5–3 FWHM | 47 | −4.0 | [−13.6, +3.9] | 0.32 |
> | 3–6 FWHM | 62 | +8.0 | [+1.2, +15.5] | **0.029** |
> | 6–inf FWHM | 50 | **+0.2** | [−5.0, +4.1] | 1.00 |
>
> **가장 한산한 별에서는 두 엔진이 같고(+0.2 mmag), 가장 겹친 별에서만
> 벌어진다(+41 mmag).** 전체 p 가 0.14 였던 것은 방향이 반대인 구간들이
> 상쇄해서다. 따라서 옳은 문장은 "DAOPHOT 이 전반적으로 정밀하다" 가 아니라
> **"겹친 별에서 DAOPHOT 이 정밀하고, 떨어진 별에서는 차이가 없다"** 다.
> `validation/psf_engines/gap_decompose.py` · `gap_paired_effect.csv`.
>
> 같은 분해에서 나온 두 가지를 더 적어 둔다.
> ① **두 엔진이 보고하는 오차가 거의 같다**(중앙 0.0223 vs 0.0230 mag).
> 즉 정보를 덜 뽑는 게 아니라, APEX 만 자기 오차모형이 모르는 흩어짐을 더
> 갖는다(chi 1.47 vs 1.20).
> ② **이 벤치마크 산포의 약 40% 는 엔진이 아니라 주입 자리에 이미 있던
> 잔차광이다.** 두 엔진의 잔차가 그 양에 같은 기울기로 끌려간다(−0.93
> [−1.24, −0.68] vs −0.88 [−1.14, −0.66], 구분 불가). 그 추세를 빼면 산포가
> 0.042 → 0.026 · 0.031 → 0.020 으로 내려간다. **인공별 실험의 절대 산포를
> 엔진 성능으로 읽으면 안 된다.**

**이것이 위 B 축의 미해결 질문에 답한다.** APEX 의 PSF 가 프레임 내부 산포에서
12/12 조합 모두 조리개보다 나빴는데 이유를 몰랐다. 같은 픽셀에서 DAOPHOT 이
더 조밀하므로 **원인은 자료가 아니라 APEX 의 모형·적합 정책이다.**

**SNR 5 의 −0.232 mag 는 결함이 아니라 품질컷의 선택 효과다.** 게이트 전
전체는 +0.024 로 무편향이고, `snr_psf ≥ 3` 컷이 어둡게 튄 별만 잘라내 남은
표본이 밝아진다(잘린 것들의 중앙 +0.221). 검출 한계의 고전적
Malmquist/Eddington 편향이다. ALLSTAR 은 측정 SNR 이 아니라 적합 수렴으로
버리므로 등급 의존적으로 잘리지 않는다. **완전도 그림은 무사하다** — 그 회수 정의가 Step 4 검출 기준이라
( 의 recovered + detection_index) 이 편향을 물려받지 않는다.
확인함.

~~남은 교란요인: 인공별의 모양이 APEX 의 ePSF 에서 온다~~ →
**2026-08-14 통제 실험으로 해소.** 프레임의 실제 별 40 개에 독립 적합한
Moffat(γ=7.01, α=3.16 — APEX ePSF 도 DAOPHOT 의 moffat15 도 아님)으로
400 개를 다시 심었다. **APEX 완전도는 전혀 안 움직였고(0.76→0.76,
SNR 10 에서 0.81→0.80) 정밀도 우위는 4/4 혼잡구간 전부로 넓어졌다**
(같은 별 matched 0.056 vs 0.032, 1.77 배). 즉 "APEX 는 더 건지고
DAOPHOT 은 더 정밀하다"는 **주입 모양과 무관하게 성립** — 세 교란요인
(적합창·varorder·nclean)에 이어 마지막 것까지 닫혔다.
`recovery_comparison_pooled_moffat.csv`. 남은 한계는 프레임 1 장뿐.

**정정** — 2026-08-11 에 인라인으로 냈던 혼잡 상관 `r = -0.459`, 혼잡/한산
`4.62 배`는 스크립트를 남기지 않아 재현되지 않는다. 정본은
`crowding_aperture_vs_psf.py` 이고 밴드별로 낸다(위 표).

**부수 발견 — step10 표준화 발산 버그 (수정 `1134260`).** PSF 로 돌리자 별
하나가 `mag_std_B = -844` 로 나왔다. 측광은 멀쩡했고(SNR 14.6) 표준화가
발산했다. `solve_standard_colors` 의 수렴 논증은 선형 모형의 것인데, 2차항이
붙으면서 축소율이 `|ct + 2·ct2·C|` — **계수가 아니라 별의 색에 달린 값** — 이
되었다. 계수만 묶는 기존 방어(|ct|≤0.8, |ct2|≤0.25)로는 못 막는다. 같은 결함이
구경 경로에도 있었고 그쪽은 최소 색이 우연히 수렴 영역 안이었을 뿐이다.
적합 색 범위 클램프 + 미수렴 시 NaN 으로 고쳤다.

### 2026-08-10 · 내장 WCS 솔버 A 축 △→○

`validation/wcs_engines/` — M13 8 프레임에서 internal 0.346 px vs ASTAP/anet
0.493 px (8/8 프레임 우위, 1.42 배). 단 속도는 ASTAP 가 3.6 배 빠르고,
매칭 수 상한(600 vs 679–1,147) 교란요인이 미해결. C 축은 여전히 ✕.

### 2026-08-10 · step10 영점 A 축 △→○ — 앵커 계통오차 실측

`validation/zp_anchor_ps1_m67.py` → `validation/paper/data/zp_anchor_ps1_m67.json`.
M67 보정 카탈로그 968 별을 PS1 DR2 에 매칭, 색항을 적합에 흡수한 뒤
중앙 색에서의 오프셋(14.0 < PS1 < 16.5, 3σ 클립):

| 밴드 | APEX−PS1 | Jordi 참조−PS1 | APEX 자체 전달 오차 |
|---|---:|---:|---:|
| g | **+45.1 mmag** | +49.2 | −4.1 |
| r | −0.7 | +2.7 | −3.4 |
| i | **−30.7 mmag** | −24.4 | −6.3 |

**판정** — APEX 의 영점·색항 기계는 참조를 ≤6 mmag 로 전달한다(엔진 무죄).
계통오차는 **Jordi+2010 변환 앵커 자체**에서 오며, 색으로는 (g−r) +46 ·
(g−i) +76 mmag 다. PS1 을 참으로 잡으면 M67 이소크론 적합의 소광 초과분
약 1/3 이 이 색 오프셋으로 설명되고 나머지가 축퇴다. 부호(+45 = 더 어두움)는
거리지수 rail 의 "영점이 밝다" 가설과 반대 — 전향점 검사와 일관.

**한계** — PS1 과 SDSS 도 동일 계가 아니므로 이것은 "Jordi 가 틀렸다"가 아니라
**두 참조 선택지 간 차이의 실측**이다. 절대 판정에는 M67 실측 표준성
(Clem 2007 u′g′r′i′z′ 등)이 필요 — 후속.

### 2026-08-10 · PDM·SYSREM A 축 ✕→○ / QC 게이트 판정 (계획 2·3·4 완료)

- **PDM** — pyPDM 과 최적 주기 격자 한 칸 이내(Δ≤2.8×10⁻⁴ d), θ 상관 0.996+.
- **SYSREM** — PySysRem 핵심 루프와 지배 성분 |corr| 0.90. 꼬리 성분 불일치는
  가중 저계수 근사의 비유일성이며, "성분별 제거량 보고"가 다음 개선 지점.
- **depth-cost 게이트** — DES t_eff(Neilsen+ 2016)가 직접 선례. 신규성 철회,
  인용·적응 서술로 전환. 초과검출 게이트는 두 차례 검색에서 선례 미발견
  (조건부 주장 유지).
- **남은 A 축 ✕**: 강제측광 오케스트레이션(재중심·포화·깊이 QC), 구경보정
  워크플로, 다중밤 앙상블 — 앞 둘은 인공별 주입으로, 앙상블은 독립 대조
  상대가 없어(AIJ 는 수동 GUI) 주장 자체를 하지 않는 쪽이 맞다.
